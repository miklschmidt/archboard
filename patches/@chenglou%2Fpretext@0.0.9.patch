diff --git a/dist/layout.d.ts b/dist/layout.d.ts
index b16902b637b67de5000ca6b26179488b04459e71..07cb8684c1928024e95c0bb751df5c35f6bee828 100644
--- a/dist/layout.d.ts
+++ b/dist/layout.d.ts
@@ -73,3 +73,4 @@ export declare function clearCache(): void;
 export declare function setLocale(locale?: string): void;
 export {};
 //# sourceMappingURL=layout.d.ts.map
+export { setMeasureFunction } from './measurement.js';
diff --git a/dist/layout.js b/dist/layout.js
index 5e54b366daefb8af90d0af8f6a8891c4e1214ac3..e67fb79dbec54417fdd4c28072a9e3d4a1e5f952 100644
--- a/dist/layout.js
+++ b/dist/layout.js
@@ -366,3 +366,5 @@ export function setLocale(locale) {
     setAnalysisLocale(locale);
     clearCache();
 }
+
+export { setMeasureFunction } from './measurement.js';
diff --git a/dist/measurement.d.ts b/dist/measurement.d.ts
index 24330a40505cedf334e6e19b9ad5a6ee59077bf1..d2feac53046e7da92ecea22b0054d1baf51c1f66 100644
--- a/dist/measurement.d.ts
+++ b/dist/measurement.d.ts
@@ -26,3 +26,5 @@ export declare function getFontMeasurementState(font: string, needsEmojiCorrecti
 };
 export declare function clearMeasurementCaches(): void;
 //# sourceMappingURL=measurement.d.ts.map
+/** Bind a synchronous measurement backend; changing it invalidates cached widths. */
+export declare function setMeasureFunction(measure: ((text: string, font: string) => number) | null): void;
diff --git a/dist/measurement.js b/dist/measurement.js
index e264e15c93f17cfa91d9436f7e2196272286539f..866e8732cdcd9b65ba1f975956021ba725ce5324 100644
--- a/dist/measurement.js
+++ b/dist/measurement.js
@@ -2,6 +2,14 @@ import { getSharedGraphemeSegmenter } from './analysis.js';
 let measureContext = null;
 const segmentMetricCaches = new Map();
 let cachedEngineProfile = null;
+let customMeasureFunction = null;
+let activeFont = '';
+/** Bind a synchronous measurement backend; changing it invalidates cached widths. */
+export function setMeasureFunction(measure) {
+    customMeasureFunction = measure;
+    clearMeasurementCaches();
+    cachedEngineProfile = null;
+}
 // Safari's prefix-fit policy is useful for ordinary word-sized runs, but letting
 // it measure every growing prefix of a giant segment recreates a pathological
 // superlinear prepare-time path. Past this size, switch to the cheaper
@@ -34,9 +42,10 @@ export function getSegmentMetricCache(font) {
 export function getSegmentMetrics(seg, cache) {
     let metrics = cache.get(seg);
     if (metrics === undefined) {
-        const ctx = getMeasureContext();
         metrics = {
-            width: ctx.measureText(seg).width,
+            width: customMeasureFunction === null
+                ? getMeasureContext().measureText(seg).width
+                : customMeasureFunction(seg, activeFont),
         };
         cache.set(seg, metrics);
     }
@@ -195,10 +204,13 @@ export function getSegmentBreakableFitAdvances(seg, metrics, cache, emojiCorrect
     return metrics.breakableFitAdvances;
 }
 export function getFontMeasurementState(font, needsEmojiCorrection) {
-    const ctx = getMeasureContext();
-    ctx.font = font;
+    activeFont = font;
+    if (customMeasureFunction === null) {
+        const ctx = getMeasureContext();
+        ctx.font = font;
+    }
     const cache = getSegmentMetricCache(font);
-    const emojiCorrection = needsEmojiCorrection ? getEmojiCorrection(font) : 0;
+    const emojiCorrection = customMeasureFunction === null && needsEmojiCorrection ? getEmojiCorrection(font) : 0;
     return { cache, emojiCorrection };
 }
 export function clearMeasurementCaches() {
diff --git a/src/layout.ts b/src/layout.ts
index 9c28b49088fe5530cc54499f67cb43f77d8e3a11..c0660952d69e83417c0c5acf4fa54d37c19a0848 100644
--- a/src/layout.ts
+++ b/src/layout.ts
@@ -693,3 +693,5 @@ export function setLocale(locale?: string): void {
   setAnalysisLocale(locale)
   clearCache()
 }
+
+export { setMeasureFunction } from './measurement.js';
diff --git a/src/measurement.ts b/src/measurement.ts
index 75dfe0bea47d2b6edec8ba66c6af759a112b9983..d962086271d4b0fada03a0e93c7178e6af85b511 100644
--- a/src/measurement.ts
+++ b/src/measurement.ts
@@ -20,3 +20,12 @@ export type BreakableFitMode = 'sum-graphemes' | 'segment-prefixes' | 'pair-cont
 let measureContext: CanvasRenderingContext2D | OffscreenCanvasRenderingContext2D | null = null
 const segmentMetricCaches = new Map<string, Map<string, SegmentMetrics>>()
 let cachedEngineProfile: EngineProfile | null = null
+let customMeasureFunction: ((text: string, font: string) => number) | null = null
+let activeFont = ''
+
+/** Bind a synchronous measurement backend; changing it invalidates cached widths. */
+export function setMeasureFunction(measure: ((text: string, font: string) => number) | null): void {
+  customMeasureFunction = measure
+  clearMeasurementCaches()
+  cachedEngineProfile = null
+}
@@ -59,9 +68,10 @@ export function getSegmentMetricCache(font: string): Map<string, SegmentMetrics>
 export function getSegmentMetrics(seg: string, cache: Map<string, SegmentMetrics>): SegmentMetrics {
   let metrics = cache.get(seg)
   if (metrics === undefined) {
-    const ctx = getMeasureContext()
     metrics = {
-      width: ctx.measureText(seg).width,
+      width: customMeasureFunction === null
+        ? getMeasureContext().measureText(seg).width
+        : customMeasureFunction(seg, activeFont),
     }
     cache.set(seg, metrics)
   }
@@ -252,9 +262,12 @@ export function getFontMeasurementState(font: string, needsEmojiCorrection: bool
   cache: Map<string, SegmentMetrics>
   emojiCorrection: number
 } {
-  const ctx = getMeasureContext()
-  ctx.font = font
+  activeFont = font
+  if (customMeasureFunction === null) {
+    const ctx = getMeasureContext()
+    ctx.font = font
+  }
   const cache = getSegmentMetricCache(font)
-  const emojiCorrection = needsEmojiCorrection ? getEmojiCorrection(font) : 0
+  const emojiCorrection = customMeasureFunction === null && needsEmojiCorrection ? getEmojiCorrection(font) : 0
   return { cache, emojiCorrection }
 }
