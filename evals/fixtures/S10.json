{
	"$comment": "S05's pipeline as Flask 3.0.0 calls it: full_dispatch_request calls finalize_request, which calls make_response and then process_response. Traffic is already illustrated on the response path, so turning it off is a real change.",
	"registerRepo": true,
	"steps": [
		{
			"op": "new",
			"board": "Flask request pipeline",
			"input": {
				"level": "service",
				"nodes": [
					{
						"name": "WSGI server",
						"kind": "external",
						"responsibility": "Calls the application once per request"
					},
					{
						"name": "Flask app",
						"kind": "app",
						"responsibility": "The WSGI application object",
						"binding": { "repo": "$FLASK", "path": "src/flask/app.py" }
					},
					{
						"name": "Flask.wsgi_app",
						"kind": "function",
						"parent": "Flask app",
						"responsibility": "Pushes the request context and dispatches",
						"binding": { "repo": "$FLASK", "path": "src/flask/app.py" }
					},
					{
						"name": "full_dispatch_request",
						"kind": "function",
						"parent": "Flask app",
						"responsibility": "Preprocess, dispatch and finalize one request",
						"binding": { "repo": "$FLASK", "path": "src/flask/app.py" }
					},
					{
						"name": "finalize_request",
						"kind": "function",
						"parent": "Flask app",
						"responsibility": "Turns the view's return value into the response and post-processes it",
						"binding": { "repo": "$FLASK", "path": "src/flask/app.py" }
					},
					{
						"name": "make_response",
						"kind": "function",
						"parent": "Flask app",
						"responsibility": "Turns a view's return value into a Response",
						"binding": { "repo": "$FLASK", "path": "src/flask/app.py" }
					},
					{
						"name": "process_response",
						"kind": "function",
						"parent": "Flask app",
						"responsibility": "Runs after_request functions and saves the session",
						"binding": { "repo": "$FLASK", "path": "src/flask/app.py" }
					}
				],
				"edges": [
					{
						"from": "WSGI server",
						"to": "Flask.wsgi_app",
						"kind": "call",
						"label": "environ, start_response",
						"emphasis": "hero"
					},
					{
						"from": "Flask.wsgi_app",
						"to": "full_dispatch_request",
						"kind": "call",
						"description": "The hot path every request takes"
					},
					{
						"from": "full_dispatch_request",
						"to": "finalize_request",
						"kind": "call",
						"label": "rv",
						"emphasis": "muted"
					},
					{
						"from": "finalize_request",
						"to": "make_response",
						"kind": "call",
						"label": "rv"
					},
					{
						"from": "finalize_request",
						"to": "process_response",
						"kind": "call",
						"label": "response",
						"traffic": {}
					}
				]
			}
		}
	]
}
