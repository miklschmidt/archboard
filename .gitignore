# Dependencies
node_modules/

# Build artifacts
dist/
public/dist/

# Environment files
.env

# Logs
*.log

# Editor artifacts
.cursor/
.agent/

# Development artifacts
*.excalidraw

.DS_Store

# --- archboard ---
# Committed architecture diagrams are a deliverable, not a build artifact.
!diagrams/*.excalidraw

# Agent skills are derived, not authored, so they stay untracked:
#   .agents/skills/  third-party skills pinned by skills-lock.json, plus our
#                    own skills synced from the tracked skills/ directory
#   .claude/skills/  symlinks into .agents/skills/
# Everything else under .claude/ and .agents/ (settings.json, commands/,
# agents/) is authored configuration and IS tracked.
.agents/skills/
.claude/skills/

# Agent worktrees, created per subagent run and removed after.
.claude/worktrees/
