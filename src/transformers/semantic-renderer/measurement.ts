// Placement and focused runtime checks consume the same measured-text contract.
export { measureArchitecture } from "@/transformers/semantic-renderer/lib/measurement";
export type {
	MeasuredArchitecture,
	MeasuredNode,
	MeasuredLabel,
	TextRun,
} from "@/transformers/semantic-renderer/lib/drawing";
