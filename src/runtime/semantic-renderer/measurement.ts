// Placement and focused runtime checks consume the same measured-text contract.
export { measureArchitecture } from "@/runtime/semantic-renderer/lib/measurement";
export type {
	MeasuredArchitecture,
	MeasuredNode,
	MeasuredLabel,
	TextRun,
} from "@/runtime/semantic-renderer/lib/drawing";
