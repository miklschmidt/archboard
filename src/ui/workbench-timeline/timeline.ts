import { MessagePartPrimitive, MessagePrimitive, ThreadPrimitive } from "@assistant-ui/react";

/** Private bridge that keeps the reviewed package imports in their sole policy owner. */
export const assistantTimelinePrimitives = Object.freeze({
	ThreadPrimitive,
	MessagePrimitive,
	MessagePartPrimitive,
});
