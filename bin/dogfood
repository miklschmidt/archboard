#!/usr/bin/env bash
# Run the CLI against this checkout's permanent architecture vault.
set -euo pipefail
archboard_checkout="$(dirname "$(dirname "$(realpath "$0")")")"
export ARCHBOARD_VAULT="$archboard_checkout/.archboard/vault"
exec "$archboard_checkout/bin/canvas" "$@"
