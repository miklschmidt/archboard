---
id: TASK-138
title: Restore GitHub Actions CI and keep main green
status: In Progress
assignee:
  - '@codex'
created_date: '2026-08-29 16:14'
updated_date: '2026-08-30 04:18'
labels: []
dependencies: []
references:
  - 'https://github.com/miklschmidt/archboard/actions/runs/33100307429'
  - .github/workflows/ci.yml
  - docs/agents/test-suite.md
  - 'https://github.com/miklschmidt/archboard/actions/runs/33268988057'
  - 'https://github.com/miklschmidt/archboard/actions/runs/33270430736'
  - 'https://github.com/miklschmidt/archboard/actions/runs/33273256248'
  - 'https://github.com/miklschmidt/archboard/actions/runs/33276279206'
  - 'https://github.com/miklschmidt/archboard/actions/runs/33279315414'
  - 'https://github.com/miklschmidt/archboard/actions/runs/33281484674'
  - 'https://github.com/miklschmidt/archboard/actions/runs/33283664973'
  - 'https://github.com/miklschmidt/archboard/actions/runs/33288114113'
  - 'https://github.com/miklschmidt/archboard/actions/runs/33289473418'
  - 'https://github.com/miklschmidt/archboard/actions/runs/33291111341'
modified_files:
  - .github/workflows/ci.yml
  - AGENTS.md
  - docs/agents/test-suite.md
  - src/runtime/board-inspection/tests/comparison-limits.test.ts
  - src/runtime/board-inspection/tests/sweep-filtering.test.ts
  - src/runtime/engine/tests/board-version-note.test.ts
  - src/shared/timing/timing.ts
  - tests/system/board-inspection/package-limits.test.ts
  - tests/system/board-inspection/package-totality.test.ts
  - tests/system/boards/malformed-input.test.ts
  - tests/system/browser/run-browser-lane.ts
  - tests/system/browser/support/agent-browser.ts
  - tests/system/canvas-state/hot-reload.test.ts
  - tests/system/code-targets/activation-contract.test.ts
  - tests/system/code-targets/launcher-lifecycle.test.ts
  - tests/system/code-targets/opener-persistence.test.ts
  - tests/system/code-targets/support/opener-fixture.ts
  - tests/system/repository-policy/boundaries.test.ts
  - tests/system/repository-policy/ci-browser-gate.test.ts
  - tests/system/repository-policy/support/test-inventory.ts
  - tests/system/repository-policy/test-inventory.test.ts
  - tests/system/support/owned-canvas.test.ts
  - tests/system/support/owned-canvas.ts
priority: high
type: bug
ordinal: 154000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
GitHub Actions was stale and repeatedly failed on the repository current complete gate. Maintainers need the public push gate to cover the fullest reliable hosted subset while the public local bun run check remains complete. Any hosted-only exception must be backed by exact runner evidence, name the omitted owner visibly, remain fail-closed and repository-policy enforced, and leave lint, formatting, both TypeScript checks, modules, system, repository inventory, and every viable browser owner required.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 The currently failing GitHub Actions run is diagnosed from its public workflow and job logs, with the actionable cause recorded.
- [ ] #2 The complete local bun run check remains unchanged and passes; any user-authorized hosted-only exception names the exact excluded owner, cites repeated runner evidence, and is enforced by repository policy.
- [ ] #3 The completed main branch is pushed to origin only after the local fix is verified.
- [ ] #4 Every required GitHub Actions check for the pushed main commit completes successfully.
- [ ] #5 Any subsequent CI-only failure is diagnosed and repaired or narrowly excepted under the recorded hosted-only policy in a monitored push-and-check loop until the pushed commit is green.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Update `.github/workflows/ci.yml` to keep one sequential Ubuntu job, replace stale `actions/checkout@v4` with `@v7`, retain package-pinned Bun through `oven-sh/setup-bun@v2`, frozen dependency installation, and pinned `agent-browser@0.34.0`, explicitly install `strace`, then install Chrome and Linux libraries with `agent-browser install --with-deps`. Remove the standalone frontend build and directory assertion because the canonical `test:serial-browser` lane inside `bun run check` owns frontend freshness and builds once when needed. Leave `bun run check` as the workflow's only package-script invocation.
2. Strengthen the existing CI repository policy in `tests/system/repository-policy/support/test-inventory.ts` and `test-inventory.test.ts`: require exactly one `bun run check` and reject any other direct package-script invocation, with mutation diagnostics for missing, duplicate, direct-build, and direct-test drift. The browser adapter and inventory remain the fail-closed enforcement for the 15 literal owners and their prerequisites.
3. Run the focused fixed-base CLI case and repository-policy owner, then one sequential `bun run check`, keeping logs under `/tmp` and auditing cleanup. This exercises lint, formatting, both TypeScript projects, modules, serial system owners, repository inventory/policy, and all 15 serial headless browser owners.
4. Commit the repair and TASK-138 plan/evidence conventionally. Fetch `origin/main`, prove it remains an ancestor of local `main`, and push without force.
5. Monitor GitHub Actions for the exact pushed SHA until terminal. For a failure, inspect the workflow and job logs, apply the smallest repair, rerun proportionate local gates, commit, push, and repeat on the new SHA. Do not omit or soften a lane.
6. After a green pushed repair, read the finalization guide, verify each acceptance criterion from objective evidence, finalize TASK-138, commit and push that metadata, and monitor the final pushed SHA to green.

7. Browser executable isolation amendment. After agent-browser install --with-deps, CI resolves the exact downloaded Chrome executable and exports it as AGENT_BROWSER_EXECUTABLE_PATH for the one bun run check process. tests/system/browser/run-browser-lane.ts validates that configured path before any frontend build or owner acquisition and returns could-not-run exit 2 with an actionable diagnostic when it is missing, not a file, or not executable. The adapter passes that exact path through browserTestEnvironment into each isolated owner while retaining the per-owner HOME/XDG/socket/session/namespace contract; canvas and other child environments continue to strip browser configuration. A focused fake/preflight regression proves the executable reaches an isolated owner and that missing/non-executable configured paths stop before build/owner acquisition. The workflow also declares top-level permissions: contents: read and retains the 30-minute timeout.

8. Review remediation. Keep AGENT_BROWSER_EXECUTABLE_PATH optional for documented local PATH-based use, but validate and normalize it strictly before build when configured. Delete every owner argv substitution hook; test the exported environment seam and canonical adapter behavior without changing owner selection. Parse workflow run steps so only one executable step equal to bun run check is accepted, while comments and quoted output cannot satisfy policy. Preserve independent mutation cases under the 500-line owner cap. Re-run focused policy/browser coverage and one clean complete check, then commit for rereview without pushing or finalizing.

9. Second review remediation. Replace the partial command-start scanner with fail-closed detection over YAML run scalars after quoted text and shell comments are removed. Reject unquoted bun run tokens behind env/command wrappers, grouping, conditionals, and pipelines while ignoring echo-only text. Split each predecessor inventory mutation family into its own Bun test, keep every owner below 500 lines, and run repository-policy plus type, lint, format, and diff checks. Do not rerun browser or the full gate unless these focused checks expose wider impact.

10. Quoted command-substitution amendment. Keep single-quoted shell text inert, but retain the bodies of dollar-parenthesis and backtick command substitutions inside double quotes so executable bun run tokens cannot hide there. Add separate negatives for echo with dollar-parenthesis, assignment with dollar-parenthesis, and double-quoted backticks while retaining the inert quoted and echo controls. Run the two focused policy owners and static checks only, then commit for rereview without pushing or finalizing.

11. Dollar-substitution comment amendment. Add shell-comment state to dollarSubstitutionAt using the scanner boundary rule so quotes and parentheses inside a comment cannot close the substitution before newline. Keep the unterminated-body fail-closed fallback. Add the valid multiline Bash form with a commented closing parenthesis as its own negative, run both focused policy owners and static checks, then commit without browser/full validation, push, or finalization.

12. Clean-runner module-owner remediation. Reproduce the exact pushed CI failure with ARCHBOARD_VAULT absent, then make board-version-note.test.ts own a temporary vault before the first config-sensitive dynamic import. Preserve and restore a caller-provided sentinel exactly, delete an originally absent variable, remove the owned root, and retain Bun module isolation. Verify unset and sentinel-focused runs, the module lane and proportional static gates, then run one clean bun run check with ARCHBOARD_VAULT explicitly unset. Commit for integration review without pushing or finalizing.

13. Unset-vault system-owner amendment. The complete unset full gate exposed three additional same-process test-owner failures after the module repair: one boards case and two code-target default-dependency cases. Give each owner a temporary vault before its first config-sensitive import, assert the imported config uses that owned root, dispose its canvas/server/fixture resources before removing the root, and restore or delete the caller environment exactly. Verify the two owners with both absent and caller-sentinel environments, then run the full unset system lane, static gates, and the clean complete gate; do not change production, default missing-vault behavior, or workflow environment.

14. Hosted-runner lifecycle-ceiling amendment. Preserve the exact 4,096 path-segment/2,048 zero-segment assertions and the exact attempted comparison 2,000,001 plus retained-finding assertions; these are the product/resource contracts. Move only the Bun test lifecycle caps into src/shared/timing/timing.ts: a focused sweep cap sized above the measured 5.274-second hosted case, a terminal module cap sized above the measured 27.094-second hosted case, and a package cap derived for its two sequential terminal CLI executions plus below-limit setup. Apply them to sweep-filtering.test.ts, comparison-limits.test.ts, and package-limits.test.ts without changing fixtures, counts, assertions, algorithms, production code, or workflow. After approval, run the two focused module owners and package limit owner with ARCHBOARD_VAULT unset, module/system lanes as proportionate, static gates, then a clean unset bun run check before commit and CI reintegration.

15. Terminology clarification for item 14: these values are synchronous Bun lifecycle failure thresholds that accommodate measured runner variance. They are neither true hang-kill ceilings nor product performance SLAs; synchronous work may complete after the threshold and Bun then reports the case failed.

16. Hosted system-owner remediation. Preserve the hot-reload owner's exact plain-server argv/refusal/dead-PID proof, but replace its immediate post-stop PID assertion with a condition-driven wait for that exact PID to become unobservable. Put the reusable bounded wait beside processExists in tests/system/support/owned-canvas.ts, polling at TEST_CANVAS_HEALTH_POLL_MS and failing after TEST_CANVAS_CHILD_EXIT_TIMEOUT_MS; the owner remains at or below 500 lines and production stop semantics stay unchanged. Preserve all 20 shipped-bin totality inspections, reversal datasets, exact statuses, output parsing, and identity assertions; add a dedicated 15,000 ms synchronous Bun lifecycle failure threshold in src/shared/timing/timing.ts and apply it only to the totality case. Extend the compact timing rationale with the measured hosted 5.004-second totality relationship and recover line room by compacting the adjacent canvas identity-threshold comment without changing any value. After approval, run the hot owner repeatedly, the package-totality owner with absent vault, the complete canvas-state and board-inspection system slices, static gates, then one clean unset bun run check before commit; no production, workflow, dataset, assertion, browser selection, skip, or allow-failure change.

17. CI4 plan-review amendments, superseding item 16 where more specific. In tests/system/support/owned-canvas.ts, make processExists return false only for an error whose code is ESRCH; wrap and rethrow EPERM or any other kill-zero error with the observed PID. Add waitForProcessExit(pid, timeoutMs = TEST_CANVAS_CHILD_EXIT_TIMEOUT_MS): it sends no signal, polls processExists at TEST_CANVAS_HEALTH_POLL_MS, treats zombies as observable, and on timeout names the PID, the exact timeout in milliseconds, and that the identity may be live, zombie, or recycled. PID reuse may prolong observation but never hides the PID or authorizes signaling. Use this observer only after archboard stop as a hot-reload test audit; stopCanvas retains its listener-based production semantics and cleanup ownership stays with hot-reload. In tests/system/support/owned-canvas.test.ts, directly prove a retained short-lived child is observed until delayed disappearance, and prove a retained live child yields the exact diagnostic using a test-only short timeout before that child is reaped through its handle. Update TEST_CANVAS_CHILD_EXIT_TIMEOUT_MS documentation to retain its lifecycle-subprocess relationship while also naming post-stop PID observation and the server's 2,000 ms forced-exit fallback. Format and recount before changing hot-reload; hot-reload.test.ts must remain at most 500 physical lines. Add exactly TEST_BOARD_INSPECTION_TOTALITY_CASE_TIMEOUT_MS = 15_000 and apply it only to the final package-totality case: six label-pair reversal inspections plus fourteen obstacle reversal inspections. Document that 15,000 ms is about three times the observed hosted 5,003.69 ms and is a synchronous Bun lifecycle failure threshold for the observed child-lifecycle failure mode, not a hang ceiling. Validation order is the complete tests/system/support slice sequentially, then focused/full canvas-state and board-inspection slices with ARCHBOARD_VAULT unset, static gates, and one clean unset bun run check; preserve every behavioral dataset, status, output, identity assertion, and dead-PID proof.

18. Narrow CI4 Standards amendment. Add one isolated owned-canvas support test that spies process.kill, proves processExists calls the expected PID with signal zero, injects an EPERM-coded Error, and requires the exact PID-bearing diagnostic with the original cause preserved. Restore the spy in finally. Prove the case fails against an all-errors-mean-absent mutant, restore the strict ESRCH-only implementation, then run direct and complete sequential support plus TypeScript, Oxlint, formatting, and diff checks. Do not rerun browser or the complete gate because production behavior and the affected owner were already accepted.

19. CI5 deterministic lint-diagnostic remediation. In tests/system/repository-policy/boundaries.test.ts, make the owned Oxlint subprocess helper pass exactly --format=default so public diagnostic assertions retain the rule name and actionable guidance across local terminals and GitHub Actions, where Oxlint otherwise auto-selects the annotation formatter and omits help text. Keep the existing rule and Maximum allowed is 500 guidance assertions, and add direct enforcement that the helper argv contains the exact formatter selection. Do not change workflow, Oxlint configuration, boundary limits, rules, lint/test composition, skips, or allow-failure behavior. After approval, run the focused boundary owner, complete repository-policy lane, TypeScript, Oxlint, formatting, and diff checks, then one clean sequential bun run check before commit, push, and exact-SHA CI monitoring.

20. CI6 launcher-lifecycle observation remediation. The public five-second post-first-case log localizes the observed hang to the second case's unbounded await of owner.exited; its later two-second waits were never reached. Zombie-aware fake-process observation is a separate semantic hardening required by the owner contract, not a demonstrated root cause. Keep the complete suite and do not add --no-orphans.

Use one exact Linux /proc/<pid>/stat parser shared by processExistsEvidence and the detached-PGID proof, deleting the duplicate loose parser. Validate a positive safe-integer expected PID, the matching leading PID, the final `) ` delimiter so comm may contain spaces or `)`, a recognized state token, and a numeric process-group field. Only ENOENT or ESRCH means absent. Every other read error, malformed record, or PID mismatch must throw an actionable error naming the expected PID and stat path and preserving the cause where available. States Z, X, and x mean nonrunning; recognized live or stopped states mean running; an unknown state fails closed. Add synthetic launcher-owner coverage for comm containing spaces and closing parentheses, live states, zombie/dead states, PID mismatch, truncation, and invalid fields, plus a retained real child proved live and then killed and reaped only through its handle.

Immediately after Bun.spawn, register retained-handle cleanup and begin draining stderr. Race natural owner.exited against TEST_OPENER_LIFECYCLE.timeoutMs. A timeout remains a test failure naming PID, exit code, signal, process-state evidence, and captured stderr. Cleanup may signal only through owner.kill, then performs a separately bounded second await of the same exited promise while continuing to drain stderr; aggregate cleanup failure with the original timeout rather than hiding either. Never use numeric kill and never unref the test owner. Give cleanup and release a fresh ordered deadline computed from the same TEST_OPENER_LIFECYCLE.timeoutMs so an expired invocation deadline cannot race fixture-root removal. Preserve and assert capture, owner exit before the exit receipt, fake running before release, detached Linux PGID, release receipt, and fake nonrunning afterward. Do not add explicit process.exit to the fixture. Prove a mutant red by removing only production child.unref, then restore it before repetitions.

After approval, run focused repetitions, the complete code-targets slice, the complete sequential system lane, TypeScript, Oxlint, formatting, and diff checks, then one clean unset bun run check. Obtain independent review before push and monitor the exact pushed SHA. Do not change workflow, gates, skips, allow-failure behavior, or fixture datasets. If review finds owner.exited bounding inconsistent, follow its exact amendment.

21. CI7 opener-persistence ownership remediation. Public run 33276279206 at exact SHA 6a71bb8834a8d4dc0e24f1720827b6d4cc1a3b85 passed 400 modules and the activation/settings owners, then opener-persistence reached Bun's 5,000 ms lifecycle deadline, printed `killed 1 dangling process`, emitted no test result, and blocked the serial lane until the 30-minute workflow cancellation. The persistence owner currently launches four real detached fake processes directly from the Bun test process even though subprocess detachment/unref/release is owned by launcher-lifecycle. Its `releaseAndWait` accepts Linux Z as semantically nonrunning; a deterministic retained-handle probe proves a direct child can be Z with `processExistsEvidence` false while its parent handle still has null exit and has not reaped it, then disappears only after the retained exit receipt. That semantic is correct for launcher-lifecycle's detached grandchild and must not be reverted globally.

Keep production launch, shared Z semantics, launcher-lifecycle, datasets, workflow, gates, and all timeout values unchanged. Modify only `tests/system/code-targets/opener-persistence.test.ts` to use the existing `routeDependencies.launch` seam as a synchronous in-process command recorder. Preserve the public PUT/activation HTTP paths, two independent callers, fixture restart, machine config persistence, exact latest-selection precedence, resolved target and literal argv proof, note byte/mtime stability, portable-note forbidden bytes, owned vault restoration, and fixture cleanup. Remove this owner's redundant invocation directories, deferred release waits, and real child processes; the dedicated launcher owner remains the sole proof of production spawn, `child.unref`, detached PGID, response-before-exit, and retained-handle cleanup.

Use the controlled current-owner CPU-pressure command as TDD RED: three of three runs reached the unchanged 5,000 ms deadline, with direct dangling children reported in two runs; also retain the direct-child Z-before-reap trace. Add exact recorded-command count/order assertions so removing the injected seam is a deterministic mutant failure. After implementation, run that mutant, focused absent-vault and caller-sentinel repetitions, complete code-targets and sequential system lanes, TypeScript/Oxlint/format/diff checks, then one clean unset `bun run check`. Audit exact roots/processes and remove only owned diagnostic residue. Commit code and TASK-138 evidence for independent review; do not push or finalize.

22. CI7 direct-child reaping amendment, superseding item 21's proposed in-process recorder after independent diagnosis reproduced the exact topology. Preserve the real route launches in persistence. Split fixture completion semantics explicitly: `Invocation.releaseAndWait()` is the default for route-launched direct children and must wait, without signaling, until `/proc/<pid>/stat` is absent on Linux (only ENOENT/ESRCH completes); Z/X/x remains observable until the Bun/Node parent reaps it. Add `releaseAndWaitForNonrunning()` solely for the second launcher-lifecycle owner's detached grandchild, where Z/X/x correctly means no longer executing and PID1 reaping is outside the owner. Use that explicit method for both the second owner's direct and deferred cleanup calls; keep the first launcher activation case and every activation/settings/persistence route case on exact-absence `releaseAndWait()`.

Factor the two bounded evidence predicates/waits once in opener-fixture support, retaining the fresh `TEST_OPENER_LIFECYCLE.timeoutMs` release deadline and actionable PID diagnostics. Add synthetic evidence-sequence coverage proving exact-absence observes live then Z and completes only at null, while grandchild nonrunning completes at Z; add a retained direct-child proof and persistence route integration assertions that every captured direct child has disappeared after explicit release. Preserve all existing persistence save/order/restart/note/config assertions and the launcher owner's `child.unref`, natural owner exit, detached PGID, response-before-exit, exit receipt, and nonrunning proof. No production interface, server/request timeout, workflow, gate, timeout value, dataset, or Z semantic change. TDD and validation after approval: first prove the predecessor/default semantic-Z wait fails both the new synthetic live→Z→null exact-absence owner and the retained-direct-child owner; restore the split implementation; run focused absent-vault and caller-sentinel repetitions, the complete code-targets and sequential system lanes, TypeScript, Oxlint, formatting, and diff checks, then one clean unset `bun run check` and cleanup audit. The controlled CPU-pressure RED and direct-Z trace remain diagnosis evidence only, not acceptance reruns.

23. CI7 capture-readiness amendment. The required 20-run unset repetition exposed one real fixture race: the fake opener preserves O_EXCL identity by creating its capture path before writing the JSON body, while `waitForCaptures` treated filename count as completed content and once parsed the in-progress file as `Unexpected EOF`. Keep the exclusive-create identity and every shared poll/timeout value unchanged. Make capture readiness require schema-valid complete JSON under the existing fresh `TEST_OPENER_LIFECYCLE.timeoutMs` deadline: positive safe-integer PID, string target, string-array extra, and string-array argv. Retry only the reachable incomplete/malformed capture state while the deadline remains. If a record stalls, fail with an actionable diagnostic naming the exact file and the latest raw content rendered with JSON.stringify-style escaping, while preserving the latest JSON parse Error or schema-validation Error as `cause`; do not silently accept malformed content or replace it with an empty/default record.

Add deterministic direct support coverage that creates one exact capture path exclusively, starts the reader against a partial body, completes the same file after a controlled delay, and proves the schema-valid record is returned without changing identity. Add a separate stalled malformed record that reaches a test-only short deadline and asserts the exact file/raw diagnostic plus the preserved Error cause. Preserve the route-level 20-run repetition as integration evidence. After approval, run these direct cases first, then rerun a fresh unset 20× route repetition before caller-sentinel repetitions and the remaining item-22 code-targets/system/static/one clean unset full check and cleanup sequence. The prior 540/540 SyntaxError-only WIP run remains diagnosis evidence, not acceptance. No fake-opener sleep, timeout constant, production code, workflow, gate, skip, or dataset change.

24. CI8 opener-persistence aggregate case-threshold amendment. Hosted run 33279315414 at exact SHA 421c9e880e3662e000bc3713c3cf15bb67cf85a3 again entered opener-persistence and reached Bun's implicit 5,000 ms case timeout at 5,052 ms, after which Bun killed one still-owned fake opener and the job hung until workflow cancellation. Constrained unchanged-owner RED reproduces that exact case-level timeout at 5,013.48 ms with eight completed expectations. A stage-equivalent throwaway harness completes every operation: baseline total 187.31 ms; under one-CPU/64-competitor pressure total 10,686.59 ms, with the slowest individual stage 1,751.83 ms. Capture, child, and reap waits did not hit their existing 2,000 ms diagnostics. Restart is separate: it completed in 248.96 ms under severe pressure and is bounded only by the aggregate Bun test-case timeout, not an operation window. Under two-CPU/64-competitor pressure the complete workload took 2,394.77 ms. The demonstrated defect is the implicit aggregate Bun budget preempting valid sequential work and cleanup, not another unbounded child path.

Add exactly `TEST_OPENER_PERSISTENCE_CASE_TIMEOUT_MS = 15_000` at line 500 of `src/shared/timing/timing.ts`, with a compact one-line rationale, and apply it only as the third argument of the existing opener-persistence Bun test. This is an aggregate Bun test-case timeout, not a synchronous lifecycle threshold or product performance SLA. Its structural budget is five fresh 2,000 ms operation windows—selection-A capture; the initial selection-B capture batch, whose two captures share one deadline; the post-restart selection-B capture batch; selection-B release; and selection-A release—plus 5,000 ms for fixture setup, requests, restart, assertions, and disposal. The 15,000 ms threshold is exactly 3× the implicit 5,000 ms cutoff, about 2.97× the hosted 5,052 ms cutoff, and 4,313.41 ms above the severe-contention completion. Keep timing.ts at no more than 500 physical lines. Preserve every route, child, capture, release, note/config assertion, workflow gate, and per-operation timeout value.

After approval, prove the unchanged implicit-timeout predecessor red with the recorded one-CPU contention loop, apply only the shared aggregate case timeout, and prove the same loop completes without a dangling-process warning. Run focused absent-vault and caller-sentinel repetitions, complete code-targets and sequential system lanes, TypeScript/Oxlint/format/diff and line-cap checks, then one clean unset `bun run check`; remove the `[DEBUG-ci8]` throwaway harness and audit exact roots/processes. Commit source and evidence without pushing or finalizing.

25. CI9 presentation-contract aggregate case-threshold remediation. Hosted run 33281484674 at exact SHA aa7ed0f043e759ca5b73b5ab1941562dfe7033cb passed modules and the preceding code-target owners, entered tests/system/code-targets/presentation-contract.test.ts, printed killed 1 dangling process 5,034 ms later, emitted no case result, and remained stuck until the unchanged 30-minute workflow cancellation. The unchanged focused owner passes locally in 955.06 ms. Under one CPU with 32 recorded competitors it reproduces the implicit Bun cutoff at 5,000.03 ms; the identical workload with a command-line 15,000 ms probe completes all 101 assertions in 14,815.78 ms with no dangling warning. Every owned competitor was reaped. This demonstrates forward progress under constrained scheduling and localizes the defect to the implicit aggregate case budget, not a fixed wait or cleanup hang.

Add exactly TEST_CODE_TARGET_PRESENTATION_CASE_TIMEOUT_MS = 20_000 to src/shared/timing/timing.ts with a compact rationale that names the hosted cutoff, the constrained completion, and that this is an aggregate Bun test-case threshold rather than a product SLA or inner-operation timeout. Recover one blank line so timing.ts stays at no more than 500 physical lines. Apply that constant only as the third argument of the existing presentation-contract test in tests/system/code-targets/presentation-contract.test.ts. Preserve all 101 assertions, routes, git and registry transitions, pane export, note byte/mtime proofs, owned canvas cleanup, workflow, gates, 30-minute job timeout, datasets, and inner timeout values.

Validation: first retain the recorded implicit-timeout RED; after implementation, rerun the same one-CPU/32-competitor loop and require 101 assertions, no dangling warning, and no owned PID residue. Run focused absent-vault and caller-sentinel repetitions, the complete code-targets slice, the complete sequential system lane, TypeScript, Oxlint, formatting, diff, and line-cap checks, then one clean ARCHBOARD_VAULT-unset bun run check. Obtain independent visible-thread Standards and Spec review against aa7ed0f before commit/push. If review is clean, commit source and TASK-138 evidence conventionally, prove origin/main is an ancestor, push without force, and monitor the exact SHA. Any failure restarts this inspect-amend-repair-review-push loop; a green repair SHA proceeds through Backlog finalization, a final metadata commit/push, and exact-SHA green monitoring.

26. CI10 large-fixture browser-open remediation. Exact pushed SHA bfc936d8d8aa997dde8ac8454d16e7b4c315d153 cleared lint, formatting, both TypeScript projects, 400 modules, all 281 sequential system owners including presentation-contract, and 100 repository-policy tests. Run 33283664973 then failed in the first serial browser owner after 36,779.12 ms with a blank SuppressedError and only the three pre-browser assertions completed. The canonical focused owner passes locally in 56,271.59 ms / 64 assertions. A canonical one-CPU/eight-recorded-competitor RED reproduces the exact three-assertion cursor and exposes the primary error: agent-browser open is SIGTERM-d by the shared 30,000 ms command timer; all owned browser and competitor processes are reaped. A one-CPU/no-competitor calibration lets open complete and later rejects 10 holds against the unchanged <= reports + 3 performance oracle, so artificial contention must not be used to soften or tune the product measurement.

Keep the 10,000-element owner as one stateful test: its 64 runtime assertions come from 31 static expect sites and matrices over one canvas lifecycle; splitting would either duplicate the process-heavy checkout/vault/canvas/browser setup or introduce ordered shared state, and would hide aggregate resource behavior behind multiple case budgets. Preserve every fixture size, response/body/frame/fsync/report/hold/release assertion and the 240,000 ms aggregate case threshold.

Add TEST_HUMAN_PERFORMANCE_OPEN_TIMEOUT_MS = 3 * TEST_BROWSER_COMMAND_TIMEOUT_MS in src/shared/timing/timing.ts, documented as the finite initial 10,000-element render allowance rather than a product SLA or general browser timeout, while retaining the 500-line cap. In tests/system/browser/support/agent-browser.ts, replace positional stdin with a typed BrowserCommandOptions object containing optional stdin and timeoutMs, validate timeoutMs as a positive finite integer before spawn, use the selected value for graceful and forced command timers, and make timeout errors name the command and exact elapsed threshold. Default every existing command and cleanup path to TEST_BROWSER_COMMAND_TIMEOUT_MS. In human-edit-performance.test.ts, pass the 90,000 ms override only to browser.run open; all later commands use the unchanged default.

Add a deterministic direct repository-policy support case using an isolated fake agent-browser and exact environment roots: a controlled slow command must time out with an actionable short-threshold diagnostic, the same command must complete under a larger override, invalid configured thresholds must fail before spawn, cleanup must succeed, and caller environment/root state must restore. This proves option forwarding without imposing artificial CPU contention on the product performance oracle. Retain the canonical focused owner as rendered validation. Run the direct regression, focused human-performance owner, complete repository-policy lane, complete 15-owner serial browser lane, TypeScript, Oxlint, formatting, diff and line-cap checks, then one clean ARCHBOARD_VAULT-unset bun run check. Obtain independent visible-thread Standards and Spec review against bfc936d before a conventional commit, fast-forward push, and exact-SHA monitoring. Any failure restarts the same inspect-amend-review loop; do not split, skip, retry, allow-fail, or weaken any browser owner or performance assertion.

27. CI10 independent-review amendment. The item-26 claim of 31 static expect sites is withdrawn: the unchanged human-performance owner has 54 textual expect sites producing 64 runtime assertions. Keep that owner intact. Tighten only the direct fake adapter regression: replace the 150 ms sleep/50 ms cutoff with a condition-controlled command, a finite 5,000 ms timeout, and a 20,000 ms success allowance so fresh Bun startup has ample hosted-runner margin; assert the complete timeout diagnostic exactly. Use AsyncDisposableStack so close, caller-environment restoration, and owned-root removal all run even when an earlier assertion or close fails, while preserving disposal failures. Capture the close command before root removal and assert close observation, exact environment restoration, and root absence after disposal. Correct final physical-line evidence from measurements after formatting. Rerun the direct regression, complete repository-policy lane, both TypeScript projects, Oxlint, formatting, diff/line caps, then request independent rereview against bfc936d. The previously clean rendered human owner, 15-owner browser lane, and final bun run check remain acceptance evidence because no product browser owner, timing constant, adapter production logic, or browser lane behavior changes in this review-only test-harness amendment.

28. CI10 rereview correction. Enter the AsyncDisposableStack scope and register fixture-root removal immediately after createBrowserPreflightFixture returns, before path-dependent mkdir, fake executable writes, environment construction, or any other setup that can throw. Keep caller-environment restoration registered immediately before environment mutation and retain close/capture ordering plus the three post-disposal assertions. Reproduce the intentional aggregate case-cap collision only as a temporary test mutation, save it under a distinct retained /tmp log, restore the reviewed 30-second case threshold exactly, and rerun focused/repository/static gates. Append a correction that withdraws the overwritten red-log citation and names only the new retained artifact, then request rereview.

29. CI11 opener-persistence responsibility correction. Run 33285965183 at exact SHA 06ad185 passed all 400 modules, activation-contract, and settings-contract, then entered opener-persistence at 01:35:36.612Z and emitted no case result before the retained 30-minute cancellation at 02:04:15.992Z. A controlled current-source one-CPU/256-competitor run reached the existing 15,000ms aggregate case timeout at 15,427.87ms with eight expectations, killed one dangling process, and required 34.82s total to unwind. This supersedes item 22 only where it required persistence itself to retain real subprocess launches: repeated hosted evidence now shows that topology duplicates launcher-lifecycle/activation coverage and prevents the persistence contract from failing predictably. Keep one unsplit persistence case and its public PUT/activation routes, independent callers, restart, latest-selection precedence, exact resolved target/argv, note byte/mtime stability, portable-note forbidden-byte proof, vault restoration, and fixture cleanup. Inject a synchronous command recorder through the existing `routeDependencies.launch` seam, assert the exact four-command order across selection A, selection B twice, and post-restart selection B, and leave real spawn/unref/detached/reap coverage in activation-contract and launcher-lifecycle. Remove `TEST_OPENER_PERSISTENCE_CASE_TIMEOUT_MS` and return this process-free case to Bun's default finite threshold; keep all operation limits and the workflow 30-minute cap unchanged. Prove the predecessor goes red when the recorder is injected before the old capture waits are removed, then run focused, code-targets, sequential system, both TypeScript projects, Oxlint, formatting, diff, and one clean `bun run check`; obtain independent visible-thread review before push.

30. CI11 rereview remediation. Keep opener persistence as one stateful public HTTP/restart case and one exact equality, but compare one ordered timeline containing each launch, every completed activation request, and the completed restart boundary. This preserves the assertion-focused shape while proving selection A, selection B from each independent caller, and selection B after restart cannot be masked by duplicate pre-restart launches. Give TEST_OPENER_LIFECYCLE its own adjacent 2,000 ms operation-bound rationale and place the 20,000 ms aggregate-case rationale directly beside TEST_CODE_TARGET_PRESENTATION_CASE_TIMEOUT_MS. Run the focused persistence owner, the complete code-targets slice, both TypeScript projects, Oxlint, formatting, and diff checks, then request rereview before commit or push.

31. CI12 human-performance browser timeout handoff. Run 33288114113 at exact SHA 93df3aa passed lint, formatting, both TypeScript projects, 400 modules, 281 system tests, and 101 repository tests, then human-edit-performance failed during its initial 10,000-element open after 36,062.58 ms with only the three pre-open expectations reached. agent-browser 0.34.0 uses a 25,000 ms internal default operation timeout for navigation lifecycle waits; the existing 90,000 ms adapter process budget did not change that nested bound. Pass AGENT_BROWSER_DEFAULT_TIMEOUT=TEST_HUMAN_PERFORMANCE_OPEN_TIMEOUT_MS only to the human-performance owner from run-browser-lane.ts, while retaining ordinary owner environments, 30,000 ms adapter bounds, the 30,000 ms daemon idle cleanup contract, the 240,000 ms Bun case cap, and every performance assertion. Extend the existing fake adapter integration to prove an ordinary owner receives no override and the human owner receives exactly 90,000 ms alongside the configured executable. Capture a test-first RED, run the focused policy owner and focused real human browser owner, proportional TypeScript/lint/format/diff checks, obtain independent rereview, then commit only the owned TASK-138/browser-policy paths. Validate the exact commit with one clean sequential bun run check in an isolated clean worktree so protected shared design/TASK-140 drift is neither inspected nor changed; fast-forward push and monitor the new exact SHA.

32. CI12 independent-review remediation: make the browser-gate fixture factory remove a partially initialized root on setup failure; register every completed fixture with an AsyncDisposableStack immediately on acquisition, including adapter-owned roots, before any later write or spawn; have the fake traced-canvas boundary record AGENT_BROWSER_DEFAULT_TIMEOUT and assert it is absent while retaining the ordinary-owner empty and human agent-browser 90000 observations. Re-run the focused policy owner, repository lane and static gates, obtain clean rereview on the isolated five-path diff, then run one isolated bun run check before the scoped commit/push/monitor loop.

33. CI13 hosted opener-timeout remediation. Treat run 33289473418 as the RED: the identical owner passed the preceding hosted run in 87.46 ms, but this retry crossed Bun 1.4.0 implicit 5-second case failure handling, printed killed 1 dangling process, and then made no progress until the unchanged 30-minute job cap cancelled it. Restore a dedicated finite 20-second Bun case threshold beside its shared timing rationale and pass it as the owner test third argument. Do not change the owner body, phase-aware exact timeline assertion, system lane, browser lane, workflow, or job timeout. Run the focused owner repeatedly, code-targets, static gates, and one isolated clean bun run check; obtain independent visible-thread review, commit only TASK-138 plus timing/opener paths, fetch, fast-forward push, and monitor the new exact SHA. The user explicitly permits a documented CI-only exclusion if this small repair still proves GitHub cannot execute the local contract, but no exclusion is implemented while a bounded fix exists.

34. CI14 explicit hosted-only owner exception. Run 33291111341 proves the CI13 opener threshold repair worked: modules 400/400, system 281/281, and repository 102/102 passed. The same human-edit-performance browser owner then failed for a second clean hosted run at 36.91 seconds with only its three pre-open expectations, despite the existing 90-second adapter/open budget and the new exact 90-second agent-browser native timeout handoff; the identical owner passes locally with the full 10,000-element dataset and 64 expectations. Honor the user explicit override by keeping package.json and local bun run check unchanged while the GitHub Run all checks step sets one named CI-only exclusion for tests/system/browser/human-edit-performance.test.ts. The serial runner validates that the exception is used only in package mode with CI=true, emits a visible diagnostic, filters only that exact owner before prerequisites, and rejects every other value or focused use. Repository policy hardcodes the one accepted workflow env exception and tests local/full selection plus fail-closed invalid states. Run focused RED/GREEN, repository and static gates, one local complete bun run check with the exclusion absent, obtain independent review, commit, fast-forward push, and monitor the exact SHA.

35. CI14 canonical-contract review remediation. Rename the hosted job and step as the hosted subset, update AGENTS.md and docs/agents/test-suite.md to state that local bun run check retains all 15 owners while GitHub runs the exact 14-owner exception, and remove the now-unused hosted strace installation because the excluded human-performance owner is its only consumer. Extend repository policy to pin the honest hosted job name and reject a strace run step. Re-run the focused policy owner, repository lane, both TypeScript projects, Oxlint, formatting, and diff checks; obtain independent rereview, then run one clean local bun run check with CI and the exception unset before commit and push.

36. CI14 supersession clarification. Item 35 supersedes item 1 only where item 1 requires hosted strace installation, and supersedes item 34 only where item 34 names the old Run all checks step. The final hosted contract installs no standalone strace prerequisite because the excluded human-performance owner is its only consumer; local bun run check still selects that owner and therefore still requires strace.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Diagnosis and complete-workflow audit, 2026-08-29:

- Failing run: https://github.com/miklschmidt/archboard/actions/runs/33100307429 at origin/main `06764ab762b1951379c0ed5e89a6d66edc41da8e`. Job `98615922915`, step `Run all checks`, failed in the legacy `test:contracts` compatibility runner. The golden expected `open /proc/AGENTS.md`; GitHub produced `mkdir /proc/.archboard/vault`. The legacy runner inherited caller `ARCHBOARD_VAULT`, so the local environment and clean Actions environment reached different late-failure operations. Current HEAD replaced that runner with `createPackageCliOwner`, which always supplies an owned vault; the 11-case focused owner passes locally.
- Trigger and concurrency: push and pull_request on main, one job, cancel prior same-ref run. This preserves required serialization.
- Checkout: `actions/checkout@v4` is stale. The failed log warns that its Node 20 runtime is deprecated and Actions is forcing Node 24. Final uses the current v7 major.
- Bun: `oven-sh/setup-bun@v2` already reads `packageManager: bun@1.4.0`; `bun install --frozen-lockfile` matches the lockfile contract. No dependency cache exists, and install took about two seconds in the failed run, so adding cache state has no evidence-backed value.
- Browser CLI: `bun add --global agent-browser@0.34.0` and `$HOME/.bun/bin` are correct and intentionally pinned to the adapter contract. The failed run proves installation succeeds on ubuntu-latest.
- Chromium/runtime: `agent-browser install --with-deps` successfully downloaded Chrome for Testing and apt-installed browser libraries in the failed run. It does not install the human-performance owner's `strace` prerequisite, so final setup installs `strace` explicitly.
- Frontend build: separate `bun run build` and `test -d dist/frontend` are stale duplicate ownership. The serial browser adapter checks freshness and builds at most once; removing these steps makes CI exercise the same clean-checkout path as `bun run check`.
- Public gate: `bun run check` is correct and expands to lint, formatting, both TypeScript checks, modules, the eight serial system directories, repository policy/inventory, and the 15 literal serial headless browser owners. No lane is split, sharded, skipped, allowed to fail, or invoked separately.
- Timeout/resource risk: keep the 30-minute job limit. The browser lane is serial by contract and may dominate runtime; Chrome download and apt/network setup are the main CI-only risks. All prerequisites fail closed, and the push monitor loop owns remediation. No impractical lane has been found, so no follow-up omission task is justified.

Implementation evidence, fixed base ee31c07, 2026-08-29:

- TDD red: workflow mutation tests failed against the predecessor diagnostics; browser forwarding fake failed because the isolated owner did not receive AGENT_BROWSER_EXECUTABLE_PATH. Green focused repository-policy owner: 17 tests, 103 assertions.
- Focused fixed-base package compatibility: 11 tests, 165 assertions. Focused real browser recovery after an intentionally removed derived bundle: fixed-point 1/1 and human-performance 1/1, with the latter measuring 10,000 elements headlessly.
- Preflight residue proof: missing, relative, absent, directory, and non-executable configured paths exit 2 before agent-browser probing, frontend build, or owner acquisition. The focused refusal test left the full dist/frontend hash unchanged at ddf89d3782ba25e3e6b106c655514be0f55325eab9a9a1d6acd3f9a9b8d9f8f7.
- Honest failed full-check evidence is retained in /tmp/task138-full-check.log and /tmp/task138-final-full-check.log. The first used a malformed ignored bundle produced before the frozen install; the second caught a stray test assertion that rejected legitimate Vite stderr during a clean build. Neither was a product gate weakening, and both causes were removed before acceptance.
- Clean acceptance from absent dist/frontend: AGENT_BROWSER_EXECUTABLE_PATH=$(realpath ~/.agent-browser/browsers/chrome-150.0.7871.46/chrome) bun run check, exit 0. It passed lint, formatting, both TypeScript projects, 400 module tests, 250 serial system tests, 50 repository tests, and all 15 serial headless browser owners. Log: /tmp/task138-acceptance-full-check.log.
- Modified TypeScript physical lines: run-browser-lane.ts 498; browser support 385; inventory support 261; inventory owner 498. All remain below the enforced 500-line cap.
- Workflow contract: top-level contents:read; checkout@v7; setup-bun@v2; frozen install; explicit strace; pinned agent-browser@0.34.0; install --with-deps; exactly one downloaded executable named chrome resolved with realpath and exported through GITHUB_ENV; one bun run check; 30-minute timeout. No skip, shard, allow-failure, or continue-on-error path was added.

Review remediation evidence, 2026-08-29:

- Independent review RED: tests/system/repository-policy/ci-browser-gate.test.ts produced 6 expected failures against a680db7. Raw workflow text let comments and quoted echo text count, local execution rejected an unset AGENT_BROWSER_EXECUTABLE_PATH, and ARCHBOARD_TEST_BROWSER_OWNER_FIXTURE replaced the canonical owner argv. Log: /tmp/task138-review-red.log.
- The adapter now treats AGENT_BROWSER_EXECUTABLE_PATH as an optional local override. When set, it normalizes and validates the absolute file and execute bit before agent-browser probing, frontend build, or owner acquisition. When unset, agent-browser keeps its documented PATH and Nix/system browser discovery. The canonical Bun owner argv is literal and immutable. A real adapter boundary with a fake agent-browser proves that a normalized path reaches the canonical isolated owner and that the former substitution input is ignored. Canvas subprocesses retain the existing AGENT_BROWSER_* stripping.
- CI policy now parses YAML jobs and run steps. It requires one executable step exactly equal to bun run check, ignores comments and quoted echo output, and rejects missing, duplicate, direct build/test, whitespace, and multiline drift as separate Bun tests in ci-browser-gate.test.ts. The original inventory/cleanup owner remains independent.
- Focused green: ci-browser-gate.test.ts 15 tests and 46 assertions; combined inventory and CI/browser owners passed before the final run. Type-check, Oxlint, formatting, and diff checks passed.
- First clean post-review full-check attempt is retained honestly in /tmp/task138-review-full-check.log. Lint, formatting, TypeScript, and 400 module tests passed, then unrelated board-inspection subprocess owners repeatedly exceeded existing 5-second and 40-second limits. Bun later hung at one core after the 30-second source-staleness timeout; only the owned check process group was stopped, for exit 143. No TASK-138 owner failed and no gate was changed. Its one source-staleness temp directory and derived bundle were removed before retry.
- Clean retry with AGENT_BROWSER_EXECUTABLE_PATH explicitly unset passed bun run check: 400 module tests, 250 serial system tests, 60 repository-policy tests, and all 15 serial headless browser owners. Log: /tmp/task138-review-full-check-retry.log. This directly verifies the reviewed local PATH interface.
- Final physical lines: run-browser-lane.ts 486; agent-browser.ts 387; inventory support 353; inventory owner 370; CI/browser policy owner 227. Final cleanup removed dist, found no browser lane or preflight roots, and found no surviving check, browser, or owner process.

Second review remediation evidence, 2026-08-29:

- Workflow RED: the five requested executable mutations all passed through the reviewed scanner: env FOO=1 bun run test, command bun run build, a grouped bun run test, a conditional bun run test, and a pipeline into bun run build. Log: /tmp/task138-rereview2-workflow-red.log. Two self-review slices also went red before their fixes: executable backtick substitution in /tmp/task138-rereview2-backtick-red.log and a shell comment following a command separator in /tmp/task138-rereview2-comment-red.log.
- inspectWorkflow still gets run scalars from Bun.YAML. It now blanks quoted content and shell comments, finds every remaining bun run token sequence regardless of wrapper, grouping, control prefix, command substitution, or pipeline position, and exempts only an echo/printf simple command. The one accepted gate remains a scalar exactly equal to bun run check. Fifteen independent workflow-policy tests cover acceptance, comments/echo text, canonical count, direct scripts, every reviewed wrapper/control form, command substitution, whitespace, and multiline drift.
- The predecessor inventory diagnostics no longer share grouped test bodies. test-inventory.test.ts now reports 38 independent tests. Missing lane, orphan, cross-lane ownership, unreachable lane, duplicate reachability, transitional lane, system/browser drift, and each adapter mutation have separate Bun tests or test.each cases. No expectation was removed or folded into one aggregate assertion.
- Complete repository policy: 94 tests, 268 assertions, exit 0. Log: /tmp/task138-rereview2-repository-green.log. TypeScript, Oxlint, formatting, and git diff checks also pass; logs use the /tmp/task138-rereview2-* prefix. Physical lines: inventory support 360, inventory owner 408, CI/browser owner 273.
- Per second review direction, no browser or complete gate rerun was performed. This follow-up changes only the repository-policy scanner, its focused tests, and TASK evidence; the prior clean full acceptance remains /tmp/task138-review-full-check-retry.log.

Quoted command-substitution remediation evidence, 2026-08-29:

- TDD RED: echo with double-quoted dollar-parenthesis, a double-quoted assignment using dollar-parenthesis, and double-quoted backticks all hid executable bun run tokens from the scanner. All three independent negatives failed in /tmp/task138-rereview3-red.log.
- The scanner still blanks ordinary double-quoted text and every single-quoted span. It now extracts balanced dollar-parenthesis and backtick bodies only when they occur inside double quotes, sanitizes each body with the same quote/comment rules, and scans nested quoted substitutions recursively. Unquoted backticks remain covered. Separate controls retain inert plain double-quoted text, single-quoted substitution text, comments, and echo-only text.
- Focused policy owners passed 65 tests and 118 assertions: test-inventory.test.ts 38 tests; ci-browser-gate.test.ts 27 tests, comprising 19 workflow-policy cases and 8 browser-boundary cases. Log: /tmp/task138-rereview3-focused-green.log. TypeScript, Oxlint, formatting, and git diff checks pass; logs use the /tmp/task138-rereview3-* prefix.
- Physical lines: inventory support 471, inventory owner 408, CI/browser policy owner 295. Per review direction, no browser or complete gate rerun was performed because only repository-policy scanner/tests and TASK evidence changed.

Dollar-substitution comment-state remediation evidence, 2026-08-29:

- TDD RED: valid Bash echo with a double-quoted substitution, a commented closing parenthesis, then bun run test on the next line returned no policy error. The independent negative failed in /tmp/task138-rereview4-red.log.
- dollarSubstitutionAt now uses the shared shell-comment boundary check. Once a comment starts, quotes and parentheses are ignored until newline; scanning then resumes at the existing substitution depth. If no real closing parenthesis follows, the existing fail-closed fallback still returns the remaining body for executable-token inspection.
- Focused policy owners passed 66 tests and 119 assertions: inventory owner 38; CI/browser owner 28, comprising 20 workflow-policy and 8 browser-boundary cases. Log: /tmp/task138-rereview4-focused-green.log. TypeScript, Oxlint, formatting, and git diff checks pass; logs use the /tmp/task138-rereview4-* prefix.
- Physical lines: inventory support 484, inventory owner 408, CI/browser policy owner 302. Per direction, no browser or complete gate rerun was performed because only the repository-policy scanner, its focused test, and TASK evidence changed.

Clean-runner CI remediation evidence, pushed SHA c98a229, 2026-08-29:

- GitHub Actions run 33267557636, job 99140225454 failed in src/runtime/engine/tests/board-version-note.test.ts because the clean runner had no ARCHBOARD_VAULT. config.ts snapshots the environment at module import; the test created a temporary directory but never made it the process vault, while the prior local shell inherited a vault and masked the isolation defect. Full job log: /tmp/task138-ci2-job.log.
- Focused TDD RED with ARCHBOARD_VAULT removed: board-version-note 3 passed / 5 failed, all through requireVaultRoot before their intended note assertions. Log: /tmp/task138-ci2-focused-unset-red.log. The owner now exports its temporary root before any config-sensitive import and, after module cleanup, removes that exact root and restores the caller value or deletes an originally absent variable. Unset and caller-sentinel focused runs each pass 8 tests / 47 assertions; the sentinel remains empty and no owner root remains. Logs: /tmp/task138-ci2-focused-unset-green.log and /tmp/task138-ci2-focused-sentinel-green.log. The complete unset module lane passes 400 tests / 3,144 assertions: /tmp/task138-ci2-modules-green.log.
- The first complete unset check passed static gates and 400 modules, then unrelated board-inspection subprocesses exceeded existing 5-second and 40-second limits and Bun hung after the source-staleness timeout. The owned process group alone was killed after TERM was ignored, and its one staleness root was removed. Honest log: /tmp/task138-ci2-full-check.log. A clean retry cleared every timeout and enumerated exactly three further inherited-vault defects: one direct boards engine call and two code-target default-dependency reads; 247 of 250 system tests passed. Honest log: /tmp/task138-ci2-full-check-retry.log.
- Focused system-owner TDD RED reproduced those exact three failures: 21 passed / 3 failed. The boards and code-target owners now each establish an owned vault before the first config-sensitive import, assert the imported config sees it, dispose canvases/servers/fixtures first, remove the exact root, and restore or delete caller state. No production route, resolver, config, or workflow-wide environment changed. Unset and caller-sentinel focused runs each pass 26 tests / 96 assertions, retain the existing note/config byte and mtime checks, leave the sentinel empty, and leave no owner or fixture root. Logs: /tmp/task138-ci2-system-owners-red.log, /tmp/task138-ci2-system-owners-unset-green.log, and /tmp/task138-ci2-system-owners-sentinel-green.log.
- Complete unset system lane passes 252 tests / 4,130 assertions: /tmp/task138-ci2-system-lane-green.log. TypeScript, Oxlint, formatting, and diff checks pass. Physical lines remain below 500: board-version-note 345; malformed-input 225; activation-contract 446; run-browser-lane 486; agent-browser support 387; CI/browser policy 302; inventory support 484; inventory owner 408.
- Final clean acceptance from absent dist/frontend and with ARCHBOARD_VAULT explicitly removed passes bun run check: lint, formatting, both TypeScript projects, 400 module tests, 252 system tests, 99 repository-policy tests, and all 15 sequential headless browser owners. Log: /tmp/task138-ci2-final-full-check.log. Final cleanup removed the generated dist, found no new vault/browser fixture roots, and found no surviving TASK-138 check, canvas, browser, or owner process.

Evidence correction after final formatting: tests/system/boards/malformed-input.test.ts is 223 physical lines (not the pre-format 225); every stated line cap remains satisfied.

Hosted-runner timing diagnosis, pushed SHA 9028035, 2026-08-29:

- GitHub Actions run 33268988057/job 99143955951 cleared the vault-isolation failures and failed exactly two synchronous module cases: zero-segment filtering completed in 5,274.07 ms after Bun's default 5,000 ms synchronous lifecycle failure threshold; retained terminal findings completed in 27,093.94 ms after its explicit 20,000 ms synchronous lifecycle failure threshold. All 398 other module tests passed. Full log: /tmp/task138-ci3-job.log.
- Current worktree content matches pushed 9028035 for both owners, fixtures, package owner, and shared timing. Four local isolated repeats were stable: zero filtering 2,256.66-2,307.44 ms (median about 2,276 ms), terminal retention 11,598.71-11,670.20 ms (median about 11,624 ms). Hosted/local ratios are independently 2.32x and 2.33x, identifying runner throughput rather than test drift. Logs: /tmp/task138-ci3-zero-focused-1.log, /tmp/task138-ci3-zero-focused-repeat.log, /tmp/task138-ci3-terminal-focused-1.log, and /tmp/task138-ci3-terminal-focused-repeat.log.
- Neither case asserts elapsed time. The sweep owner asserts the complete 4,096-segment traversal, exact 2,048 zero-segment indexes, zero broad-phase comparisons, and the supported-pair control. The terminal owner asserts attempted comparison 2,000,001, exactly one terminal finding, and preservation of a completed zero-length finding. Production separately fixes input complexity at 1,000,000 and broad-phase comparisons at 2,000,000; no public wall-clock SLA exists.
- Component measurement on the current host attributes cost to deterministic output volume: exact 2,000,001 comparisons over 2,500 elements took 492.73 ms; canonicalizing all-zero connector findings took 2,652.80 ms for 2,048 segments, 4,938.85 ms for 4,096, and 13,792.64 ms for 10,000. The terminal fixture intentionally combines 10,000 completed findings with the exact comparison ceiling. Log: /tmp/task138-ci3-component-benchmark.log.
- The package owner executes the terminal workload twice, normal and strict, after a below-limit case. Its focused local case took 26,787.44 ms under the existing 40,000 ms synchronous Bun lifecycle failure threshold; applying the observed 2.33x hosted factor predicts about 62.4 seconds. Earlier honest local full attempts already crossed that 40-second lifecycle failure threshold under contention, while clean retries passed around 26.6-26.8 seconds. A shared derived package lifecycle cap is therefore part of the same repair, not a dataset or assertion reduction. Log: /tmp/task138-ci3-package-limit-focused.log.
- Proposed evidence-based synchronous Bun lifecycle failure thresholds are 15,000 ms for the sweep owner, 40,000 ms for the terminal module owner, and 90,000 ms for the package owner (two 40-second terminal allowances plus 10 seconds for below-limit/setup). They accommodate runner variance; they neither kill synchronous work at the threshold nor define performance assertions or product SLAs; exact work limits and all fixtures remain unchanged. No production optimization or follow-up task is justified by this deterministic, bounded workload.

Timing terminology correction: references in the preceding diagnosis to outer caps or hang ceilings mean synchronous Bun lifecycle failure thresholds only. They accommodate measured runner variance; they do not kill synchronous work at the threshold and do not establish product performance SLAs.

Hosted-runner lifecycle-threshold remediation evidence, 2026-08-29:

- Approved item 14 is implemented without changing inspection fixtures, datasets, work counts, assertions, algorithms, production behavior, workflow, or gate composition. Shared timing now defines 15,000 ms for the complete sweep case, 40,000 ms for the terminal module case, and the package threshold exactly as 2 * TEST_BOARD_INSPECTION_TERMINAL_CASE_TIMEOUT_MS + 10,000. These are synchronous Bun lifecycle failure thresholds for measured runner variance, not kill timers or product performance SLAs.
- RED evidence is the exact pushed GitHub run 33268988057/job 99143955951: the unchanged sweep completed at 5,274.07 ms after the default 5,000 ms threshold and terminal retention completed at 27,093.94 ms after the explicit 20,000 ms threshold, while their exact structural assertions remained intact. Full log: /tmp/task138-ci3-job.log.
- Focused green with ARCHBOARD_VAULT unset: sweep-filtering plus comparison-limits passed 13 tests / 83 assertions; package-limits passed 2 tests / 8 assertions. Logs: /tmp/task138-ci3-focused-modules-green.log and /tmp/task138-ci3-focused-package-green.log.
- Proportionate lanes with ARCHBOARD_VAULT unset: modules passed 400 tests / 3,144 assertions; serial system passed 252 tests / 4,130 assertions, including the package terminal case at 26,644.03 ms. Logs: /tmp/task138-ci3-module-lane-green.log and /tmp/task138-ci3-system-lane-green.log. Oxlint, formatting, both TypeScript projects, and git diff checks passed; logs use /tmp/task138-ci3-lint.log, -format.log, -type-check.log, and -diff-check.log.
- One clean sequential acceptance run started with ARCHBOARD_VAULT unset, AGENT_BROWSER_EXECUTABLE_PATH unset, dist/frontend absent, and no active owner process. bun run check passed all gates: lint, formatting, both TypeScript projects, 400 module tests, 252 serial system tests, 99 repository-policy tests, and all 15 serial headless browser owners. The log contains 766 passing cases and no failures: /tmp/task138-ci3-final-full-check.log.
- Physical lines after formatting: timing.ts 500; sweep-filtering.test.ts 454; comparison-limits.test.ts 45; package-limits.test.ts 66. The generated ignored dist bundle was removed after acceptance. Final audit found no surviving check, browser, canvas, or inspection owner and no fresh TASK-138 temp root; node_modules remains the pre-existing ignored dependency tree.

Hosted system-owner diagnosis, pushed SHA f054120, 2026-08-29:

- GitHub Actions run 33270430736/job 99147819889 passed both repaired module thresholds, then completed the serial system lane with 250 pass / 2 fail. package-totality failed at 5,003.69 ms with one dangling process killed, child status null, and empty stdout/stderr. hot-reload failed after plain canvas stop because an immediate process.kill(pid, 0) did not throw. Full log: /tmp/task138-ci4-job.log.
- Exact focused owners with ARCHBOARD_VAULT unset pass locally: hot reload 7,324.32 ms; totality 2,455.13 ms. Repeats are stable: hot 7,495.87-7,517.43 ms; totality 2,453.17-2,479.88 ms. Logs: /tmp/task138-ci4-hot-focused-1.log, /tmp/task138-ci4-hot-focused-repeat.log, /tmp/task138-ci4-totality-focused-1.log, and /tmp/task138-ci4-totality-focused-repeat.log.
- Hot stop is an owner observation race, not a retained canvas. A minimal 20-iteration exact start/health/reload-refusal/stop probe reproduced the immediate kill-zero success twice, with the PID disappearing without intervention in 1-2 ms. A resource-identity probe captured stop status 0 and the same PID in the stopped receipt; immediately afterward health was connection-refused, the exact state pidfile was absent, process.kill(pid, 0) briefly succeeded, and the PID disappeared after 1.13 ms before /proc status or argv could still be read. Logs: /tmp/task138-ci4-hot-stop-probe.log and /tmp/task138-ci4-hot-identity-probe.log. Production shutdown removes the pidfile, closes the listener, and force-exits after two seconds; there is no evidence of an actual leak. The test currently gives the OS no observation interval after the health-based stop command returns.
- Totality is a synchronous Bun lifecycle threshold, not a product performance contract or a timeout configured by readOnlyRun. The case performs 20 sequential shipped-bin inspections: six label-pair reversals and fourteen obstacle-identity reversals, checking exact exit statuses, schema output, injective pairs, and canonical obstacle identities. Locally it is stable around 2.47 seconds; hosted 5.004 seconds is 2.03x. The newly green package-limit case independently measured the same runner factor: hosted 54.289 seconds versus local 26.782 seconds.
- A deterministic local threshold probe ran the unchanged totality owner with Bun timeout 2,000 ms and reproduced the CI bytes exactly: killed 1 dangling process, status null, empty stdout/stderr, and failure at 2,000.74 ms. Log: /tmp/task138-ci4-totality-threshold-red.log. A dedicated 15,000 ms test threshold leaves three times the observed hosted duration while keeping every behavioral dataset/assertion and a bounded stuck-case failure.

CI4 plan-review amendments recorded before implementation:

- The shared observer is strict: only ESRCH means absent. EPERM and every other kill-zero error remain failures with PID context. It never signals and cannot turn PID reuse into cleanup authority; a live, zombie, or recycled identity stays observable until disappearance or an actionable 20,000 ms failure.
- Direct support tests own both reachable observer states: delayed disappearance of a retained short-lived child, and an exact short-timeout diagnostic for a retained live child followed by handle-owned reap. The complete sequential tests/system/support slice runs before the affected canvas-state and board-inspection slices.
- TEST_CANVAS_CHILD_EXIT_TIMEOUT_MS documentation will cover both its existing lifecycle-subprocess relationship and the post-archboard-stop PID observation that must outlive the server's 2,000 ms forced-exit fallback. Formatting and physical-line recount precede the hot owner edit; hot-reload remains at most 500 lines.
- The totality threshold name is exactly TEST_BOARD_INSPECTION_TOTALITY_CASE_TIMEOUT_MS. Its only consumer is the final six-plus-fourteen reversal case. 15,000 ms is about three times hosted 5,003.69 ms and addresses Bun's observed synchronous child-lifecycle failure mode; it is not a hang ceiling or product performance SLA.

CI4 hosted system-owner remediation evidence, 2026-08-29:

- TDD RED at the approved shared observer seam: the two new direct support cases failed because waitForProcessExit was undefined while all five predecessor lifecycle cases remained green (5 pass / 2 fail). Log: /tmp/task138-ci4-support-red.log. The strict implementation returns absent only for ESRCH, wraps every other observation error with PID context, polls signal-zero observations without cleanup authority, and reports the exact timeout plus live/zombie/recycled possibilities. Direct green passed 7 tests / 30 assertions: /tmp/task138-ci4-support-green-final.log.
- Focused repetitions with ARCHBOARD_VAULT unset passed unchanged behavior: hot-reload three runs (12 tests / 198 assertions; 7,442.24-7,509.65 ms for the full case) and final package-totality four runs (2,435.86-2,453.29 ms). Logs: /tmp/task138-ci4-hot-green-repeat.log and /tmp/task138-ci4-totality-green-repeat.log.
- Complete sequential affected slices passed in the required order with ARCHBOARD_VAULT unset: support 15 tests / 88 assertions, canvas-state 21 / 399, and board-inspection 19 / 219. Logs: /tmp/task138-ci4-support-slice.log, /tmp/task138-ci4-canvas-state-slice.log, and /tmp/task138-ci4-board-inspection-slice.log. The final totality case alone uses TEST_BOARD_INSPECTION_TOTALITY_CASE_TIMEOUT_MS = 15,000; every six label-pair and fourteen obstacle reversal inspection, status, parse, and identity assertion is unchanged.
- TypeScript, Oxlint, formatting, and git diff checks passed. One lint-only RED identified an invalid await on Bun rejects; the direct test now awaits the observer explicitly and compares the exact captured Error before handle-owned reap, with no production semantic change.
- One clean sequential acceptance run with ARCHBOARD_VAULT explicitly unset passed bun run check: 400 module tests / 3,144 assertions, 254 serial system tests / 4,133 assertions, 99 repository-policy tests / 273 assertions, and all 15 serial headless browser owners. Log: /tmp/task138-ci4-full-check.log. No test, dataset, assertion, production shutdown behavior, workflow lane, skip, or allow-failure path changed.
- Final physical lines: timing.ts 499; owned-canvas.ts 432; owned-canvas.test.ts 277; hot-reload.test.ts exactly 500; package-totality.test.ts 490. The generated ignored dist bundle was removed after acceptance. No check, browser, canvas, or inspection process survived, and no fresh support/hot/inspection root remained; four listed hot roots predated this run and were left untouched. Implementation commit: 335b67c.

Narrow CI4 Standards remediation evidence, 2026-08-29:

- Added an isolated process-observer contract around the existing public support seam. The test spies process.kill, verifies the exact expected PID and signal 0, injects an EPERM-coded Error, requires the exact PID-bearing diagnostic, requires the original Error as cause, and restores the spy in finally.
- Mutant RED: temporarily treating every process.kill error as absence made the new case fail with no Error after the kill-zero call was observed. Log: /tmp/task138-ci4-standards-mutant-red.log. The reviewed ESRCH-only implementation was then restored byte-for-byte.
- Direct support passed 8 tests / 34 assertions; the complete sequential support slice passed 16 tests / 93 assertions. Logs: /tmp/task138-ci4-standards-direct-green.log and /tmp/task138-ci4-standards-support-green.log. TypeScript, Oxlint, formatting, and git diff checks passed; logs use /tmp/task138-ci4-standards-{type,lint,fmt}.log.
- owned-canvas.test.ts is 301 physical lines and owned-canvas.ts remains 432. No support/canvas process survived. Per narrow review direction, no browser or second complete gate ran; the prior clean unset acceptance remains /tmp/task138-ci4-full-check.log. Test commit: 39decf1.

CI5 diagnosis, 2026-08-29:

- Hosted Oxlint auto-selected its GitHub annotation formatter, which preserved the boundary violation annotation but omitted the help text asserted by boundaries.test.ts. Local oxlint --format=github reproduces the exact hosted diagnostic shape; oxlint --format=default preserves both the rule identifier and actionable Maximum allowed is 500 guidance. The smallest deterministic repair belongs in the test-owned lint subprocess helper, not workflow or shared lint configuration. Source remains paused pending plan review.

CI5 deterministic lint-diagnostic remediation evidence, 2026-08-29:

- TDD RED: the new direct argv contract failed because lintCommand did not exist, while all eight predecessor boundary cases passed. Log: /tmp/task138-ci5-boundary-red.log. The boundary owner now constructs every Oxlint subprocess argv with exact --format=default after its owned config argument; this prevents host auto-detection from changing public diagnostic bytes.
- Focused boundary green passed 9 tests / 76 assertions, including the unchanged archboard/eslint rule-name checks and Maximum allowed is 500 guidance check. Complete repository policy passed 100 tests / 274 assertions. Logs: /tmp/task138-ci5-boundary-green.log and /tmp/task138-ci5-repository-green.log. TypeScript, Oxlint, formatting, and git diff checks passed; logs use /tmp/task138-ci5-{type,lint,fmt}.log.
- One clean sequential acceptance run started with ARCHBOARD_VAULT unset, no active owner process, and dist absent. bun run check passed lint, formatting, both TypeScript projects, 400 module tests / 3,144 assertions, 255 serial system tests / 4,137 assertions, 100 repository-policy tests / 274 assertions, and all 15 serial headless browser owners. Log: /tmp/task138-ci5-full-check.log.
- boundaries.test.ts is 309 physical lines. No workflow, Oxlint configuration, boundary rule/limit, assertion, gate, skip, or allow-failure behavior changed. The generated ignored dist bundle was removed after acceptance; no check, browser, canvas, or inspection process survived. Implementation commit: cdca08b.

CI6 diagnosis, public run 33273256248, 2026-08-29:

- The exact pushed SHA passed all 400 module tests and entered the sequential system lane. The first launcher-lifecycle case passed; about five seconds later Bun reported killed 1 dangling process, then the job produced no further progress and was canceled at the 30-minute workflow limit.
- Program order localizes the observed hang to the second case's unbounded await of owner.exited. Its later two-second process-observation waits were never reached, so raw /proc zombie observation is not the demonstrated cause.
- Exact local and CI-shaped environment runs plus more than twenty focused repetitions are green, so there is no tight local reproduction. The hosted exact-SHA run remains the reproduction.
- Zombie-aware fake-process evidence is still required semantic hardening: an exited non-executing process may remain as a zombie until PID 1 reaps it. This is separate from the demonstrated owner.exited hang and must use the one strict shared /proc stat parser defined in item 20.
- The repair must bound natural owner exit and retained-handle cleanup with TEST_OPENER_LIFECYCLE.timeoutMs, compute a fresh ordered cleanup/release deadline from that same shared value, preserve every launcher lifecycle and child.unref regression proof, and retain actionable stderr and process diagnostics. It must not suppress orphan detection, skip an owner, weaken the complete suite, use numeric kill, or force fixture exit. Source remains paused pending plan rereview.

CI6 launcher-lifecycle remediation evidence, 2026-08-29:

- TDD RED: before the shared parser existed, the new focused owner failed at import with `parseLinuxProcessStat` missing (0 pass / 1 fail). Log: `/tmp/task138-ci6-parser-red.log`. Parser GREEN covered command names containing spaces and `)`, every recognized live/stopped and dead state, PID mismatch, truncation, invalid fields, invalid expected PIDs, a retained live child reaped only through its handle, and a missing stat record.
- Required lifecycle mutant: removing only production `child.unref()` made the bounded owner fail in 2,044 ms with PID, null exit/signal, live `S` state, process group, and captured stderr; retained-handle cleanup completed instead of hanging. Log: `/tmp/task138-ci6-unref-mutant-red.log`. The production line was restored byte-for-byte and has no final diff.
- The fixture now has one strict Linux stat parser shared by process existence and detached-PGID evidence. It validates the positive safe PID, matching record PID, final command delimiter, state, and numeric process group. Only ENOENT/ESRCH means absent; malformed, mismatched, unknown-state, and other read errors are actionable. Z/X/x are semantically nonrunning.
- The launcher owner starts stderr drainage and registers retained-handle cleanup immediately after spawn. Natural exit and the second cleanup await are independently bounded by `TEST_OPENER_LIFECYCLE.timeoutMs`; cleanup signals only through `owner.kill`, uses the same exited promise, and aggregates cleanup failure with the original timeout. Capture, response-before-exit, owner-exit-before-receipt, fake-running, detached PGID, release receipt, and fake-nonrunning assertions remain. Invocation capture and ordered release each receive a fresh deadline from the same shared timeout.
- Focused GREEN passed 20 isolated repetitions: 480 tests / 700 assertions in 4.96 s. Complete code-targets passed 59 tests / 302 assertions. Complete sequential system passed 277 tests / 4,159 assertions in 165.71 s, including the repaired owner with no dangling-process warning. Logs: `/tmp/task138-ci6-launcher-repeat-green.log`, `/tmp/task138-ci6-code-targets-green.log`, and `/tmp/task138-ci6-system-green.log`.
- TypeScript, Oxlint, formatting, and diff checks passed; logs: `/tmp/task138-ci6-type.log`, `/tmp/task138-ci6-lint.log`, and `/tmp/task138-ci6-fmt.log`. One clean sequential `ARCHBOARD_VAULT`-unset `bun run check` passed lint, formatting, both TypeScript projects, 400 module tests / 3,144 assertions, 277 system tests / 4,159 assertions, 100 repository-policy tests / 274 assertions, and all 15 serial headless browser owners. Log: `/tmp/task138-ci6-full-check.log`.
- Physical lines after formatting: `launcher-lifecycle.test.ts` 246; `opener-fixture.ts` 387; shared `timing.ts` remains 499. The generated ignored `dist` tree was removed after acceptance. Final cleanup found no new opener fixture root or surviving check, launcher-owner, or fake-opener process; only the pre-existing ignored dependency tree remains.

Implementation commit for CI6 item 20: `bb21422`.

CI7 diagnosis checkpoint, 2026-08-30:

- Exact public evidence: run 33276279206/job 99163464819 at 6a71bb8 passed 400 modules. System activation and settings passed; opener-persistence began at 21:33:12.074Z, printed `killed 1 dangling process` at +5.008 s, never printed a test result, and was canceled with the workflow at 22:01:32Z. Log: `/tmp/task138-ci7-job.log`.
- CI-shaped local baselines are green: focused owner 1/1 in 303 ms; complete code-targets 59/59 in 3.50 s. Logs: `/tmp/task138-ci7-focused-baseline.log` and `/tmp/task138-ci7-code-targets-baseline.log`.
- Deterministic deadline RED: with the owner pinned behind 32 controlled CPU competitors and an outer safety cap, three of three exact-owner runs failed at the existing 5,000 ms lifecycle deadline; two printed one or two dangling processes. Logs: `/tmp/task138-ci7-pressure-red-{1,2,3}.log`. This reproduces the deadline/child-loss symptom; local Bun reports failure and exits instead of reproducing the hosted post-timeout hang.
- Direct topology proof: a retained direct `node:child_process` child was synchronously observed as state Z, semantic running false, `processExistsEvidence` false, and handle exitCode null. Yielding to and awaiting the retained exit receipt changed exitCode to 0 and removed `/proc/<pid>/stat`. Log: `/tmp/task138-ci7-direct-child-zombie-red.log`. Thus semantic nonrunning is not direct-child reaping.
- Generic server-close hypotheses ranked lower: completed keep-alive and deliberately incomplete-request probes both allowed `fixture.restart()` to resolve immediately on this Bun runtime. A timeout increase would only mask the ownership defect and is excluded. Source remains untouched pending plan approval.

Independent CI7 diagnosis amendment:

- An independent 120-launch route probe observed `releaseAndWait()` return twice while the direct route child still existed as Z, matching the retained-handle proof and the hosted dangling-child signature. The correct boundary is topology-specific: exact absence/reaping for test-process direct children, semantic nonrunning for the launcher owner's detached grandchild.
- The earlier in-process route recorder proposal is superseded because it would remove useful real route integration coverage. Request timeout or generic server-close bounding alone would only convert the hang to a different red result without proving direct-child reap.

CI7 capture-readiness RED, 2026-08-30:

- The first item-22 unset 20-run integration attempt passed 539 of 540 cases and failed one direct route activation when `waitForCapture` observed the O_EXCL-created capture filename before its write completed; JSON.parse reported `Unexpected EOF`. Honest log: `/tmp/task138-ci7-focused-unset-repeat.log`.
- A minimal WIP retry-on-SyntaxError made the same 20-run unset integration pass 540/540 tests / 1,580 assertions in 9.66 s (`/tmp/task138-ci7-focused-unset-repeat-green.log`), but it does not yet satisfy the required malformed/stalled schema and exact raw/cause diagnostic contract. Source is paused at this WIP pending item-23 plan review; sentinel and broad gates have not run.

Item-23 review correction: the earlier 540/540 SyntaxError-only WIP run is diagnosis evidence only. Acceptance restarts with the reviewed direct readiness cases and a fresh unset 20× route repetition before the caller-sentinel and broad gates.

CI7 direct-child and capture-readiness remediation evidence, 2026-08-30:

- TDD RED first proved the approved seams were absent (`/tmp/task138-ci7-tdd-red.log`). The required predecessor semantic-Z mutant then made exact-absence completion return on Z: the pure live→Z→null sequence expected index 2 but got 1, and the retained direct-child case returned with `exitCode === null` (`/tmp/task138-ci7-semantic-z-mutant-red.log`). Restoring topology-specific completion passed the two direct cases (`/tmp/task138-ci7-semantic-split-green.log`).
- Capture-readiness TDD RED failed because the reviewed complete-capture reader was absent (`/tmp/task138-ci7-capture-direct-red.log`). Direct GREEN proves a partially written O_EXCL capture becomes readable without inode replacement and a stalled schema-invalid capture times out with the exact file, JSON-stringified raw content, latest validation message, and preserved Error cause: 2 tests / 6 assertions (`/tmp/task138-ci7-capture-direct-green.log`).
- Fresh acceptance repetitions after the reviewed implementation passed with `ARCHBOARD_VAULT` absent: 20 isolated route runs, 580 tests / 1,700 assertions, 10.58 s (`/tmp/task138-ci7-final-unset-repeat.log`). The caller-sentinel repetition also passed 580 / 580 tests and left the sentinel vault path absent (`/tmp/task138-ci7-final-sentinel-repeat.log`). The earlier 539 / 540 EOF failure and SyntaxError-only WIP run remain diagnosis evidence only.
- Complete affected lanes passed with `ARCHBOARD_VAULT` absent: code-targets 63 tests / 320 assertions (`/tmp/task138-ci7-code-targets-green.log`) and the complete sequential system lane 281 tests / 4,177 assertions in 164.99 s (`/tmp/task138-ci7-system-green.log`). TypeScript, Oxlint, formatting, and diff checks passed (`/tmp/task138-ci7-type-final.log`, `/tmp/task138-ci7-lint-final.log`, `/tmp/task138-ci7-fmt-final.log`).
- One clean sequential `ARCHBOARD_VAULT`-unset `bun run check` passed lint, formatting, both TypeScript projects, 400 module tests / 3,144 assertions, 281 system tests / 4,177 assertions, 100 repository-policy tests / 274 assertions, and all 15 serial headless browser owners. Log: `/tmp/task138-ci7-full-check.log`. There were no dangling-process warnings.
- Final physical lines: `opener-fixture.ts` 479, `launcher-lifecycle.test.ts` 358, and `opener-persistence.test.ts` 129. The generated ignored `dist` tree was removed after acceptance. No worktree check, fake-opener, canvas, or opener temp root survived. PIDs 1861217/1861252 were confirmed as pre-existing owners in `/home/msc/Projects/archboard`, and PID 3418777 as a separate dev canvas in another worktree; none were signaled. Source commit: `3c7c25e`.

CI8 aggregate opener-persistence diagnosis, 2026-08-30:

- Public run 33279315414 at exact SHA 421c9e880e3662e000bc3713c3cf15bb67cf85a3 entered opener-persistence at 22:46:12.350Z, printed `killed 1 dangling process` at 22:46:17.402Z (5.052 seconds), emitted no case result, and remained stuck until the 30-minute workflow cancellation. Log: `/tmp/task138-ci8.log`.
- Red-capable unchanged-owner loop: one CPU plus 64 controlled competitors failed at 5,013.48 ms with Bun's exact `this test timed out after 5000ms` and eight completed expectations (`/tmp/task138-ci8-1cpu-64hog-red.log`). No source was edited.
- The throwaway stage-equivalent harness completed every stage. Baseline was 187.31 ms (`/tmp/task138-ci8-stage-baseline.log`). One-CPU/64-competitor pressure was 10,686.59 ms total; the largest stage was save-A at 1,751.83 ms, restart was 248.96 ms, release-B 358.02 ms, and release-A 0.08 ms (`/tmp/task138-ci8-stage-1cpu-64hog.log`). Two-CPU/64-competitor pressure was 2,394.77 ms total, with restart 32.01 ms and both releases below one millisecond (`/tmp/task138-ci8-stage-2cpu-64hog.log`). Capture, child, and reap waits did not hit their existing 2,000 ms actionable diagnostics. Restart is separate: it completed in 248.96 ms under severe pressure and is bounded only by the aggregate Bun test-case timeout. This falsifies a demonstrated unbounded capture or reaping seam and identifies the implicit aggregate five-second Bun case budget as the remaining failure.

CI8 aggregate case-timeout remediation evidence, 2026-08-30:

- Required predecessor RED: the unchanged implicit five-second owner under the recorded one-CPU/64-competitor loop failed at 5,013.48 ms with eight expectations and Bun's exact `this test timed out after 5000ms` (`/tmp/task138-ci8-1cpu-64hog-red.log`). With only the approved shared aggregate timeout applied, the same loop passed all 36 assertions in 5,482.96 ms with no dangling-process warning (`/tmp/task138-ci8-1cpu-64hog-green.log`).
- Independent tracing confirms the owner drives fourteen real processes: two fixture Git processes plus four activations, each with two identity Git processes and one fake opener. Multiple two-core pressure REDs failed at different stages between 5,008 and 5,140 ms. With the diagnostic 15-second case timeout, exact pressure runs completed all 36 assertions in 5,926 ms and 11,150 ms. No unbounded fixture wait was observed.
- Focused acceptance passed twenty absent-vault and twenty caller-sentinel repetitions, 20 tests / 720 assertions apiece; the caller sentinel remained absent (`/tmp/task138-ci8-unset-repeat-green.log` and `/tmp/task138-ci8-sentinel-repeat-green.log`). Complete code-targets passed 63 tests / 320 assertions (`/tmp/task138-ci8-code-targets-green.log`). Complete sequential system passed 281 tests / 4,177 assertions in 165.90 seconds (`/tmp/task138-ci8-system-green.log`).
- TypeScript, Oxlint, formatting, and diff checks passed (`/tmp/task138-ci8-type.log`, `/tmp/task138-ci8-lint.log`, `/tmp/task138-ci8-fmt.log`). One clean `ARCHBOARD_VAULT`-unset `bun run check` passed lint, formatting, both TypeScript projects, 400 module tests / 3,144 assertions, 281 system tests / 4,177 assertions, 100 repository-policy tests / 274 assertions, and all 15 serial headless browser owners (`/tmp/task138-ci8-full-check.log`).
- `timing.ts` is exactly 500 physical lines and `opener-persistence.test.ts` is 134. The generated ignored `dist` tree and throwaway `[DEBUG-ci8]` stage harness were removed. No check, browser, fake-opener, or contention process survived. Four diagnostic opener roots from overlapping red probes remain because exact ownership cannot be separated safely; they predate the accepted green/full runs and no process references them. Source commit: `64c346a`.

CI9 diagnosis before implementation: GitHub Actions run https://github.com/miklschmidt/archboard/actions/runs/33281484674, job 99177280359, exact SHA aa7ed0f043e759ca5b73b5ab1941562dfe7033cb, cancelled only at the retained 30-minute job timeout. The log cursor was presentation-contract start at 2026-08-29T23:41:07.5433275Z and killed 1 dangling process at 23:41:12.5775736Z. Local unchanged baseline: 1 pass, 101 assertions, case 955.06 ms. Controlled predecessor RED: one CPU plus 32 exact recorded competitors, 1 fail at 5,000.03 ms with the same dangling-process diagnostic. Non-mutating 15-second probe under the identical load: 1 pass, 101 assertions, 14,815.78 ms, no dangling warning. All recorded competitor PIDs were reaped after every loop. A deliberately excessive 64-competitor probe made forward progress to 38 assertions before the 15-second probe deadline, further contradicting a fixed wait. Logs: /tmp/task138-ci9-run-33281484674.log, /tmp/task138-ci9-presentation-baseline.log, /tmp/task138-ci9-presentation-pressure32-red.log, /tmp/task138-ci9-presentation-pressure32-probe.log.

CI9 presentation-contract remediation evidence, 2026-08-30:

- Source-level pressure GREEN: the identical one-CPU/32-competitor loop completed the unchanged 101-assertion contract in 14,404.99 ms under TEST_CODE_TARGET_PRESENTATION_CASE_TIMEOUT_MS = 20,000, with no dangling-process warning and no owned stress PID residue. Log: /tmp/task138-ci9-presentation-pressure32-green.log.
- Environment isolation passed 20/20 focused runs with ARCHBOARD_VAULT absent and 20/20 with caller sentinel /tmp/task138-ci9-caller-sentinel-vault. The sentinel path was never created; neither log contains a failure or dangling warning. Logs: /tmp/task138-ci9-presentation-unset-20x.log and /tmp/task138-ci9-presentation-sentinel-20x.log.
- Complete affected lanes passed with ARCHBOARD_VAULT absent: code-targets 63 tests / 320 assertions in 3.46 s; the complete sequential system lane 281 tests / 4,177 assertions in 164.89 s. Logs: /tmp/task138-ci9-code-targets-green.log and /tmp/task138-ci9-system-green.log.
- TypeScript for both projects, Oxlint, formatting, git diff hygiene, and the timing.ts line cap passed; timing.ts remains exactly 500 physical lines. Logs: /tmp/task138-ci9-type.log, /tmp/task138-ci9-lint.log, and /tmp/task138-ci9-fmt.log.
- One clean sequential ARCHBOARD_VAULT-unset bun run check passed lint, formatting, both TypeScript projects, 400 module tests / 3,144 assertions, 281 system tests / 4,177 assertions, 100 repository-policy tests / 274 assertions, and all 15 serial headless browser owners. Log: /tmp/task138-ci9-full-check.log. No failure or dangling-process warning appeared.
- The implementation changes only src/shared/timing/timing.ts and tests/system/code-targets/presentation-contract.test.ts: one named 20,000 ms aggregate Bun case threshold and the existing test third argument. Product code, workflow, 30-minute job timeout, test body, all 101 assertions, datasets, routes, note proofs, inner waits, skips, and allow-failure behavior are unchanged.
- Cleanup removed only four exact presentation-contract roots left by this diagnosis intentionally timing out the owner; their timestamps and contents matched the four probes. All recorded stress PIDs are gone, the caller-sentinel path is absent, and no TASK-138 check/browser/presentation process remains. The four protected ambiguous roots were not inspected or changed. The pre-existing ignored dist tree remains present.

CI9 independent visible-thread review, 2026-08-30:

- Review task 01a05014-063d-7df1-8651-02172beea3cd assessed the working-tree remediation against fixed base aa7ed0f043e759ca5b73b5ab1941562dfe7033cb using separate Standards and Spec axes. Verdict: REVIEW_CLEAN, zero findings on both axes.
- The reviewer independently verified Bun numeric third-argument behavior, timing.ts at exactly 500 lines, preservation of the test callback, all assertions/transitions and AsyncDisposableStack cleanup, the finite fail-closed threshold and 35% constrained margin, and every named red/green/broad-gate evidence claim.
- Remaining risk is limited to the planned hosted exact-SHA rerun. The reviewer changed no file and did not inspect or change the protected roots.

CI10 diagnosis before implementation: exact run https://github.com/miklschmidt/archboard/actions/runs/33283664973, job 99182990933, SHA bfc936d8d8aa997dde8ac8454d16e7b4c315d153. Hosted totals before browser were green: 400 modules / 3,144 assertions, 281 system / 4,177 assertions, and 100 repository / 274 assertions. The first browser owner failed after 36,779.12 ms with SuppressedError and three assertions, before pane/user-agent checks. Full log: /tmp/task138-ci10-run-33283664973.log.

Local canonical baseline passed 64 assertions in 56,271.59 ms (/tmp/task138-ci10-human-performance-baseline.log). Canonical one-CPU/eight-competitor RED failed after 82,450.34 ms with exactly three assertions and actionable primary error agent-browser open ended with signal SIGTERM from agent-browser.ts command timer; every recorded competitor and adapter-owned process was reaped (/tmp/task138-ci10-human-performance-pressure-red.log). One CPU without competitors allowed open to complete, then failed honestly at the unchanged hold budget with 57 assertions, proving full-owner CPU starvation changes the product oracle and is diagnosis evidence only (/tmp/task138-ci10-human-performance-onecpu-red.log). The repair remains unimplemented pending this recorded amendment.

CI10 targeted browser-open remediation evidence, 2026-08-30:

- The exact hosted SHA bfc936d run 33283664973 passed 400 module tests / 3,144 assertions, 281 system tests / 4,177 assertions, and 100 repository-policy tests / 274 assertions before the first browser owner stopped after its three pre-open assertions at 36,779.12 ms with a suppressed termination error. Log: /tmp/task138-ci10-run-33283664973.log.
- The canonical focused owner passed locally at baseline (64 assertions, 56,271.59 ms). Under one CPU and eight controlled competitors, the unchanged adapter reproduced the exact three-assertion cursor and exposed the primary cause: agent-browser open received SIGTERM at the generic 30,000 ms command cap. Log: /tmp/task138-ci10-human-performance-pressure-red.log. A one-CPU run without competitors completed open and then correctly failed the unchanged frame-hold performance oracle, so artificial full-owner contention was excluded from acceptance and timeout sizing.
- The adapter now accepts an optional validated per-command timeout while retaining the 30-second default. Only human-edit-performance initial open for the existing 10,000-element fixture receives TEST_HUMAN_PERFORMANCE_OPEN_TIMEOUT_MS = 90,000; the fixture, all 64 assertions, performance thresholds, later commands, cleanup, the 240-second Bun case cap, workflow timeout, and all gates remain unchanged. Invalid overrides reject before spawn, and timeout diagnostics include the exact command and selected threshold.
- TDD RED proved the previous API treated the options object as stdin (/tmp/task138-ci10-command-options-red.log). The isolated fake-command contract now proves a short override times out with its exact diagnostic, the same command completes under a longer override, zero rejects before spawn, and cleanup succeeds: 1 test / 6 assertions (/tmp/task138-ci10-command-options-clean.log).
- Focused real-browser acceptance passed 64 assertions in 56,071.58 ms (/tmp/task138-ci10-human-performance-green.log). Complete repository policy passed 101 tests / 280 assertions (/tmp/task138-ci10-repository-final.log), and the complete serial browser lane passed all 15 owners (/tmp/task138-ci10-browser-lane-green.log). TypeScript, Oxlint, formatting, and diff checks passed (/tmp/task138-ci10-type.log, /tmp/task138-ci10-lint.log, /tmp/task138-ci10-fmt.log).
- A final clean sequential bun run check passed 400 module tests / 3,144 assertions, 281 system tests / 4,177 assertions, 101 repository-policy tests / 280 assertions, and all 15 serial headless browser owners. No failure, timeout, dangling-process, or context-canceled diagnostic occurred. Log: /tmp/task138-ci10-full-check-final.log.
- Physical lines: timing.ts 500, agent-browser.ts 400, human-edit-performance.test.ts 471, test-inventory.test.ts 479. No test was split: the owner remains one stateful canvas contract, avoiding duplicate 10,000-element setup or ordered shared state. No owned check, browser, canvas, or contention process survived; protected TASK-139 and the four named ambiguous diagnostic roots were untouched.

CI10 independent-review remediation evidence, 2026-08-30:

- Review thread 01a05014-063d-7df1-8651-02172beea3cd requested unconditional/observable cleanup, exact timeout diagnostics, hosted-stable fake timings, and evidence-count corrections. The earlier item-26 claim of 31 static expect sites is superseded by the measured unchanged baseline of 54 textual expect sites and 64 runtime assertions. The earlier 479-line test-inventory evidence is superseded by the final formatted measurement below.
- The fake slow command is now condition-controlled by an owned release file. The timeout branch receives a finite 5,000 ms command threshold; the success branch receives 20,000 ms and is released explicitly. The diagnostic assertion matches the complete command, threshold, exit, colon, and empty stderr exactly. Invalid zero still rejects before the marker records a spawn.
- AsyncDisposableStack owns close, capture, caller-environment restoration, and owned-root removal in LIFO order. Disposal continues and preserves failures if close rejects. Post-disposal assertions require the exact [slow, slow, close] command sequence, equality with every caller environment value, and absence of the fixture root; deleting any cleanup registration makes the case red.
- Honest amendment RED: with the reviewed 5,000 ms command timeout but the implicit 5,000 ms Bun case threshold, the owner failed at 5,000.59 ms before adapter cleanup could complete (/tmp/task138-ci10-review-regression-green.log from the first attempt). The final case uses the existing finite TEST_BROWSER_COMMAND_TIMEOUT_MS = 30,000 as its Bun lifecycle threshold; command behavior remains independently bounded at 5,000/20,000 ms.
- Final focused regression passed 1 test / 9 assertions in 5,067.75 ms (/tmp/task138-ci10-review-regression-final.log). The clean complete repository-policy rerun passed 101 tests / 283 assertions with no failure, dangling-process, or context-canceled diagnostic (/tmp/task138-ci10-review-repository-final.log). Both TypeScript projects, Oxlint, repository formatting, and git diff checks passed (/tmp/task138-ci10-review-type.log, /tmp/task138-ci10-review-lint.log, /tmp/task138-ci10-review-fmt.log, /tmp/task138-ci10-review-diff.log).
- Final physical lines: timing.ts 500, agent-browser.ts 400, human-edit-performance.test.ts 471, test-inventory.test.ts 498. No product owner, timing constant, adapter production behavior, workflow, gate, assertion, or performance oracle changed after the previously clean rendered/browser/full-gate acceptance; no repeat of those process-sensitive lanes was warranted by this test-harness-only amendment.

CI10 rereview correction evidence, 2026-08-30:

- The prior sentence citing /tmp/task138-ci10-review-regression-green.log as a 5,000.59 ms RED is withdrawn: that filename was overwritten and now contains a pass, so it is not RED evidence. A first attempted exact 5,000 ms lifecycle mutant also passed at 5,051.73 ms, confirming that equal command/case deadlines form a scheduling race and should not be claimed as deterministic.
- Deterministic retained RED instead sets only the Bun case lifecycle threshold to 4,000 ms while leaving the command timeout at 5,000 ms. It failed at 4,000.86 ms with Bun exact this test timed out after 4000ms and one dangling process killed: /tmp/task138-ci10-review-case-budget-red.log. The timeout bypassed test-scope disposal and left exactly owned root /tmp/archboard-browser-preflight-ZdaDwZ; no process referenced it, and it was deleted explicitly. The final source was restored to TEST_BROWSER_COMMAND_TIMEOUT_MS = 30,000 before acceptance.
- AsyncDisposableStack and root-removal registration now occur immediately after createBrowserPreflightFixture returns, before mkdir, fake executable writes, environment construction, or any other setup. Environment restoration remains registered before mutation; close, marker capture, restoration, and root removal retain the reviewed LIFO order and post-disposal assertions.
- Final focused acceptance passed 1 test / 9 assertions in 5,064.11 ms (/tmp/task138-ci10-review2-regression-final.log). Final complete repository policy passed 101 tests / 283 assertions with no failure, dangling-process, or context-canceled diagnostic (/tmp/task138-ci10-review2-repository-final.log). Both TypeScript projects, Oxlint, formatting, and diff checks passed (/tmp/task138-ci10-review2-type.log, /tmp/task138-ci10-review2-lint.log, /tmp/task138-ci10-review2-fmt.log, /tmp/task138-ci10-review2-diff.log).
- Final physical lines supersede earlier values: timing.ts 500, agent-browser.ts 400, human-edit-performance.test.ts 471, test-inventory.test.ts 499. No fresh browser-preflight root or owned process remains.

CI10 final cleanup correction, 2026-08-30:

- The prior no-fresh-root sentence was premature: a final audit found exact task-owned `/tmp/archboard-browser-preflight-ce6ksE`, created at 03:17:39 by the earlier overwritten timing attempt. It contained only the fake preflight fixture and one `slow` marker. `lsof +D` found no process reference, and the exact root was removed without touching any other temporary directory. The browser-preflight root glob is now empty. This correction supersedes only the earlier cleanup claim; source, validation, and retained RED evidence are unchanged.

CI10 final independent review, 2026-08-30:

- Visible review task `01a05014-063d-7df1-8651-02172beea3cd` returned `REVIEW_CLEAN` on both Standards and Spec after verifying immediate cleanup ownership, exact cleanup/restoration assertions, stable fake timing, the retained deterministic RED, corrected evidence counts, and both task-owned residual-root deletions. The reviewer confirmed the `/tmp/archboard-browser-preflight-*` root namespace is empty and the five-file diff is clean. Remaining risk is only the planned hosted exact-SHA run.

CI11 hosted diagnosis, 2026-08-30:

- Exact run: https://github.com/miklschmidt/archboard/actions/runs/33285965183, job 99189107773, SHA 06ad185b510555ccab65313c6b5c9e7c205f8430. Setup and browser installation passed. Modules passed 400/400 with 3,144 assertions. System activation and settings passed, then the opener-persistence group opened at 01:35:36.612Z and produced no result or timeout diagnostic before cancellation at 02:04:15.992Z. Log: /tmp/task138-ci11-run-33285965183.log.
- Controlled current-source reproduction with one CPU and 256 owned competitors failed the same owner at its explicit 15,000ms case threshold after eight expectations, killed one dangling process, and completed cleanup only after 34.82s. All 256 competitors were reaped. Log: /tmp/task138-ci11-opener-pressure256-repro.log. A 96-competitor control remained green at 36 assertions / 7,431.05ms. Log: /tmp/task138-ci11-opener-pressure-repro.log.
- The actionable defect is duplicated real-process lifecycle in a persistence/restart contract: aggregate timeout cancellation enters slow process cleanup before Bun can report predictably. The existing route dependency seam can preserve observable persistence behavior without removing the dedicated real-launch contracts.

CI11 persistence-responsibility remediation evidence, 2026-08-30:

- TDD predecessor RED injected the existing route `launch` recorder before removing persistence's old capture waits. The public save and activation succeeded, then the old `waitForCapture` failed at its exact 2,000ms operation bound because no duplicate subprocess was launched: 0 pass / 1 fail / 3 assertions at 2,069.44ms. The owned root was removed. Log: /tmp/task138-ci11-recorder-predecessor-red.log.
- The final unsplit persistence case records commands synchronously at the route dependency seam. It asserts the exact four-command array and order: selection A once, selection B from each independent caller, and selection B after fixture restart; every command includes the exact resolved checkout target. Public PUT/activation routes, latest-selection behavior, note bytes/mtime, portable-note forbidden values, caller-vault restoration, and fixture cleanup remain. Real launch, unref, detached-process, response-before-exit, capture, exit, and reap contracts remain in activation-contract and launcher-lifecycle. The obsolete 15,000ms persistence-only aggregate exception is deleted, returning the process-free case to Bun's tighter default threshold.
- Focused acceptance passed 1 test / 22 assertions in 195.24ms. Twenty absent-vault and twenty caller-sentinel isolated runs passed; the owned sentinel stayed empty and was deleted. Logs: /tmp/task138-ci11-opener-recorder-green.log, /tmp/task138-ci11-opener-unset-20x.log, and /tmp/task138-ci11-opener-sentinel-20x.log.
- Complete code-targets passed 63 tests / 306 assertions in 3.55s, including both dedicated real-process lifecycle owners. Complete sequential system passed 281 tests / 4,163 assertions in 165.61s; persistence completed in 108.13ms. Logs: /tmp/task138-ci11-code-targets-green.log and /tmp/task138-ci11-system-green.log.
- Both TypeScript projects, Oxlint, formatting, and diff checks passed: /tmp/task138-ci11-type.log, /tmp/task138-ci11-lint.log, /tmp/task138-ci11-fmt.log, and /tmp/task138-ci11-diff.log. One clean `ARCHBOARD_VAULT`-absent `bun run check` passed 400 modules / 3,144 assertions, 281 system / 4,163, 101 repository / 283, and all 15 serial browser owners with no timeout, dangling-process, context-canceled, or failure marker: /tmp/task138-ci11-full-check.log.
- Final physical lines: timing.ts 499 and opener-persistence.test.ts 141. The case was not split. Its 14 removed runtime assertions were process capture/reap details retained in the dedicated real-launch owners; one exact ordered four-command equality now enforces the persistence interface directly. All 256 and 96 diagnostic competitors were reaped, the recorder-RED root is absent, the browser-preflight root namespace is empty, and no TASK-138 validation process remains. Protected TASK-139 and the four named ambiguous diagnostic roots were not inspected or changed.

CI11 independent rereview remediation, 2026-08-30:

- Review found two medium issues before commit: the 20,000 ms presentation-case rationale was attached to the 2,000 ms opener lifecycle object, and a final command-only [A, B, B, B] equality could not distinguish two pre-restart B launches plus no post-restart launch.
- TEST_OPENER_LIFECYCLE now has its own adjacent fake-opener 2-second operation-bound rationale; the aggregate Bun-case rationale is directly beside TEST_CODE_TARGET_PRESENTATION_CASE_TIMEOUT_MS. timing.ts remains exactly 500 physical lines.
- Opener persistence remains one unsplit public HTTP/restart case and retains one aggregate equality. That equality now compares the ordered timeline of each launch, each completed activation request, and the completed restart boundary. A duplicate launch before one caller, a caller that launches nothing, or a missing post-restart launch changes the exact sequence. The owner remains 22 runtime expectations and is 162 physical lines.
- Focused owner GREEN: 1/1, 22 expectations, 188.09 ms in /tmp/task138-ci11-rereview-focused.log. Complete sequential code-targets GREEN: 63/63, 306 expectations, 3.29 s in /tmp/task138-ci11-rereview-code-targets.log, including the real activation and launcher lifecycle owners.
- Both TypeScript projects, Oxlint, formatting, and diff checks are green in /tmp/task138-ci11-rereview-type.log, /tmp/task138-ci11-rereview-lint.log, /tmp/task138-ci11-rereview-fmt.log, and /tmp/task138-ci11-rereview-diff.log. No owned opener/code-target validation process remains; the only pgrep match was the audit shell itself. The prior clean full bun run check evidence remains applicable because this amendment changes only test timeline observability and adjacent comments, not product behavior or any gate.

CI11 findings-remediation rereview, 2026-08-30:

- Independent Standards and Spec rereview returned REVIEW_CLEAN with zero findings. It confirmed the timing rationales are adjacent to the constants they bound, line caps hold, and the typed timeline adds no actionable smell.
- The reviewer verified the production route awaits planAndLaunch before returning the public activation response, so launch entries necessarily precede their test-side activation-complete checkpoints; fixture.restart likewise resolves before restart-complete. The single exact timeline therefore detects duplicate, missing, wrong-caller, and wrong-side-of-restart launches while retaining exact command/target proof.
- The reviewer confirmed TASK-138 item 30 accurately supersedes the earlier command-only evidence and accepted the proportional validation plus retained clean full-check evidence. Review task: 01a05014-063d-7df1-8651-02172beea3cd.

CI12 hosted browser diagnosis, 2026-08-30:

- Exact pushed SHA 93df3aaee05a5f590c173e2f204f5ad36cc6f395 failed in GitHub Actions run https://github.com/miklschmidt/archboard/actions/runs/33288114113, job 99194813843. Setup, strace, agent-browser 0.34.0, the downloaded executable, and the single canonical check step all ran. Lint, formatting, both TypeScript projects, 400 module tests, all 281 sequential system tests, and all 101 repository-policy tests passed.
- The first browser owner, human-edit-performance.test.ts, failed after 36,062.58 ms with only three expectations, exactly the pre-open health/seed assertions. No pane or interaction assertion ran. The outer browser.run open budget was already 90,000 ms, so the failure was below that bound. Hosted log: /tmp/task138-ci12-run-33288114113.log.
- agent-browser v0.34.0 is upstream commit 548b159b30eef119ccf6846c8bc807d0eaa3f6f8. Its native BrowserManager defaults operation and navigation-lifecycle waits to 25,000 ms and reads AGENT_BROWSER_DEFAULT_TIMEOUT for an override. Its daemon source explicitly rechecks activity after an in-flight command releases state and rearms the idle timer, so the configured 30,000 ms daemon idle lifetime does not terminate a command that is still holding daemon state. The hosted duration is consistent with browser startup followed by the nested 25-second lifecycle wait.
- The remediation will hand the already-justified 90,000 ms human fixture open budget to the nested agent-browser operation timeout for that owner only. Ordinary owners retain their current environment and effective 30-second adapter command bounds; cleanup continues to use the separate 30-second idle observation.

CI12 nested browser timeout remediation evidence, 2026-08-30:

- TDD RED extended the existing real adapter fake to record AGENT_BROWSER_DEFAULT_TIMEOUT. The ordinary owner correctly recorded an empty value, and the human owner failed because it also recorded empty instead of the required 90000: 28 pass / 1 fail / 63 expectations. Log: /tmp/task138-ci12-operation-timeout-red.log.
- run-browser-lane.ts now sets AGENT_BROWSER_DEFAULT_TIMEOUT to the existing TEST_HUMAN_PERFORMANCE_OPEN_TIMEOUT_MS only when the selected file is human-edit-performance.test.ts. browserTestEnvironment forwards that optional value only to agent-browser; canvasTestEnvironment continues stripping every AGENT_BROWSER_* variable from canvas subprocesses. No workflow, fixture, product, assertion, outer command timeout, idle cleanup timeout, or Bun case threshold changed.
- Focused fake GREEN passed 29 tests / 63 expectations and proves the ordinary owner remains unset while the human owner receives exactly 90000 through the spawned agent-browser process: /tmp/task138-ci12-operation-timeout-green.log. The real focused human browser owner passed 1 test / 64 expectations in 55,698.12 ms with the full 10,000-element fixture and retained measurements: /tmp/task138-ci12-human-browser-green.log.
- Both TypeScript projects and targeted Oxlint, formatting, and owned-diff checks are green: /tmp/task138-ci12-type.log, /tmp/task138-ci12-lint.log, /tmp/task138-ci12-fmt.log, and /tmp/task138-ci12-diff.log. Physical lines: runner 491, browser support 403, CI browser policy 341, repository-policy support 486. The browser-preflight root namespace is empty and no owned human/browser/policy process remains.
- Shared main advanced independently to protected local commit b7be932 after validation. The CI diff does not include or inspect that design/TASK-140 content; parent coordination is required before any push that might include the unrelated commit.

CI12 independent review on the isolated 93df3aa worktree requested two P2 test-boundary corrections while finding the timeout behavior and TASK evidence correct: temporary roots were not armed for cleanup before every throwing setup/spawn edge, and the regression observed only the fake agent-browser child rather than the traced canvas subprocess. Remediation keeps production timeout selection unchanged. createBrowserPreflightFixture now removes its root if initialization throws; usePreflightFixture immediately registers every returned root on an AsyncDisposableStack; runAdapter acquires only through that helper; all adapter-boundary owners dispose through the stack. The fake strace records the canvas child value in an owned marker, and the human-owner proof now requires exact empty canvas AGENT_BROWSER_DEFAULT_TIMEOUT alongside exact owner 90000 and ordinary-owner empty. Focused policy GREEN: /tmp/task138-ci12-review-remediation-focused.log, 29 pass, 64 assertions, 3.25s; the browser-preflight root glob was empty afterward and git diff --check remained green. Source lines after formatting: ci-browser-gate.test.ts 330; repository support 493.

CI12 review-remediation validation, isolated 93df3aa worktree: focused ci-browser-gate passed 29/29 tests and 64 assertions in 3.25s (/tmp/task138-ci12-review-remediation-focused.log); complete repository policy passed 102/102 tests and 288 assertions in 12.71s (/tmp/task138-ci12-review-remediation-repository.log). Both TypeScript projects, full Oxlint, full formatting check and git diff --check passed (/tmp/task138-ci12-review-remediation-type.log, /tmp/task138-ci12-review-remediation-lint.log, /tmp/task138-ci12-review-remediation-fmt.log). No browser-preflight root remained. The isolated diff still contains exactly the authorized five paths. Final physical lines: runner 491, browser support 403, CI policy 330, inventory support 493.

CI12 presence-sentinel rereview correction: the earlier item-32 wording that empty marker bytes proved absence is superseded. Both fake boundaries now record exact key state: `absent` when AGENT_BROWSER_DEFAULT_TIMEOUT is missing and `present:<value>` when defined. The policy requires ordinary agent-browser `absent`, human agent-browser `present:90000`, and the real traced-canvas subprocess `absent`, so present-but-empty mutants fail. Focused GREEN: /tmp/task138-ci12-review-remediation-presence-focused.log, 29/29 tests, 64 assertions, 2.31s. Complete repository GREEN: /tmp/task138-ci12-review-remediation-presence-repository.log, 102/102 tests, 288 assertions, 13.11s. Both TypeScript projects, Oxlint, formatting and diff remained green in /tmp/task138-ci12-review-remediation-presence-{type,lint,fmt}.log; no browser-preflight root remained. Lines remain runner 491, browser support 403, CI policy 330, inventory support 493.

CI12 isolated clean-worktree acceptance: after REVIEW_CLEAN, one sequential `env -u ARCHBOARD_VAULT -u AGENT_BROWSER_EXECUTABLE_PATH bun run check` passed from detached base 93df3aa. Log: /tmp/task138-ci12-isolated-full-check.log. Results: lint, formatting and both TypeScript projects green; 400 module tests / 3,144 assertions; 281 serialized system tests / 4,163 assertions; 102 repository-policy tests / 288 assertions; all 15 serial headless browser owners green. The human 10,000-element owner passed 1/64 in 56.69s with the new nested operation timeout. No failure, dangling-process diagnostic, or browser-preflight root remained. The diff still contains exactly the five authorized TASK-138 paths; shared main remains untouched at b7be932.

CI13 hosted RED: run 33289473418 for pushed SHA 315c0fc81c3123fa9dcf3c6cfffecbf2b104f248 completed modules 400/400, then entered tests/system/code-targets/opener-persistence.test.ts at 03:07:30Z. Bun printed killed 1 dangling process at 03:07:35Z, emitted no owner result or later test output, and GitHub cancelled Run all checks at the 30-minute job limit at 03:35:52Z. The immediately preceding clean-runner run 33288114113 executed the identical owner in 87.46 ms and completed system 281/281, so the evidence identifies a flaky crossing of Bun default case failure handling rather than aggregate browser cost or assertion work. Logs: /tmp/task138-ci12-hosted-timeout.log and /tmp/task138-ci12-predecessor-hosted.log.

CI13 remediation validation before rereview: source adds TEST_OPENER_PERSISTENCE_CASE_TIMEOUT_MS = 20,000 with adjacent aggregate-Bun-case rationale and passes it only as opener-persistence test third argument. The owner body and 22 expectations are unchanged; timing.ts remains exactly 500 lines and opener-persistence is 167. Twenty ARCHBOARD_VAULT-unset focused processes and twenty caller-sentinel focused processes all passed 1/1 and restored state; the sentinel path remains absent. Complete serialized code-targets passed 63/63 with 306 assertions, including opener at 145.76 ms and both real launcher owners. Both TypeScript projects, Oxlint, formatting, and git diff checks passed. Logs: /tmp/task138-ci13-opener-unset-20x.log, /tmp/task138-ci13-opener-sentinel-20x.log, /tmp/task138-ci13-code-targets.log, /tmp/task138-ci13-type.log, /tmp/task138-ci13-lint.log, /tmp/task138-ci13-fmt.log, /tmp/task138-ci13-diff.log.

CI13 independent review and clean acceptance: visible review task 01a05014-063d-7df1-8651-02172beea3cd returned REVIEW_CLEAN with zero Standards and zero Spec findings. It confirmed the whitespace-insensitive owner body, phase-aware timeline, all 22 expectations, environment restoration, and fixture cleanup are unchanged; only the finite third test argument is executable. One isolated env -u ARCHBOARD_VAULT -u AGENT_BROWSER_EXECUTABLE_PATH bun run check then passed lint, formatting, both TypeScript projects, 400 modules / 3,144 assertions, 281 serialized system tests / 4,163 assertions, 102 repository-policy tests / 288 assertions, and all 15 serial headless browser owners. Opener persistence passed in 129.56 ms; the human 10,000-element owner passed 1/64 in 55.86 s. Log: /tmp/task138-ci13-full-check.log. Final precommit audit found exactly the three planned paths, clean diff, no browser-preflight root, and no surviving owned check/browser/opener process.

CI14 hosted evidence: run 33291111341 for cd1b51978ab14e8004ab17be22987a3d215a6960 passed the repaired opener owner within complete system 281/281 and passed repository 102/102. It then failed human-edit-performance at 36,908.46 ms with 0/1 and only three expectations, before the open returned and before any interaction/performance assertion. This is the same hosted-only boundary as run 33288114113 after the CI12 native timeout handoff, while local CI13 complete acceptance ran that unchanged 10,000-element owner 1/1 with 64 expectations in 55,856.34 ms. Exact log: /tmp/task138-ci13-hosted-run.log. The user explicitly overrode the prior no-weakening requirement for CI only and required the complete suite to remain local; acceptance criterion 2 and the description are updated accordingly.

CI14 TDD and validation before review: repository policy RED passed 28 existing cases and failed only because the real workflow had no explicit hosted exception environment (/tmp/task138-ci14-workflow-red.log). The final policy passed 35/35 with 77 assertions and proves local package selection retains all 15, hosted package selection removes only human-edit-performance, the original selection is immutable, missing CI/wrong-owner/focused use rejects, and the real package adapter prints the exclusion then advances to an ordinary owner (/tmp/task138-ci14-adapter-final.log). A real hosted-mode serial browser lane printed the exact exception and passed all 14 remaining owners in canonical order (/tmp/task138-ci14-hosted-browser-green.log). Complete repository policy passed 108/108 with 301 assertions; both TypeScript projects, Oxlint, formatting, and diff checks passed (/tmp/task138-ci14-repository-final.log and /tmp/task138-ci14-{type,lint,fmt,diff}-final.log). The first static sequence honestly found and then fixed only a readonly matcher typing error; no behavior changed. Physical lines: runner 498, browser support 422, CI policy owner 416. No browser-preflight root or owned process remains.

CI14 first independent review found the fail-closed exclusion implementation and Spec clean, but rejected two stale hosted contracts: the workflow job plus AGENTS.md and docs/agents/test-suite.md still promised the complete hosted suite, and the workflow still installed strace even though the sole consumer is excluded before prerequisite probing. The remediation makes the complete-versus-hosted boundary explicit in both canonical docs, removes that unused privileged/network setup, and pins the hosted job name plus absence of a strace run step in repository policy. This supersedes item 34 only where it described the hosted setup/documentation; the exact one-owner exception and complete local gate remain unchanged.

CI14 canonical-contract remediation validation: .github/workflows/ci.yml now names the hosted subset, removes the standalone strace apt step, and keeps the exact CI-only environment only on bun run check. AGENTS.md and docs/agents/test-suite.md now define bun run check as the complete local 15-owner gate and GitHub Actions as the exact 14-owner hosted subset; the local human-performance prerequisite remains documented. Repository policy pins the hosted job name, proves no run step installs/invokes strace, and retains the exact environment/selection checks. Focused policy passed 35/35 with 79 assertions (/tmp/task138-ci14-doc-remediation-focused.log); complete repository policy passed 108/108 with 303 assertions (/tmp/task138-ci14-doc-remediation-repository.log). Both TypeScript projects, Oxlint, formatting, and diff checks passed (/tmp/task138-ci14-doc-remediation-{type,lint,fmt,diff}.log). Final current physical lines: runner 498, browser support 422, CI policy 424. The diff contains exactly seven TASK-138 paths, no browser-preflight root remains, and the only process-audit match was the audit shell itself.

CI14 plan-history correction: the preceding statement that item 35 superseded only item 34 was too narrow. This correction explicitly supersedes item 1 single hosted-strace-install instruction as well. The current workflow, canonical docs, repository-policy checks, local complete gate, hosted exact exception, and all validation evidence are unchanged.

CI14 findings-remediation rereview: visible task 01a05014-063d-7df1-8651-02172beea3cd returned REVIEW_CLEAN with zero Standards and zero Spec findings after item 36 closed the stale hosted-strace plan instruction. The reviewer confirmed canonical docs, workflow, repository policy, exact fail-closed exception, seven-path scope, line caps, retained logs, and local-15/hosted-14 contract are consistent.

CI14 isolated complete local acceptance after REVIEW_CLEAN: env -u ARCHBOARD_VAULT -u AGENT_BROWSER_EXECUTABLE_PATH -u ARCHBOARD_CI_EXCLUDED_BROWSER_OWNER -u AGENT_BROWSER_DEFAULT_TIMEOUT -u CI bun run check passed in /tmp/task138-ci14-full-check.log. Lint, formatting, and both TypeScript projects passed; modules passed 400/400 with 3,144 assertions; serialized system passed 281/281 with 4,163 assertions; repository policy passed 108/108 with 303 assertions; and all 15 serial browser owners passed. The local-only human 10,000-element owner passed 1/1 with 64 assertions in 57,710.10 ms. No CI-only exclusion diagnostic, failure, dangling-process diagnostic, browser-preflight root, or owned validation process remained; the sole process-audit match was the audit shell itself. The final uncommitted diff contains exactly the seven reviewed TASK-138 paths and git diff --check is green.
<!-- SECTION:NOTES:END -->
