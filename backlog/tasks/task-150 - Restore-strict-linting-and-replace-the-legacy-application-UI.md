---
id: TASK-150
title: Restore strict linting and replace the legacy application UI
status: In Progress
assignee:
  - '@codex'
created_date: '2026-09-05 00:08'
updated_date: '2026-09-05 03:47'
labels: []
dependencies: []
references:
  - TASK-143.08.01
  - TASK-140
  - TASK-144
  - TASK-149
  - TASK-149.01
  - docs/design/assets/operator-sidebar-reference.png
  - 'https://ui.shadcn.com/docs/cli'
  - 'https://ui.shadcn.com/docs/components-json'
  - 'https://oxc.rs/docs/guide/usage/linter/type-aware'
priority: high
type: feature
ordinal: 290000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Unchecked TypeScript paths and parallel CSS/control systems make UI work slower and less trustworthy for maintainers and agents. Restore a complete enforced strict baseline before replacing the application UI with official shadcn Base UI components, Tailwind layout and styling, and Remix Icon. Implementation has not started; final shared-understanding confirmation is pending.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Before fresh UI construction, quarantine and code repair establish passing strict lint and compiler gates for every retained TypeScript source class, including active product logic, tests, scripts, tooling and locally generated source. Restore the seven named rules, audit all applicable rules and enforce 500 physical lines on authored code. The inert local legacy/ archive is explicitly excluded and never committed or imported. Approved vendor/shadcn safety and structural-exemption policies remain enforced. Archived UI product logic stays inert until TASK-150.07, after TASK-150.05; copied code immediately receives the enforced strict policy.
- [ ] #2 Ordinary pinned Oxlint type-aware linting is mandatory in local and hosted gates, demonstrably checks the agreed source inventory, and reports actual type-aware violations. Keep strict compiler checks, propagate failures, and avoid parallel compiler/test orchestration or a bespoke analysis runner.
- [ ] #3 The application uses official pinned shadcn Base UI source in a shared component module, Tailwind for application layout and styling, and verified Remix Icon generation while product behavior remains in named modules. Shared official shadcn component source retains compiler, type-aware safety, React correctness and accessibility checks and is exempt only from authored-code style, module-layout and file-length rules. The workbench uses fresh official assistant-ui components compatible with the shadcn Base UI foundation and the existing Codex-owned product runtime.
- [ ] #4 The replacement presentation is built fresh from the approved shadcn preset and operator reference. Product logic copied from legacy/ is repaired in src/ui under full applicable checks. No legacy archive content enters any commit or active import/build graph, and no old application CSS, JSX controls, compatibility wrappers or parallel styling system remains.
- [ ] #5 Rendered desktop workflows preserve all canvas, navigation, persistence, recovery, text, voice, fullscreen, selection, binding and accessibility behavior in light and dark themes, with complete normal gates and honest manual evidence passing. Browser tests execute only in complete workflow verification after the rebuild is reported ready; all proposed browser-test replacements or behavioral rewrites require prior case-by-case approval by the orchestrating agent, with required behavior and accessibility coverage preserved.
- [ ] #6 Grunt implementation and verification use visible gpt-6-astra low tasks, including TASK-150.07 UI product-logic porting after TASK-150.05. TASK-150.01.02 repairs only source that remains active after quarantine and retains the previously permitted gpt-daybreak-blue-latest low assignment. No independent review starts until all implementation subtasks and complete workflow verification report complete; then run the complete BASE..TARGET gpt-6-astra medium review loop.
- [ ] #7 The official LiveKit wave visualizes model audio output only, integrated with the existing voice playback through an explicitly named output-level contract. Microphone input cannot drive it. No second voice backend or legacy meter remains; strict checks, accessible static/status presentation and final rendered verification cover the adaptation.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Planning record, 2026-09-05. Implementation has not started. Astra owns this plan.

Starting condition
The user will commit the current UI/workbench changes before execution. Record the resulting commit as BASE, refresh the inventory there, and preserve all existing capabilities. Do not use the earlier dirty-checkout HEAD as the review base. Planning inventory found roughly 65 authored files over 500 lines, 820 generated Codex TypeScript files currently excluded from lint, a 1,600-line shell stylesheet, and parallel dialog implementations. These counts are provisional, not acceptance thresholds. TASK-149 and TASK-149.01 remain owned by the current work; consume their committed result rather than replay their scope.

Confirmed decisions
- User-approved archive dependency policy: tests and diagnostic probes that depend exclusively on retired UI may join its ignored, uncommitted local legacy/ snapshot. Inventory their imports and record each protected product behavior, whether the old assertion is obsolete or still required, and the task responsible for restoring required coverage. This includes UI-dependent files outside src/ui; directory location does not decide whether code is retired. Independently useful tests, scripts and active product code stay under full strict checks. Do not archive mixed-use code or difficult active code merely to pass checks; resolve its retained contract explicitly. Remove archived owners from active compiler/test inventories coherently and keep an explicit record of deferred coverage. This temporary retirement does not count as passing product verification and does not authorize skipped tests or weakened final gates. Restore required behavior coverage against the rebuilt implementation in TASK-150.07 or TASK-150.06 as appropriate. Any affected browser-test retirement/replacement remains subject to the orchestrating Astra agent's recorded case-by-case approval. Every deferred behavior must be accounted for before final acceptance; obsolete implementation-only assertions may be retired with a recorded reason.
- Adopt the official LiveKit Agents UI AgentAudioVisualizerWave renderer for the model's voice/audio output only. The user's microphone must not drive the wave. Keep assistant-ui for chat and Archboard's existing Codex voice session and playback authoritative. Reuse pinned upstream renderer/shader source with provenance and license; configure it within the approved theme, using the lime status accent. Adapt only the necessary state/audio-input boundary, with full applicable safety checks. Do not introduce a LiveKit room, server, token endpoint or second voice session. The upstream hook imports LiveKit audio utilities even when volume is supplied, so inspect the actual dependency graph and use a narrowly documented adaptation where needed. This is the explicit custom integration allowance, not permission to rebuild the visualizer or exempt its source broadly from lint. Build the wave presentation in TASK-150.05 with typed model-output level and lifecycle inputs; connect actual audio only in TASK-150.07. Distinguish status animation from audio-driven speech: listening/thinking must not suggest that model audio is playing. Keep readable status and static reduced-motion/renderer-unavailable presentation. Stop the animation when its view/session is inactive, and preserve mute/stop actions.
- Retain assistant-ui as the explicit foundation for the new workbench. Use fresh official assistant-ui chat component source and primitives, its Base UI registry flavor, the approved shadcn theme and shared controls, rather than porting Archboard's old workbench JSX or rebuilding available chat components. Configure the style-aware @assistant-ui registry https://r.assistant-ui.com/styles/{style}/{name}.json with base-nova; verify source compatibility with the pinned @assistant-ui/react version and keep reproducible source provenance. Use shared shadcn dependencies and Remix icons without installing a parallel application control/theme system. Install only chat components needed by actual workflows.
- assistant-ui's runtime provider/view adapter is compatible with the single-runtime requirement: Archboard's private Codex app-server session and product store remain authoritative. Do not introduce Assistant Cloud, an AI SDK backend, another transport, persistence owner or queue merely because a quick-start installs them. Preserve send/steer while running, explicit queue choices, approvals, transcript identity, read-only/recovery states and separate voice coordinator behavior. Reassess the current headless-only/import allowlist and custom composer restrictions against real product contracts and supported upstream components; replace obsolete implementation restrictions with scoped ownership/behavior checks, preserving safety rules. The orchestrating agent decides necessary product-specific adaptations case by case. Existing workarounds are evidence to investigate, not automatically required new code.
- UI product-logic porting happens only after presentation TASK-150.05, in TASK-150.07 using visible gpt-6-astra tasks with low reasoning. TASK-150.01.02 repairs code that remains active after quarantine; it does not port archived UI logic. The strict baseline applies to active source first, then immediately to every file copied out of legacy/. Read actual product contracts during presentation design to avoid guessing; do not port their archived implementation early or let its visual structure dictate the new UI.
- Browser tests run only in TASK-150.06, complete workflow verification, after TASK-150.01 through TASK-150.05, TASK-150.07 and their execution leaves report the rebuild and integration ready. Do not run browser suites, selected browser owners, browser smoke tests or aggregate commands that invoke them during quarantine, code repair, foundation or UI implementation. Strict lint/type checks, builds where applicable and focused non-browser logic tests continue throughout. Visual inspection for design does not authorize early browser workflow tests. Earlier task completion means implemented and ready for complete workflow verification, not browser-verified.
- Existing browser tests are evidence about possible product contracts, not authority for the new UI's markup, selectors, layout, timing or component structure. The orchestrating Astra agent must decide and approve each proposed browser-test replacement, behavioral/interaction rewrite, deletion or transfer to a cheaper test owner before a worker makes that change. For each affected test, record the protected workflow/regression, the obsolete or harmful assumption, the proposed replacement and why its observable assertions still protect the required behavior and accessibility. A failing test alone is not grounds to change either the UI or the test: determine which violates the agreed contract. Reject production changes introduced solely to satisfy old selectors, DOM structure, test hooks, arbitrary waits or harness shortcuts. Workers may investigate and propose changes, but may not self-approve replacements, skip failing tests, reduce coverage or rubber-stamp the existing test suite. Preserve real behavior and recovery obligations. Record the orchestrator's decision with the affected test in Backlog notes; no new approval framework or user approval is required. These case-by-case decisions are orchestration, not an independent review loop; independent review still waits until all implementation work and complete workflow verification report complete. Browser harness replacement/concurrency redesign remains deferred.
- User-approved fresh-UI strategy: Retire the old UI into local ignored legacy/ reference files, never staged or committed. No active imports, re-exports, dynamic loads, build entries or served assets may reach that directory. This is an explicit user-approved exemption from lint and TypeScript coverage for inert archived code only. Copy needed product logic into src/ui and fix it under the full applicable rules; do not import from the archive. Build new presentation from the approved shadcn preset and reference, without old JSX, CSS, token bridges or layout-specific state. Record BASE so workers can recover reference files locally in any checkout without adding them to Git. The retirement is allowed during strict restoration; fresh UI construction remains blocked until retained source passes.
- User-approved desktop resizing: Smaller desktop windows retain the same composition: the canvas flexes and overflowing content scrolls where needed. Keep existing panel collapse controls usable. Do not add alternate compact/mobile layouts or require a fixed 1920×1080 minimum window. 1920×1080 remains the design and rendered-acceptance target.
- The application shell is desktop-only, with 1920×1080 as its explicit design and rendered-acceptance target. Mobile/phone layouts, mobile navigation, mobile breakpoints and mobile test gates are out of scope. Do not introduce them during this rework.
- The user removed dedicated-display support and acceptance from scope. Remove retired dedicated-display assumptions throughout current repository guidance and test policy. Delete FLIP_WHITEBOARD.md and its installation-policy listing; remove Samsung/WM75FX/75-inch hardware references from AGENTS.md, design and reference documents, applicable ADR explanations, skills and authored code comments. Remove hardware-specific acceptance requirements and scenario names. Retain independently useful generic desktop scaling, pointer interaction, accessibility and canvas behavior coverage; do not keep a hardware requirement under a different name. Documentation/policy cleanup may land in the strict baseline; rendered control changes remain after it. Do not rewrite Git history.
- User-approved exception policy: Additional lint suppressions may be statement-level only, for demonstrated false positives or required behavior with no clearer compliant implementation. Document the concrete reason beside the suppression and include it in final-review evidence. Migration effort, file size and achieving a passing check never justify suppression. No file-wide or broad rule/path disable. Prefer correcting the owning contract or writing clear compliant code; do not contort code solely to evade the rule. The final reviewer must assess every added exception; no interim review loop is introduced.
- Approved shadcn preset: https://ui.shadcn.com/create?preset=b3QvqlIdU . Base UI / Nova, Neutral base, Blue theme, Lime chart colors, Small radius, Default / Solid menus, Subtle menu accent and Remix Icon. Manrope is the builder preview substitute; implementation retains approved bundled Onest and DM Mono. Use this preset as the component/theme starting point, with the agreed reference adaptations. Preset preparation is complete; UI implementation still waits for the strict-lint milestone and final planning agreement.
- Strict lint restoration and retained-code repair together form the first blocking milestone. Retiring the old UI into an inert local archive happens within it; fresh UI construction starts only after it passes.
- Verified, untouched vendor-generated TypeScript declarations retain compiler and type-aware safety checks but are exempt from authored-code style, module-layout and file-length rules. This is a specific user-approved exception, not a blanket generated-directory ignore. The generator and authored adapters remain under the full policy. Do not rewrite upstream declaration layout merely to satisfy the 500-line limit.
- User-approved shadcn policy: shared official shadcn component source retains strict compiler checks, type-aware safety, React correctness and accessibility lint. It is exempt from Archboard-authored code-style, module-layout and file-length rules. This is not an all-lint ignore. Archboard-authored feature compositions, adapters, generators and tests receive the full applicable ruleset. Do not place product-specific behavior in the exempt component source to evade rules.
- User-approved shadcn customization boundary: allow theme changes, Tailwind classes, size and color variants, Remix imports and import-path adjustments. Preserve official markup, focus handling, keyboard behavior and state mechanics. Product-specific behavior belongs in Archboard feature modules.
- Use actual official shadcn Base UI component implementations in a shared source directory, with product behavior remaining in named modules. The user selected this over Radix and per-component product module placement.
- Use Tailwind for application layout and styling and Remix Icon for application icons. components.json currently already selects remixicon; verify generation as well as the configuration.
- The user requested deletion of docs/design/archboard-ui-aesthetics.md. Do not recreate its implementation restrictions under a different filename. The operator-canvas reference and approved mockups remain visual authority.
- Delete the legacy application styling system completely. Preserve capability and accessibility rather than legacy appearance.
- User-approved visual priority: preserve the operator reference composition, fonts and color roles, while starting from shadcn control styling. Make targeted density, sizing and usability adjustments. Do not recreate the deleted guide's detailed control geometry or legacy token/utility restriction system through Tailwind overrides.
- This is desktop-only. No dedicated touchscreen hardware support or verification is in scope.
- Browser test harness replacement and concurrency redesign are deferred. A narrow no-await-in-loop exception for the existing serial browser execution/cleanup loop is explicitly approved; all other uses remain subject to the restored rule.
- During later implementation, grunt work requires visible gpt-6-astra tasks with low reasoning, except TASK-150.01.02 active-source lint repair, which explicitly uses gpt-daybreak-blue-latest with low reasoning. This is an execution requirement only. Planning stays in the current Astra task without delegated tasks or subagents. No independent review loop until all implementation subtasks report complete.

1. Restore enforcement, repair active source and pass the strict baseline. TASK-150.01. BLOCKS fresh UI construction.
This milestone contains two ordered tasks. Restoring configuration alone does not establish a passing baseline.

1a. Quarantine the old UI and restore strict enforcement. TASK-150.01.01.
Astra owns architectural decisions; visible gpt-6-astra low tasks perform grunt work.
After the user's current changes are committed, record BASE. Preserve the old UI in local ignored legacy/ as reference, remove retired files from the active tracked tree and disconnect the old rendering entry. Preserve the approved font binaries, licenses, provenance and wordmark as active assets. A fresh implementation checkout may have an incomplete frontend; do not keep a runnable legacy route or feature flag.
The archive is never committed, including intermediate commits. Commit the retirement, ignore rule and enforcement, not a tracked rename into legacy/. Verify the staged tree as well as tracked files. Workers in other worktrees reconstruct their local reference from BASE as needed. Do not create new archive contents to hide difficult active code.
Exclude only this inert archive from lint, TypeScript programs, builds and test discovery. Enforce that active code cannot import, re-export, dynamically load or serve it. A TypeScript exclude alone is not an import prohibition. Use the existing repository-check pipeline to reject active references and tracked/staged archive files; do not add a separate analysis framework.
Audit effective rules and TypeScript program assignment for every retained TS/TSX/MTS/CTS/declaration source class: production, frontend logic, tests and fixtures, scripts, tooling, root configurations, copied components and locally generated declarations. Audit overrides, ignores, CLI flags, inline disables, relaxed options and thresholds, compiler options and the full rule catalogue. Enable every applicable rule; document genuinely inapplicable or mutually exclusive rules.
Restore no-await-in-loop and typescript/no-unsafe-type-assertion, no-unnecessary-type-assertion, no-base-to-string, no-unnecessary-condition, no-unnecessary-type-conversion and consistent-return. Enforce the 500 physical-line authored-code limit. Preserve the agreed vendor-declaration and official-shadcn style/module-layout/file-length exceptions and their required safety coverage.
Restore ordinary pinned Oxlint type-aware execution and replace the repository policy that bans it with real diagnostic proof. The historical OOM was caused by parallel compiler/test orchestration, not an established Oxlint defect. Keep ordinary compiler commands sequential. Do not introduce an alternate-engine investigation, compiler fan-out, custom supervisor, profiling project or per-slice analysis framework.
Keep the approved statement-level no-await-in-loop exception for the existing browser runner's execution/cleanup awaits in runSelection. Browser harness replacement and concurrency redesign remain deferred. Other demonstrated false positives or required semantics may use the separately approved narrow, documented exception policy; no blanket file or test exclusions.
This task can report enforcement restored while diagnostics remain. It does not unblock the new UI.

1b. Repair active source and pass the strict baseline. TASK-150.01.02.
Visible gpt-daybreak-blue-latest tasks with low reasoning perform the previously approved active-code cleanup and non-browser verification; Astra handles difficult module decisions. UI product-logic porting is now a separate later task using Astra low.
Repair the source that remains active after quarantine: server, CLI, shared modules, tests, scripts, tooling and necessary active configuration. Do not copy archived UI logic into src/ui here. The inert archive remains exempt and unreadable through active imports, builds or serving paths.
Correct unsafe boundaries and redundant assertions/conditions/conversions, returns and file structure at their owning contracts. Preserve required sequential I/O and concurrency limits; never mechanically replace loops with Promise.all. Split oversized active files along meaningful responsibilities without forwarding-only fragments or cycles. Do not repair discarded presentation or archived UI logic merely to satisfy the baseline.
Verified untouched vendor-generated declarations remain compiler/type-aware checked with only their agreed structural/style exemptions. Fix real issues at the generator or owning integration without handwritten substitute protocols or patched generated output.
Prove the real lint command emits type-aware diagnostics and covers every retained source class, including imported types. Use small direct good/bad cases and existing checks, not full repository copies or nested compiler suites. Errors, missing prerequisites and violations propagate normally.
Exit for TASK-150.01: both children complete; strict lint, strict compiler checks and applicable retained-logic checks pass with zero warnings and coverage evidence. These checks remain mandatory throughout new UI construction. Configuration restoration alone is insufficient.
The original full bun run check includes frontend build and browser workflows, so it is not a truthful gate for an intentionally retired frontend. Track those unmet workflows explicitly until reconnected, keep behavioral obligations, and do not skip or weaken the complete command to manufacture a green result. Full bun run check is mandatory before final completion. No new hosted exceptions. Previously approved generated/shadcn and statement-level exceptions remain the only active-code exemptions.

2. Establish the fresh shadcn/Tailwind foundation. TASK-150.02.
Depends on 1. Astra high owns the visual/component specification; Astra low UI workers implement it.
Use src/ui/components as the shared module, with generated components at its root, for example button.tsx and dialog.tsx, and registry aliases pointing there. Root component files are public entrypoints; product modules retain their state and imports. Keep dependencies pointing from product composition to components, never back into shell/workbench. Amend the documented boundary contract and its narrow checks to permit this explicit component owner without disabling general boundary lint.
Start from official registry source for Base UI, pinned to the selected shadcn CLI and identifiable registry version or digest. Install only needed components. Keep their compound APIs, semantics, data attributes, accessibility and conventions. Apply only the agreed presentation and integration customizations: theme, Tailwind classes, size/color variants, Remix imports and import paths. Preserve official markup, focus, keyboard and state mechanics; product-specific behavior stays in feature modules. Do not reduce components to unstyled Base UI wrappers. Apply the agreed shadcn safety checks to copied source. Do not restructure official components merely to satisfy the authored-code style, module-layout or 500-line rules; those rules are exempt here. Local changes must retain official component semantics and accessibility.
Create a fresh app.css as the coherent Tailwind/shadcn theme entry: Tailwind directives, shadcn theme variables, approved light/dark tokens, font loading, minimal base rules and reduced-motion support. Keep normal useful Tailwind utilities available. Do not reconstruct the legacy semantic-utility restriction system or convert shell.css declaration-for-declaration to class strings. Use the official single class-merging utility; do not port legacy-only extensions.
Verify an actual generated component uses Remix imports and the intended aliases; retain the canonical wordmark and product-specific artwork. Implement the reset and layer order for the mounted Excalidraw integration; final browser proof belongs to TASK-150.06.
Build a fresh application entry and reference-led desktop frame using only new components. Define small typed view inputs and action callbacks grounded in known product contracts. Official Excalidraw may be mounted using its vendor interface, but copying and connecting Archboard's archived canvas/session logic waits for TASK-150.07. No old shell, navigator, dialog, inspector or workbench presentation may be imported. Keep presentation examples minimal and clearly separate from real runtime state; do not build a fake backend or a second state machine.
Exit: fresh foundation and both theme configurations are implemented without legacy application styling; strict and applicable build/non-browser checks pass. The workflow inventory names behaviors still to connect. Browser verification is deferred to TASK-150.06.

3. Build the fresh canvas shell and navigation. TASK-150.04.
Depends on TASK-150.02. Astra low UI workers following Astra's composition.
Map the navigator to official Sidebar parts, Collapsible groups and Button actions, with plain stable board names, indented variants, two-line long names and a separate Scratch area. Define preview presentation to consume real scene data supplied by the later integration; do not invent canonical or persisted board content. Map pane choices to the suitable Tabs/ToggleGroup parts, action menus to DropdownMenu, supporting disclosures to Collapsible, icon help to Tooltip, and inspector actions to Button. Use Tailwind grid/flex and shared separators for header, navigator, canvas, optional inspector and workbench.
Preserve open/new/save/clear/install, both panes and their independent boards, fullscreen transfer/stop/recovery, persistence and connection state, claims/doing/take-back control, selection/binding/code-target/opening and path focus. Show implemented state only; avoid redundant status bars and empty panels.
Compose the shell from the approved reference and new shared components. Read product contracts to represent independent panes, navigation, inspection and recovery accurately, but supply typed view inputs/action callbacks rather than porting archived logic. Do not import or recreate retired header/navigation/inspector/pane CSS or markup. Product-action wiring and real data connection belong to TASK-150.07.
Exit: reference-led 1920x1080 desktop composition, one/two-pane and fullscreen presentation, navigator and inspector components are implemented with typed inputs/callbacks; strict and applicable non-browser checks pass. Runtime integration remains pending for TASK-150.07. Rendered/browser proof belongs to TASK-150.06.

4. Build fresh dialogs, forms and actionable feedback. TASK-150.03.
Depends on TASK-150.04. Astra low UI workers.
Use official Dialog and AlertDialog compositions for board open/create/save-as, confirmation, external-change recovery and library installation; Field, Input, Select or Combobox and Button for their fields and choices. Migrate opener and agent settings to the same family. Preserve exact recovery choices, validation, busy state, focus and submission behavior. Use Alert for persistent actionable notices; only use transient feedback where losing the message does not lose a recovery action.
Build official component compositions with typed values, validation/busy/error presentation and action callbacks based on known product contracts. Real validation and recovery logic is ported and connected in TASK-150.07. The old shell/Modal.tsx, ui/dialog, ui/button and their selector families were retired at quarantine; none may be copied back, wrapped or re-exported.
Exit: dialog presentation, official component interaction handling, accessible names and supplied validation/busy/error states are implemented; strict and appropriate non-browser checks pass. Product actions and recovery are integrated in TASK-150.07. Old dialog/form selectors have no consumers. Actual browser workflow verification waits for TASK-150.06.

5. Build the fresh assistant-ui text and voice workbench. TASK-150.05.
Depends on TASK-150.03. Astra low UI workers.
Use shared shadcn controls for composer, queue operations, approval choices, thread/coordinator/account settings, voice controls and disclosure. Select official Field/Textarea/InputGroup/Select/Checkbox/RadioGroup components as the real input contracts require. Use official assistant-ui thread/message/content/composer components where they fit the actual workflows, built on the shared shadcn Base UI controls and theme. Install fresh upstream source rather than porting old workbench presentation. Keep assistant-ui's supported primitives for necessary product-specific composition; do not reinvent components available upstream or add a second authoritative product runtime.
Keep product-specific projections for messages, approvals, queue, captured context, transcript and lifecycle. Preserve the one private app-server session, explicit thread link, separate workhorse/coordinator and visible authority/recovery. Keep live voice mute/stop reachable when collapsed and in fullscreen.
Build new workbench/transcript/queue/approval/voice presentation with typed inputs/action callbacks grounded in actual message, approval, queue and lifecycle contracts. Copying and connecting archived implementations waits for TASK-150.07. Do not copy old frame/composer/control JSX or styles; do not expose a feature-flagged legacy view.
Exit: composer, queue, approvals, connection/recovery, context/transcript, mute and fullscreen-stop presentation is implemented with strict and appropriate non-browser checks passing. The complete presentation is ready for TASK-150.07 porting and integration; no working end-to-end product claim is made. Controlled text/voice browser execution and rendered proof wait for TASK-150.06.

6. Port UI product logic and integrate the rebuilt presentation. TASK-150.07.
Depends on TASK-150.05. Visible gpt-6-astra tasks with low reasoning perform all copying, repair, integration and non-browser verification.
Only now copy required product logic from local legacy/ into named src/ui modules. Fix it there under the already-enforced full applicable lint/type policy. Preserve board/session synchronization, persistence, independent panes, navigation, selection/binding, previews, recovery, queue, approvals, private app-server communication and voice/fullscreen lifecycle as required. Retain useful logic tests and existing authoritative server/shared contracts. No import may point into legacy/, and its contents never enter a commit.
Connect the new presentation to real data and actions. For the workbench, copy and repair the necessary assistant-ui external-store integration after TASK-150.05, using the existing Codex-owned state and transport. Revalidate current custom composer and headless-only restrictions rather than assuming they remain necessary. Remove temporary presentation examples and no-op callbacks from the runtime. Do not copy old JSX, selectors, styling tokens, visual wrappers or layout-specific state. Do not add a second agent runtime or synthetic product state.
Reading actual contracts during design reduces late integration surprises, but implementation integration may expose missing states or mismatched view interfaces. The orchestrating Astra agent decides required corrections case by case, preserving real product capabilities, accessibility and the approved reference rather than restoring the old layout.
Every copied file immediately receives full applicable strict lint/type checks, with only the previously agreed general exceptions. Use focused non-browser checks for product logic. Do not run browser tests yet, and do not rewrite them without the orchestrator's case-by-case approval.
Exit: all required product workflows are implemented and the integrated rebuild is reported ready; strict lint/type and applicable non-browser checks pass. Browser and complete product verification remain pending for TASK-150.06.

7. Complete workflow verification and final legacy cleanup. TASK-150.06.
Starts only after TASK-150.01 through TASK-150.05, TASK-150.07 and their execution leaves report the rebuild and integration ready. Astra low tasks execute verification, with one serial browser owner. The orchestrating Astra agent owns case-by-case test-replacement decisions.
Verify src/ui/shell/shell.css and its import remain deleted since quarantine. Remove any accidental old selectors, token bridges, unused theme aliases, compatibility wrappers, duplicate icons/controls, legacy-only merge configuration and tests that merely pin those implementation details. Delete the unchanged, reproducible local archive when it is no longer needed and remove its temporary lint/TypeScript exclusions. Keep the Git ignore and prohibition on tracked archive contents. Retain behavior and visual outcome checks with the new implementation. Final application CSS consists of the new Tailwind/shadcn entry, not a second shell stylesheet under another name.
Explicit custom inventory: the model-output audio/state adapter for the official LiveKit wave renderer; board preview geometry; connected-path scene overlay; product state projections; canvas mounting and fullscreen coordination. Implement their visual composition with Tailwind and shared controls. Runtime dimensions may use tightly scoped CSS variables or inline geometry. None warrants a separate application design system.
Excalidraw keeps its vendor CSS. Prefer its public view/presentation APIs. If viewport sizing or fullscreen behavior requires a vendor-selector adapter, isolate only those necessary vendor rules with the canvas integration and prove their necessity; no Archboard buttons, dialogs, theme bridges or shell layout may enter that adapter.
Add cheap source/CSS-import enforcement rejecting shell.css, legacy app CSS imports, old selector families and duplicate application icon sources. Inspect compiled CSS and actual component consumers as well as source filenames, so renaming the old stylesheet cannot pass. A structural check cannot prove registry fidelity or visual quality; those also require rendered evidence and final review.
Before browser execution, inspect the existing browser tests against the rebuilt workflows and accepted product contracts. Apply the case-by-case orchestrator approval policy above to proposed replacements; do not mechanically port old selector or markup assumptions. Once approved test changes are in place, run bun run fmt:check, strict lint/type checks, affected module contracts and the complete bun run check through the existing serial browser lane. Diagnose failures as product defects or test defects; the orchestrator approves test replacements, while actual product defects are fixed in the UI. Rerun affected browser owners within this verification phase and pass the complete gate before reporting complete. Capture light/dark, one/two-pane/fullscreen evidence. Verify keyboard-only use, labels/roles/announcements, visible focus and contrast, Escape/portal stacking/focus return, reduced motion, forced colors and accessible control targets. Cover actual loading/success/empty/partial/failure/recovery states via existing owners rather than an artificial Cartesian matrix. Preserve Excalidraw drawing, text editing, pan/zoom, selection, library, metadata/binding round-trip and note persistence. Distinguish automated accessibility evidence from an actual screen-reader smoke; do not claim the latter from DOM roles.
Exit: no parallel legacy styling system, complete strict baseline still green, rendered capabilities/accessibility proven, and every implementation subtask reports complete with evidence.

Final review and handoff
Only after all seven milestones and their execution leaves report complete, fix TARGET and start one independent gpt-6-astra reviewer with medium reasoning over BASE..TARGET, the accepted plan and repository rules. No per-subtask or interim reviewer loop. Strict checks and focused non-browser tests happen throughout. Browser tests run only in complete workflow verification after the rebuild is reported ready. The orchestrator's case-by-case approval of test replacements is distinct from this final independent review.
Reject any weakened lint/type rules, uncovered TypeScript, retained legacy styling, disguised shadcn wrappers, lost capabilities or unverified UI claims. Route valid findings to the original Astra low owners, rerun affected checks, and reuse the same reviewer on the complete updated range until clean.
Every worker prompt includes exact checkout, model/reasoning, role, owned/protected paths, dependencies, required deletions, evidence, and a required callback to the parent task with BASE/HEAD, completion, changed scope, validation, risk and next parent action. One owner edits shared config/theme/styles at a time; use separate worktrees only for concrete overlapping work. Astra handles architecture and visual decisions. All mechanical edits, conversions, test maintenance, commands and grunt work use Astra low visible tasks except the explicitly assigned TASK-150.01.02 active-source lint repair, which uses Daybreak low. Each worker reconstructs any needed untracked legacy reference locally from BASE; never commit or depend on another checkout's archive.
After clean final review, reconcile acceptance criteria and stale/duplicate tasks through Backlog CLI, report commands and remaining genuine limitations, and leave reproducible screenshots/build output ignored. Do not publish this private repo.

Evidence sources
Current working-tree inventory dated 2026-09-05; TASK-143.08.01 historical analysis-orchestration incident, with user correction that Oxlint was not the cause; TASK-140 reference; TASK-144 adoption; TASK-149/149.01 current work.
Official shadcn CLI: https://ui.shadcn.com/docs/cli
Official shadcn registry/path configuration: https://ui.shadcn.com/docs/components-json
Official Oxlint type-aware execution: https://oxc.rs/docs/guide/usage/linter/type-aware
These sources establish available mechanisms; the milestone requires proof using the pinned installed versions.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
User clarification: model and thread requirements apply to future execution only. Continue planning in the current Astra task without further threads or subagents. Strict-lint milestone reopened for discussion; final shared understanding is not confirmed. Directly requested aesthetic-guide deletion committed in 8c6e78fe; self-contained reference policy correction in 55f7d2d2. Implementation has not started.

Rule-policy decision: audit every applicable rule, including the full catalogue rather than just a strict typed preset. Local suppressions remain undecided. Sequential browser-owner execution is an existing repository workflow, not a newly requested requirement or an approved lint exception.

User-approved temporary exception: enable no-await-in-loop globally, but allow explicit rule-specific suppressions scoped only to the awaits that implement sequential browser test execution and cleanup in runSelection in tests/system/browser/run-browser-lane.ts. Each suppression must explain the existing runner constraint. Do not disable the rule for the file, browser directory, tests generally, unrelated loops, or other sequential operations. Replacing the browser harness with direct Playwright fixtures and reconsidering concurrency is deferred outside TASK-150; this exception must not trigger a harness redesign during strict-lint restoration.

Verified, untouched vendor-generated TypeScript declarations retain compiler and type-aware safety checks but are exempt from authored-code style, module-layout and file-length rules. This is a specific user-approved exception, not a blanket generated-directory ignore. The generator and authored adapters remain under the full policy. Do not rewrite upstream declaration layout merely to satisfy the 500-line limit. The possible shadcn component exemption is still under discussion.

User correction: the historical OOM came from overengineered parallel compiler/test orchestration, not an established Oxlint defect. Restore ordinary Oxlint type-aware linting. Do not create an alternate-engine investigation, compiler fan-out, per-slice analysis framework, memory-profiling project or custom analysis supervisor for this work.

User-approved shadcn policy: shared official shadcn component source retains strict compiler checks, type-aware safety, React correctness and accessibility lint. It is exempt from Archboard-authored code-style, module-layout and file-length rules. This is not an all-lint ignore. Archboard-authored feature compositions, adapters, generators and tests receive the full applicable ruleset. Do not place product-specific behavior in the exempt component source to evade rules.

User-approved shadcn customization boundary: allow theme changes, Tailwind classes, size and color variants, Remix imports and import-path adjustments. Preserve official markup, focus handling, keyboard behavior and state mechanics. Product-specific behavior belongs in Archboard feature modules.

User-approved visual priority: preserve the operator reference composition, fonts and color roles, while starting from shadcn control styling. Make targeted density, sizing and usability adjustments. Do not recreate the deleted guide's detailed control geometry or legacy token/utility restriction system through Tailwind overrides.

User approved the browser-generated preset b3QvqlIdU and requested continued grilling.

User selected narrowly documented exceptions for demonstrated false positives or required semantics. Additional lint suppressions may be statement-level only, for demonstrated false positives or required behavior with no clearer compliant implementation. Document the concrete reason beside the suppression and include it in final-review evidence. Migration effort, file size and achieving a passing check never justify suppression. No file-wide or broad rule/path disable. Prefer correcting the owning contract or writing clear compliant code; do not contort code solely to evade the rule. The final reviewer must assess every added exception; no interim review loop is introduced.

User withdrew dedicated-display requirements. Desktop UI and general accessibility remain in scope. Remove associated repository references as specified in the plan.

User explicitly retained the 1920×1080 design target and ruled out mobile. The application shell is desktop-only, with 1920×1080 as its explicit design and rendered-acceptance target. Mobile/phone layouts, mobile navigation, mobile breakpoints and mobile test gates are out of scope. Do not introduce them during this rework.

User chose an uncommitted local legacy/ archive and copying required product logic into src/ui for repair. This supersedes in-place presentation migration and lint repair of discarded UI. TASK-150.01 now contains enforcement/quarantine followed by Daybreak-low code repair; both are required before fresh UI. Strict baseline and complete product gate are distinguished because the old rendered frontend is retired. New presentation follows foundation -> shell/navigation -> dialogs/recovery -> workbench -> full verification. No source moves or implementation have been performed during planning.

User deferred all browser-test execution until complete workflow verification after the rebuild is thought complete. The orchestrating agent must decide and approve test replacements case by case so old browser-test practices do not dictate the new UI. This supersedes earlier per-milestone rendered/browser pass requirements; strict and non-browser checks continue.

User moved UI logic porting after TASK-150.05 and assigned it Astra low. TASK-150.07 now owns copying/repairing archived logic and integrating the finished presentation. TASK-150.01.02 is narrowed to active-source lint repair, not UI porting. Complete workflow verification now depends on TASK-150.07. This supersedes the earlier early-porting plan; implementation has not started.

User retained assistant-ui for the new workbench. Verified existing @assistant-ui/react use and current official Base UI registry support. TASK-150.05 builds fresh assistant-ui presentation; TASK-150.07 ports and repairs the product/runtime adapter. Old headless-only restrictions and custom composer workarounds require contract-based reassessment.

Execution authorized by the user through source task 01a06e76-b1e4-70d3-823d-fe0ad8dbc6a6 on 2026-09-05. Fixed BASE is 0d1706d06b21df1c72910a640dadad35cd37234a, clean isolated checkout /home/msc/.codex/worktrees/2709/archboard, branch codex/task-150-ui-rebuild. This supersedes historical planning-pending statements only; all recorded decisions and phase gates remain. Source checkout and live port-3000 server are protected. No merge, push or publication. Coordinator task 01a06fac-22b4-7830-b6df-01db727d103a owns phase release, test-change decisions and final review.
<!-- SECTION:NOTES:END -->
