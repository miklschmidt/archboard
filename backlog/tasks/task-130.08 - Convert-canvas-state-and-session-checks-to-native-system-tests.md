---
id: TASK-130.08
title: Convert canvas state and session checks to native system tests
status: In Progress
assignee:
  - '@codex'
created_date: '2026-08-28 01:04'
updated_date: '2026-08-28 22:37'
labels: []
dependencies:
  - TASK-130.01
  - TASK-086
  - TASK-130.06
references:
  - scripts/check-branch-compare.mjs
  - scripts/check-changes.mjs
  - scripts/check-doing.mjs
  - scripts/check-hot-reload.mjs
  - scripts/check-side-by-side.mjs
  - scripts/check-staleness.mjs
  - scripts/check-version.mjs
  - TASK-086
parent_task_id: TASK-130
priority: medium
type: task
ordinal: 143000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Convert the non-browser checks for branch comparison, change feeds and injection, doing, hot reload, side-by-side panes, staleness, and board versions. These tests share canvas lifecycle and session mechanics but assert distinct public state transitions.

Reuse the completed TASK-086 lifecycle only where a test owns the same canvas process. Preserve the hot-reload kept-state boundary and injection safety rather than simplifying the tests into in-process mocks.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 check-branch-compare, check-changes, check-doing, check-hot-reload, check-side-by-side, check-staleness, and check-version are replaced by typed native system tests grouped by public behavior.
- [ ] #2 Change-feed tests preserve ordering, cursor behavior, injection opt-in, loopback refusal, configured task routing, and the rule that an agent never receives its own injected drawing.
- [ ] #3 Doing and version tests preserve write-boundary narration requirements, real board versions, single stale refusal, actionable conflict output, and unchanged note bytes on refusal.
- [ ] #4 Hot-reload tests preserve kept state, deliberate canary failure, terminal and tab reporting, pane registrations, socket count, feed cursor, and the one unsaved-board exception.
- [ ] #5 Branch and side-by-side tests preserve board identity, variant routing, pane identity, operation order, exact compare results, and unchanged unrelated boards.
- [ ] #6 Owned canvas processes use the TASK-086 lifecycle; every test restores environment state and leaves no process, listener, socket, port, vault, or temporary branch on success or failure.
- [ ] #7 Every test source file is at most 500 lines and representative state-ordering, reload, stale-write, injection, and variant-routing regressions fail the native coverage before legacy deletion.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Dependencies, integration gate, and public boundary. Keep TASK-130.01 and completed TASK-086, and add TASK-130.06 as an explicit dependency. Disjoint native authoring may overlap on its exact paths after TASK-130.01/TASK-086, but task completion and reconciliation wait for TASK-130.06 so every ordinary canvas process imports the single typed tests/system/support/owned-canvas.ts lifecycle. Do not add a declaration shim, copy that helper, or change product behavior. Module-owned tests import only public src/runtime/engine root entrypoints; system tests use bin.archboard, public HTTP/WebSocket behavior, and the public dev-canvas process. TASK-130.11 owns final lane names and final suite documentation, not these seven predecessor deletions.

2. Exact module-owned mapping under src/runtime/engine/tests:
- scripts/check-changes.mjs pure semantic cases -> changes-semantics.test.ts, changes-layout.test.ts, and change-feed.test.ts.
- scripts/check-version.mjs note and diagnosis cases -> board-version-note.test.ts and board-version-conflict.test.ts.
changes-semantics.test.ts exclusively owns anonymous versus promoted metadata, the 12-pixel insignificant nudge, structural cut/reroute/add/promote classification, cosmetic suppression, and exact narration. changes-layout.test.ts exclusively owns bound-label drift, cluster leave/enter, region movement, frame bounds, whole-board panning, wholesale rearrangement, and exact named versus anonymous output. change-feed.test.ts exclusively owns human/agent origin, 30-update coalescing, explicit settlement, baseline deep copies, cursor net diffs, unreachable cursor null, cosmetic silence, and wholesale baseline adoption.
board-version-note.test.ts exclusively owns note frontmatter version 1 and increments, head-only reads, same-document byte identity, stated/remembered precedence, invalid human version preservation, and no version from current note alone. board-version-conflict.test.ts exclusively owns unchanged/behind/ahead/unknown moves, foreign-write versus revert/ahead diagnoses, exact messages and structured numbers, note-watch marks, and test cleanup of remembered versions and watches.
board-version-note.test.ts and board-version-conflict.test.ts assign every BoardState.file directly to a named path beneath that file owner's mkdtemp root. They do not call vaultPathFor or mutate ARCHBOARD_VAULT. Each owner removes its root and its remembered-version, board-store, and note-watch entries in finally. The exact five-file module command must pass with ARCHBOARD_VAULT pointed at an owned sentinel directory, and that sentinel must remain empty afterward.

3. Exact tests/system-owned mapping under tests/system/canvas-state:
- scripts/check-branch-compare.mjs -> branch-compare.test.ts and the branch half of variant-eval-contracts.test.ts.
- scripts/check-side-by-side.mjs -> side-by-side.test.ts and the side-by-side half of variant-eval-contracts.test.ts.
- scripts/check-changes.mjs injection cases -> injection.test.ts.
- scripts/check-doing.mjs -> doing-boundary.test.ts and doing-activity.test.ts.
- scripts/check-hot-reload.mjs -> hot-reload.test.ts.
- scripts/check-staleness.mjs -> staleness-source.test.ts and staleness-frontend.test.ts.
- scripts/check-version.mjs real-canvas cases -> board-version-route.test.ts and board-version-client.test.ts.
branch-compare.test.ts owns source save, copied identity, exact comparable/shared/added/removed/unchanged counts and order, redraw and independent-board negative controls, multipart stencil promotion, swept-arrow promotion, describe bytes, and unchanged unrelated source. side-by-side.test.ts owns the cold CLI trace, palette-before-draw ordering, branch save moving no pane, explicit new-pane display, two-pane capacity refusal, pane-targeted screenshot, proposal-only writes, compare output, source staying in its original pane, and the board-open negative control. variant-eval-contracts.test.ts owns only eval ids 5 and 7 grader paths and the objective expected-output references; it does not repeat branch or pane behavior.

injection.test.ts owns non-loopback refusal, opt-in switch, explicit thread routing, exact app-server protocol without jsonrpc, quiet versus loud delivery, 150 ms debounce/minimum interval behavior, self-injection refusal even with doing, one human delivery, and exact board/doing context.
injection.test.ts is the sole system owner allowed to dynamically import the public root src/runtime/engine/injection.ts, only for the literal 0.0.0.0 and 192.168.1.20 non-loopback refusal predicates that the fixed-loopback owned-canvas lifecycle cannot expose. That direct guard uses a plain owned CODEX_HOME root and no daemon listener because both starts refuse before socket inspection. It captures CODEX_HOME, LOG_FILE_PATH, ARCHBOARD_VAULT, ARCHBOARD_INJECT, ARCHBOARD_INJECT_LOUD, ARCHBOARD_INJECT_THREAD, ARCHBOARD_INJECT_DEBOUNCE_MS, and ARCHBOARD_INJECT_MIN_INTERVAL_MS; sets LOG_FILE_PATH and ARCHBOARD_VAULT beneath the owned root; performs the first import inside registered try/finally; and finally calls stopInjection(), restores every value exactly, and removes the root. HOST is neither captured nor mutated because startInjection receives the two literal hosts directly. Disabled-switch, missing-thread, armed protocol, quiet/loud delivery, debounce, self-injection refusal, and one human delivery remain public HTTP behavior of sequential owned real canvases. No other system owner may import injection.ts. doing-boundary.test.ts owns the missing-doing refusal exclusively: all current write routes, blank/whitespace/paragraph limits, DOING_REQUIRED status/body, CLI global flag/help, claim reason not substituting for a write line, refusal non-mutation, human report exemption, and Save/Clear human authority. Missing-doing must not appear in TASK-130.06 public-http-refusals. doing-activity.test.ts owns pane broadcasts, board/by/kind fields, other-board notification, no activity for refused writes, recent five oldest-first, repeated-line collapse, late-pane replay, and the exact absence of doing data from element/note bytes.

hot-reload.test.ts owns source-save silence, stale indication/remedy, explicit reload in the same PID, port/socket/pane/unsaved element/feed id/cursor preservation, exactly-once post-reload broadcast, the one unsaved-board canary exception, deliberate scratch-store loss, exact terminal and tab failure reports, and plain start non-reloadability. staleness-source.test.ts owns evaluated time/newest source, quiet status, touched-source loud status, unchanged PID, JSON/stderr wording, restart remedy and pane cost, with exact mtime restoration. staleness-frontend.test.ts owns served bundle identity and stale/current/Vite/unnamed pane registration responses. board-version-route.test.ts owns returned fingerprint/version/hash, stated precondition acceptance, stale refusal with unchanged bytes and current document/version, invalid expectation usage, no-note null version, non-specialized route coverage, and CLI exit 5 versus usage exit 2. board-version-client.test.ts owns claim-seeded expectations, told-once stale recovery, long-lived client memory, stated override, human non-refusal, and foreign-note hash refusal with exact untouched bytes.
board-version-client.test.ts is the sole system owner allowed to dynamically import the public root src/runtime/engine/canvas-client.ts. It first starts its owned real canvas, captures the prior test-process environment, sets EXPRESS_SERVER_URL to canvas.base, ENABLE_CANVAS_SYNC=true, EXCALIDRAW_NO_AUTOSTART=1, ARCHBOARD_VAULT to its owned vault, and LOG_FILE_PATH to an owned temporary file, then performs the first dynamic import inside a registered try/finally. In finally it calls forgetVersionsSeen(), setExpectedVersion(null), setRequestedBoard(null), and setWriteDoing(null), restores every captured environment value exactly, awaits canvas disposal, and removes the owned root. It runs this long-lived sequence serially, may not deep-import implementation or mock the server, and may not share canvas-client state with another owner.

4. Same-owner support and lifecycle rules. Add tests/system/canvas-state/support/http.ts for typed request/response capture, doing query attachment, liveness checks, and bounded condition polling; pane-session.ts for typed pane registration and event capture; injection-daemon.ts for the exact Unix-socket protocol double; and reversible-checkout-edit.ts for byte/mtime snapshots, exact restore, and unchanged git-status proof. These are mechanics only: statuses, bodies, event order, messages, bytes, and timing margins stay in owning tests. Ordinary process tests import TASK-130.06 owned-canvas.ts directly. hot-reload.test.ts is the sole exception because bun --hot is the behavior under test: its test-local typed process owns the exact dev-canvas command, PID/health verification, stderr, bounded termination, and restoration; it is not exported or reused as a second general lifecycle. Every test, support, and authored TS fixture remains at or below 500 physical lines. No general state/session framework or mutable shared fixture exists.
InjectionDaemon.dispose is idempotent, clears every retained notification timer, terminates or closes every retained client, awaits the WebSocketServer close callback and the HTTP Server close callback, then removes its exact owned root. Armed HTTP tests register daemon disposal before canvas startup and dispose the canvas before the daemon.
reversible-checkout-edit.ts snapshots exact bytes and mtimeNs from statSync(path, { bigint: true }); it does not snapshot or restore atime, which is changed by ordinary reads on the pinned ext4 relatime checkout and is not a protected contract. On restore, write the snapshot bytes, verify exact bytes, invoke spawnSync("touch", ["-m", "-d", "@<seconds>.<nine-digit-nanoseconds>", "--", path]) for each file, require status 0 with stderr in any failure, verify statSync(path, { bigint: true }).mtimeNs exactly, verify checkout status is unchanged, and only then mark the edit restored. hot-reload.test.ts independently captures each of the three source SHA-256 hashes and bigint mtimeNs values before acquiring reversibleCheckoutEdit. Its one registered deferred cleanup always calls edit.restore() and then compares those independent hashes and mtimeNs values, so the audit runs on success and failure. No atime guarantee is made.

5. Protected contracts. Preserve exact compare counts and narration order; source and proposal note bytes; two-pane identity and operation order; change-feed origin, cursor, baseline, and injection routing; missing-doing statuses/bodies and recent-five order; the hot reload 300 ms shortened settle window and every production duration imported from src/shared/timing/timing.ts; condition-based reload/staleness waits with named margins; source bytes and mtimes; all version/hash/frontmatter bytes and conflict output. No fixed wait becomes a product timing claim. The hot test runs alone, never alongside another test or a formatter/type checker, because it temporarily edits src/server.ts, src/runtime/engine/board-store.ts, and src/server/canvas/lib/application.ts and restores captured bytes in finally.

6. Red and parity proof. Keep all seven legacy scripts while native files are authored. In disposable checkouts, prove old/new failure for: redraw a proposal instead of branching; repoint the source pane; reorder or drop a feed event; permit missing doing; inject an agent's own change; clear one kept value on reload; suppress the deliberate canary; hide a stale source or stale bundle; accept a stale board version; and write after a refused precondition. Capture exact stdout/stderr/status/body/note and event-order parity for representative success and refusal cases. Run the hot deliberate-regression proof only in its disposable checkout and verify source bytes, mtimes, process, socket, state dir, and vault are restored before deletion.

7. Serialized package, eval, and deletion cutover. Once parity is recorded, the reconciliation owner performs one integration:
Before atomic cutover, every behavioral owner except variant-eval-contracts.test.ts must pass. Author variant-eval-contracts.test.ts with final-only assertions for eval ids 5 and 7: exact graded_by paths tests/system/canvas-state/branch-compare.test.ts and tests/system/canvas-state/side-by-side.test.ts, unchanged files: [], and the exact native-path references in expected_output. Before changing the cutover seam, capture the exact bytes of skills/archboard/evals/evals.json and src/runtime/board-inspection/tests/completion-contract.test.ts. In the first uncommitted cutover slice, update evals.json as already approved; update completion-contract.test.ts only by replacing eval 8's two legacy file expectations with the two native paths and removing its transitional direct eval 5/7 assertions, which now belong exclusively to variant-eval-contracts. Preserve its eval 8 graded_by/own-file assertion and every inspection fixture/hash/compare assertion. Before deleting any legacy script or editing package.json, run bun test tests/system/canvas-state/variant-eval-contracts.test.ts, then bun test src/runtime/board-inspection/tests/completion-contract.test.ts. If either fails, restore both captured files byte-for-byte, verify their hashes, leave package.json and all legacy scripts unchanged, and stop for plan review. If both pass, perform the approved package mapping and seven legacy deletions, then run the exact Step 8 final lanes.
- Map package.json test:changes exactly to bun test src/runtime/engine/tests/changes-semantics.test.ts src/runtime/engine/tests/changes-layout.test.ts src/runtime/engine/tests/change-feed.test.ts && bun test tests/system/canvas-state/injection.test.ts.
- Map test:doing to bun test tests/system/canvas-state/doing-boundary.test.ts tests/system/canvas-state/doing-activity.test.ts.
- Map test:branch to bun test tests/system/canvas-state/branch-compare.test.ts tests/system/canvas-state/variant-eval-contracts.test.ts.
- Map test:side-by-side to bun test tests/system/canvas-state/side-by-side.test.ts.
- Map test:staleness to bun test tests/system/canvas-state/staleness-source.test.ts tests/system/canvas-state/staleness-frontend.test.ts.
- Map test:hot to bun test tests/system/canvas-state/hot-reload.test.ts.
- Map test:version exactly to bun test src/runtime/engine/tests/board-version-note.test.ts src/runtime/engine/tests/board-version-conflict.test.ts tests/system/canvas-state/board-version-route.test.ts && bun test tests/system/canvas-state/board-version-client.test.ts.
- Update skills/archboard/evals/evals.json after TASK-130.05: replace ids 5 and 7 graded_by paths and their expected-output references with branch-compare.test.ts and side-by-side.test.ts; replace those two paths in eval 8 files while retaining TASK-130.05 completion-contract.test.ts.
- Delete scripts/check-branch-compare.mjs, check-changes.mjs, check-doing.mjs, check-hot-reload.mjs, check-side-by-side.mjs, check-staleness.mjs, and check-version.mjs.
Do not land native files before this atomic mapping/deletion/eval cutover. The real TASK-130.02 inventory must remain green and reach every new file exactly once across the isolated package commands.

8. Exact focused validation, in this order:
bun test src/runtime/engine/tests/changes-semantics.test.ts src/runtime/engine/tests/changes-layout.test.ts src/runtime/engine/tests/change-feed.test.ts src/runtime/engine/tests/board-version-note.test.ts src/runtime/engine/tests/board-version-conflict.test.ts
bun test tests/system/canvas-state/doing-boundary.test.ts tests/system/canvas-state/doing-activity.test.ts tests/system/canvas-state/branch-compare.test.ts tests/system/canvas-state/variant-eval-contracts.test.ts tests/system/canvas-state/side-by-side.test.ts tests/system/canvas-state/staleness-source.test.ts tests/system/canvas-state/staleness-frontend.test.ts tests/system/canvas-state/board-version-route.test.ts
bun test tests/system/canvas-state/injection.test.ts
bun test tests/system/canvas-state/board-version-client.test.ts
bun test tests/system/canvas-state/hot-reload.test.ts
Run the hot command alone only after the preceding command exits, then verify git status and source byte/mtime restoration. Next run bun run type-check, bun run lint, bun run fmt:check, bun run check, and git diff --check sequentially. Eventual categories are module for src/runtime/engine/tests and system for tests/system/canvas-state; TASK-130.11 owns final package names.

9. Overlap and merge boundary. Exact native filenames are disjoint from TASK-130.03/.04/.05/.09/.10 even where src/runtime/engine/tests is a shared directory. src/runtime/board-inspection/tests/completion-contract.test.ts is the sole serialized predecessor-test overlap with TASK-130.05; its inspection behavior remains protected. tests/system/support/owned-canvas.ts is read-only here and owned by TASK-130.06. package.json overlaps every predecessor; skills/archboard/evals/evals.json overlaps TASK-130.05; both are edited only during serialized reconciliation. Author .08 and .09 in parallel only on their disjoint native paths after TASK-130.01/TASK-086; because TASK-130.06 is an explicit dependency of both, neither task completes or enters reconciliation until .06 is integrated, then integrate .08 and .09 one at a time with full validation. TASK-130.10 waits for .06/.08/.09, and TASK-130.11 is last.

10. Fixed-range review remediation. Keep package, eval, deletion, product, and dependency scope unchanged. Replace board-version-route product helpers with owner-local SHA-256 and literal frontmatter parsing. Make startHotCanvas dispose its fully constructed cleanup handle before rethrowing any readiness error and prove that failure path directly within the existing hot owner. Make reversibleCheckoutEdit restore and verify every snapshot best-effort, aggregate per-file and checkout-status failures, and mark restored only after total success. Extend doing-boundary with a claimed-board missing-doing refusal that preserves exact public elements and note bytes. Run each changed owner alone, hot independent success/failure cleanup and byte/mtime audits, exact Step 8 non-browser lanes, inventory, type-check, lint, fmt:check, and git diff --check. Do not launch a full or browser check until parent reconciliation authorizes it.

11. Second fixed-range rereview remediation. In change-feed.test.ts capture ARCHBOARD_SETTLE_MS before setting the owner value and restore the exact prior string or absence in afterAll; prove the exact five-module process leaves a caller sentinel value unchanged. In hot-reload.test.ts retain the successful readiness-failure cleanup proof and add a literal dual readiness/disposal failure case by wrapping the captured real disposer, awaiting real cleanup, then throwing a retained sentinel. Assert AggregateError message, ordered error identity, cause identity, dead PID, and absent root. Keep hot-reload.test.ts below 500 formatted lines without moving behavior to another owner/helper. Run the requested non-browser focused and Step 8 lanes plus inventory and static gates; do not run full/browser.

12. Final hot cleanup remediation. In the existing plain-start block, declare optional plainPid and enter try/finally before invoking bin/canvas start. Keep status, JSON parse, argv, health, and reload-refusal assertions inside try. Always invoke bin/canvas stop with plainEnv in finally, and if parsing produced a PID assert that process is dead after stop. Use a deliberate wrong start-status expectation once to prove the finally path stops the real canvas and root cleanup still completes. Keep the formatted owner below 500 by removing only incidental setup lines. Run the requested non-browser hot and Step 8 gates; do not add a helper, owner, browser, or full run.

12a. Capacity amendment pending approval: remove the single blank separator immediately before the existing plainRoot setup, then apply the approved pre-start try/finally structure without any other compaction. Baseline is 498 formatted lines; the required optional PID declaration and post-stop dead-PID assertion add two net lines because the existing try is moved rather than added, and removing that one blank separator yields 499. No assertion, lifecycle step, helper, owner, or behavior changes.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
PLAN_APPROVAL_REQUIRED at fixed base 146353c1066c0487010712839fbcddaa8903d7ef. The approved owner map requires board-version-client.test.ts to preserve long-lived client memory across two writes, learn version 6 from one stale refusal, and let a stated version override remembered state. The legacy oracle proves those contracts by importing the public root src/runtime/engine/canvas-client.ts. Plan step 1 says system tests use bin.archboard, public HTTP/WebSocket behavior, and the public dev-canvas process; bin.archboard is a fresh process per command and cannot retain client memory. Focused review replacement wording: board-version-client.test.ts is the sole system owner allowed to dynamically import the public root src/runtime/engine/canvas-client.ts. It first starts its owned real canvas, captures the prior test-process environment, sets EXPRESS_SERVER_URL to canvas.base, ENABLE_CANVAS_SYNC=true, EXCALIDRAW_NO_AUTOSTART=1, ARCHBOARD_VAULT to its owned vault, and LOG_FILE_PATH to an owned temporary file, then performs the first dynamic import inside a registered try/finally. In finally it calls forgetVersionsSeen(), setExpectedVersion(null), setRequestedBoard(null), and setWriteDoing(null), restores every captured environment value exactly, awaits canvas disposal, and removes the owned root. It runs this long-lived sequence serially, may not deep-import implementation or mock the server, and may not share canvas-client state with another owner. Work stopped before implementing the disputed client test.

Focused plan-remediation evidence at fixed BASE/HEAD 146353c1066c0487010712839fbcddaa8903d7ef. Replaced the unsafe canvas-client seam proposal with the focused-review wording in Step 3 and appended the exact two-module-owner isolation rule to Step 2. Changed only board-version-note.test.ts and board-version-conflict.test.ts: every BoardState.file now names a path under that owner's mkdtemp root; neither file calls vaultPathFor or mutates ARCHBOARD_VAULT; registered cleanup forgets owner-prefixed remembered versions, deletes tracked board-store keys, clears note-watch state, and removes the root in finally. Reviewer red evidence before correction was 17 pass / 6 fail in the combined five-file run. Green evidence after correction: board-version-note alone 4 pass / 0 fail / 19 expectations; board-version-conflict alone 2 pass / 0 fail / 11 expectations; exact five-file command under an owned sentinel ARCHBOARD_VAULT 23 pass / 0 fail / 92 expectations. Sentinel /tmp/task-130-08-sentinel.ISkrjI contained 0 entries and was removed; final task temp-root audit found 0 owner or sentinel directories. OWNER_GLOBAL_VAULT_REFERENCES=0. Physical lines: board-version-note 123, board-version-conflict 108. git diff --check passed. No disputed system behavior, package/eval/deletion cutover, commit, push, broad check, or browser check was performed. Task remains In Progress, @codex, all 7 AC unchecked, final summary empty. Remaining risk: the corrected dynamic-import/system isolation plan still needs focused rereview before board-version-client or other disputed system authoring resumes.

PLAN_APPROVAL_REQUIRED after approved board-version authoring, fixed BASE/HEAD 146353c1066c0487010712839fbcddaa8903d7ef. Exact contradiction: injection.test.ts owns non-loopback refusal for 0.0.0.0 and a LAN address, but the approved system boundary requires an owned real canvas and tests/system/support/owned-canvas.ts sets HOST=127.0.0.1 after caller env, so /api/injection can never expose that required state. Changing owned-canvas would modify TASK-130.06's read-only interface; directly importing injection.ts would be a second unapproved system-module exception. Focused injection review replacement wording: “injection.test.ts is the sole system owner allowed to dynamically import the public root src/runtime/engine/injection.ts, only for the literal 0.0.0.0 and 192.168.1.20 non-loopback refusal predicates that the fixed-loopback owned-canvas lifecycle cannot expose. That direct guard uses a plain owned CODEX_HOME root and no daemon listener because both starts refuse before socket inspection. It captures CODEX_HOME, LOG_FILE_PATH, ARCHBOARD_VAULT, ARCHBOARD_INJECT, ARCHBOARD_INJECT_LOUD, ARCHBOARD_INJECT_THREAD, ARCHBOARD_INJECT_DEBOUNCE_MS, and ARCHBOARD_INJECT_MIN_INTERVAL_MS; sets LOG_FILE_PATH and ARCHBOARD_VAULT beneath the owned root; performs the first import inside registered try/finally; and finally calls stopInjection(), restores every value exactly, and removes the root. HOST is neither captured nor mutated because startInjection receives the two literal hosts directly. Disabled-switch, missing-thread, armed protocol, quiet/loud delivery, debounce, self-injection refusal, and one human delivery remain public HTTP behavior of sequential owned real canvases. No other system owner may import injection.ts.” No package/eval/deletion cutover, commit, push, broad check, or browser check. Exact combined system lane remains pending until all approved owners exist.

Injection seam plan remediation at fixed BASE/HEAD 146353c1066c0487010712839fbcddaa8903d7ef. Through Backlog CLI, Step 3 now contains the exact approved direct-guard wording; Step 4 contains the exact InjectionDaemon disposal/armed-test ordering wording; Step 7 maps test:changes and test:version to the approved process-isolated commands; Step 8 excludes injection.test.ts and board-version-client.test.ts from the combined system command and runs each alone; the real inventory must reach every new file exactly once across isolated package commands. Source repair was limited to tests/system/canvas-state/support/injection-daemon.ts: it now retains notification timers and connected clients, deletes timers as they fire, clears pending timers on disposal, terminates retained clients, reuses one disposal promise, awaits both WebSocketServer and HTTP Server close callbacks with all-settled completion, then removes only its exact root. Lifecycle red audit of the pre-repair source: timers were unretained, clients were only read from sockets.clients at disposal, disposal was not idempotent, WebSocketServer close was not awaited, and root deletion followed only the HTTP close callback. Green focused support probe used a real Unix-socket WebSocket connection, initialized while the notification timer was pending, called dispose concurrently and again after completion, and reported {idempotent:true,clientClosed:true,rootRemoved:true,retainedRootDescriptors:0}. Targeted standalone TypeScript for injection-daemon passed; targeted oxlint passed; targeted oxfmt check passed; git diff --check passed. Full project tsc was attempted and is presently blocked only by pre-existing errors in the other uncommitted TASK-130.08 module tests; it reported no injection-daemon error. injection-daemon.ts is 115 physical lines. The probe-owned root was removed; older pre-existing /tmp/ab-* roots dated before this turn were identified as unrelated and left untouched. package.json and injection.test.ts remain unchanged/uncreated; no package/eval/deletion cutover, injection behavior authoring, commit, push, broad suite, or browser run occurred. Task remains In Progress, assigned @codex, all seven acceptance criteria unchecked, final summary empty. Remaining risk: focused rereview must approve the corrected injection seam and daemon lifecycle before injection.test.ts authoring resumes.

Approved injection authoring checkpoint at fixed BASE/HEAD 146353c1066c0487010712839fbcddaa8903d7ef. Added tests/system/canvas-state/injection.test.ts at 376 physical lines. Direct guard is the sole dynamic public-root injection.ts import, captures the eight approved environment keys before first import, uses a plain owned root and no daemon, passes literal 0.0.0.0 and 192.168.1.20 hosts, asserts both exact refusal messages, calls stopInjection, restores every value exactly, and removes the root in finally. Public sequential owned canvases prove the disabled switch and exact probe refusal, enabled missing-thread refusal, armed pinned route, real Unix-socket initialize and no-jsonrpc protocol, quiet thread/inject_items developer input, loud turn/steer expectedTurnId, 150 ms debounce/minimum interval configuration, self-injection refusal after an agent write with doing, and one debounced human delivery with board, no-request, and doing context. The armed test starts and registers InjectionDaemon before the canvas, so LIFO disposal awaits canvas before daemon. TDD red: a deliberately wrong 0.0.0.0 refusal expected value failed 0 pass/1 fail with the exact received message. A first public attempt also rejected a timing-dependent two-event pending assertion; the corrected test matches the legacy one-human-delivery contract. Green: bun test tests/system/canvas-state/injection.test.ts passed 4/0 with 41 expectations. Final audit found zero task-owned injection roots and zero src/server.ts processes; git diff --check passed. One exact timeout-owned root from the failed run was removed with Bun after verification. No package/eval/deletion cutover, commit, push, broad suite, or browser run occurred.

PLAN_APPROVAL_REQUIRED at fixed BASE/HEAD 146353c1066c0487010712839fbcddaa8903d7ef after approved injection and doing authoring. Exact contradiction discovered before branch/eval mutation: Step 3 makes variant-eval-contracts.test.ts the owner of exact eval 5 and 7 grader paths, and Step 7 says those paths change from scripts/check-branch-compare.mjs and scripts/check-side-by-side.mjs to the native test paths only during the atomic eval/package/deletion cutover. The latest parent instruction keeps that cutover paused until every owner and exact focused lane is green. A strict variant-eval-contracts test cannot pass before cutover while enforcing the final native paths; accepting both legacy and native paths would weaken the exact mapping contract; changing its assertions during cutover would add an unrecorded test-file mutation. Stopped before branch-compare.test.ts, side-by-side.test.ts, or variant-eval-contracts.test.ts authoring. Smallest proposed Backlog amendment: add to Step 6/7, “Before atomic cutover, every behavioral owner except variant-eval-contracts.test.ts must pass. Author variant-eval-contracts.test.ts with the final exact native grader-path assertions but first run it immediately after the eval JSON mapping inside the uncommitted atomic cutover, before legacy deletion; then complete deletions and run the exact Step 8 final lanes.” This preserves strict final path enforcement and keeps the cutover coherent without an accept-both transition state. Completed evidence remains: injection alone 4/0/41; doing exact two-file lane 2/0/56; exact five module files 23/0/92 under empty owned sentinel; full type-check green; targeted oxlint/oxfmt green; git diff --check green. Existing owner-local type/lint repairs only: guaranteed owned board.file narrowing, nullable note read, refusal Error.message capture, complete arrow binding fixtures, nullable layout frames, safe copied metadata access, and non-spread test movement copies. Lines: injection 379, doing-boundary 147, doing-activity 184, changes-semantics 197, changes-layout 162, change-feed 178, board-version-note 159, board-version-conflict 134. Cleanup audit found zero task-owned roots and zero canvas processes. Task remains In Progress, @codex, 0/7 AC checked, finalSummary empty. No package/eval/deletion cutover, commit, push, broad check, or browser run occurred.

Hot-restoration plan correction at fixed BASE/HEAD 146353c1066c0487010712839fbcddaa8903d7ef. The first isolated hot-reload behavior run passed 1/0 with 41 expectations, but the independent audit found Date-based restoration truncated sub-millisecond source mtimes. All three original hashes and nanosecond mtimes were restored and verified immediately. The rejected notes-only atime restoration proposal has been removed; the sole reviewed candidate is the exact mtime-only contract recorded in Step 4. No implementation or cutover resumed.

Implementation evidence at fixed BASE 146353c1066c0487010712839fbcddaa8903d7ef. Replaced the seven legacy canvas-state/session checks with five typed module owners, twelve typed system owners, and four canvas-state-local mechanical supports. Ordinary processes reuse tests/system/support/owned-canvas.ts; the hot process remains test-local. The only dynamic imports are the approved public-root injection and canvas-client owner seams, with exact environment capture/restoration and state cleanup. No production source or dependency changed.

TDD and parity: preserved the earlier module 17-pass/6-fail isolation red and corrected 23-pass/0-fail/92-expectation sentinel green; injection deliberate refusal red 0/1 then 4/0/41 green; hot exact-mtime repair followed the observed Date-truncation audit and now passes 1/0/41. All seven legacy scripts passed at the fixed base in a disposable checkout. Representative disposable old/new mutations killed branch identity, pane targeting, feed retention, self-injection, doing refusal, source/frontend staleness, version conflict, and reload canary reporting; each affected its legacy and native owner as recorded, with redraw/independent-board negatives also asserted directly. Disposable worktrees, roots, processes, sockets, and state were removed.

Atomic cutover: captured evals.json SHA-256 0e0a25a710c9bc0aa02023968ec02bd30c748d9ae7b2b5972acc9ae11c217995 and completion-contract.test.ts SHA-256 0a54458655312b9c2e4f62b3ca732467f7d1e254dce0b48bfb4df7c3e92840df before mutation. The first slice changed only those files; variant-eval-contracts passed 2/0/10 and completion-contract passed 3/0/6 before package mapping or deletion. Then package scripts were remapped exactly and only the seven approved legacy scripts were deleted. Final real inventory passed 11/0/11 and reaches every native test exactly once.

Final focused lanes: exact five module files under an owned sentinel 23/0/92 with the sentinel empty; combined system lane 12/0/262; injection alone 4/0/41; board-version-client alone 1/0/23; hot alone 1/0/41. Hot outer audit restored the exact three independent SHA-256 values and bigint mtimeNs values; no task-owned root/process/socket/state residue remained. type-check, lint, fmt:check, test:suites, and git diff --check passed. The one permitted serial/headless bun run check passed every static, module, system, inspection, CLI, and the first three browser lanes, including every TASK-130.08 owner. Its pre-existing test:live-session lane had one late failure: after explicit Take back control, the next agent write returned 200 instead of the expected CLAIM_REVOKED 409; this task changes no production or live-session source, and no second browser run was made. This isolated existing browser timing/state risk remains for fixed-range review.

Physical lines: changes-semantics 197, changes-layout 162, change-feed 178, board-version-note 159, board-version-conflict 134; board-version-client 206, board-version-route 176, branch-compare 348, doing-activity 184, doing-boundary 147, hot-reload 387, injection 379, side-by-side 394, staleness-frontend 92, staleness-source 92, variant-eval-contracts 35; support/http 77, injection-daemon 115, pane-session 108, reversible-checkout-edit 67. Maximum is 394, below 500. Task intentionally remains In Progress, @codex, 0/7 AC checked, final summary empty.

Fixed-range review remediation on reviewed HEAD 691e3cd50e6c1473702f92067c33a114fd13db7d. Changed only the four approved owners/support plus this Backlog record. board-version-route now has zero board.ts/board-version.ts imports, computes raw-note SHA-256 through node:crypto, and parses exactly one literal numeric version field from its owned frontmatter. doing-boundary now saves the empty payments note, claims payments with reason reworking the payment path, proves the returned claim reason, then asserts an agent write without per-write doing returns exact status 400/code DOING_REQUIRED/overall-reason wording while public elements and exact note bytes remain unchanged.

Hot startup now constructs its cleanup-capable handle before readiness, catches readiness failure, awaits idempotent disposal, and rethrows the same readiness error; a disposal failure aggregates both errors and names the disposal failure as cause. The owner-local failure proof captures the started handle, forces readiness rejection, asserts object identity for the rejection, root removal, and dead PID. reversible-checkout-edit now attempts write, byte verification, checked touch, and bigint mtime verification for every snapshot, accumulates all failures, always compares checkout status, remains unrestored after aggregate failure, supports a successful retry, and marks restored only after zero failures. Its focused failure proof blocks the first path, verifies snapshots two and three still restore exactly, verifies checkout-status drift is reported, repairs the blocked path, retries all three exact bytes/mtimeNs successfully, and calls restore idempotently once more.

TDD evidence: the initial hot owner ran 1 pass/2 fail/45 expectations because startHotCanvas ignored the forced readiness failure and reversibleCheckoutEdit returned the first EISDIR instead of an aggregate/restoring later files. The claimed-board deliberate red expected 200 and received 400, 0 pass/1 fail/10 expectations. Pre-change structural audit found the two forbidden product imports. Green owner runs: board-version-route 4/0/30; doing-boundary 1/0/33; hot 3/0/60. Independent hot audits before and after each final run matched all three source SHA-256 hashes and nanosecond mtimes exactly: server ebfb043e287f2cc6991d2ec065eb69b6fcdc4678fe68d46d8012968dbaca3d50 at 2026-08-28 22:23:21.106713096 +0200; board-store c87932b696f40356c3f5021be1ff6a11494b61620e133fa25a7530835a3c3cbe at .103595295; application ebfbaeb584b49e8ab0e6949f424f6309057dce2f4ac820a955f2e572f55c67c7 at .107165248. No hot process or task-owned root remained.

Exact Step 8 non-browser lanes: five module files 23/0/92 under an owned empty sentinel; combined system lane 12/0/269; injection alone 4/0/41; board-version-client alone 1/0/23; hot alone 3/0/60. Real inventory 11/0/11 and every native test remains reached once. type-check, lint, fmt:check, and git diff --check pass. No full/browser run was launched under the parent hold. package.json, evals.json, completion-contract.test.ts, production, dependencies, and the accepted seven-script deletion scope are byte-unchanged from reviewed HEAD. Lines: board-version-route 186, doing-boundary 183, reversible-checkout-edit 94, hot-reload 480; all remain below 500. Task remains In Progress, @codex, 0/7 AC checked, finalSummary null.

Second rereview remediation on reviewed HEAD 4cbf992ddd503616c142af25d0bd1f6c9642566e. Changed only change-feed.test.ts, hot-reload.test.ts, and this Backlog record. change-feed captures the prior ARCHBOARD_SETTLE_MS string or absence before assigning 60000. Its afterAll deletes the key when originally absent or restores the exact original string, then asserts the restored value. TDD red under ARCHBOARD_SETTLE_MS=caller-sentinel was 6 pass/1 fail/16 expectations: teardown received 60000 instead of caller-sentinel. Green with the key absent was 6/0/15. The exact five-module process under ARCHBOARD_SETTLE_MS=caller-sentinel and an owned ARCHBOARD_VAULT sentinel passed 23/0/92; the vault sentinel remained empty, and the owner teardown assertion proved the process value returned to caller-sentinel.

The hot owner keeps its successful-disposal readiness proof and adds a literal dual-failure proof. The readiness callback captures the real canvas, wraps its disposer, awaits real disposal, then throws a retained disposal Error before throwing a retained readiness Error. The proof asserts the exact AggregateError message, errors[0] readiness identity, errors[1] disposal identity, cause disposal identity, absent root, and dead PID. TDD red kept real cleanup and ran 3 pass/1 fail/61 expectations: the received exact message was Hot canvas readiness failed and disposal also failed. while the deliberate expectation was wrong aggregate message. Green non-mutating failure cases passed 2/0/10; full hot passed 4/0/66. Independent outer audits preserved exact source SHA-256 and bigint mtimes: server ebfb043e287f2cc6991d2ec065eb69b6fcdc4678fe68d46d8012968dbaca3d50 at 21.106713096, board-store c87932b696f40356c3f5021be1ff6a11494b61620e133fa25a7530835a3c3cbe at 21.103595295, application ebfbaeb584b49e8ab0e6949f424f6309057dce2f4ac820a955f2e572f55c67c7 at 21.107165248. No root or process remained.

Final exact Step 8 non-browser sequence: module 23/0/92 with empty sentinel and caller environment proof; combined system 12/0/269; injection 4/0/41; board-version-client 1/0/23; hot 4/0/66 with exact outer byte/mtime audit. Inventory 11/0/11, type-check, lint, fmt:check, and git diff --check pass. No browser/full lane ran, per reviewer instruction. hot-reload.test.ts formats to 498 lines and change-feed.test.ts to 185, both below 500. No new helper/owner exists. Production, package, dependencies, eval, completion contract, and deletion scope are unchanged from reviewed HEAD. Task remains In Progress, @codex, 0/7 AC checked, finalSummary null.

PLAN_APPROVAL_REQUIRED at reviewed HEAD 549f6ab36ff63aae5726bbaf51324b790f140a47. hot-reload.test.ts is 498 formatted lines. The first literal implementation moved the existing try before start, added the optional PID and post-stop dead-PID assertion, and attempted to compact only the two-command Git setup. oxfmt expanded that setup and produced 501 physical lines. Per the explicit stop rule, no test was run and the source edit was reverted byte-for-byte to reviewed HEAD. The minimum required cleanup itself is two net formatted lines over baseline, which reaches 500; the attempted Git compaction added one instead of saving lines. Smallest amendment is Step 12a: authorize removal of the one blank separator immediately before plainRoot, leaving the exact requested visible cleanup at 499 lines with no semantic/assertion change. No product, package, eval, completion, deletion, or other test source changed. Worktree contains only this Backlog CLI plan/evidence update. Task remains In Progress, @codex, 0/7 AC, finalSummary null.

Step 12a final hot cleanup remediation on reviewed HEAD 549f6ab36ff63aae5726bbaf51324b790f140a47. Changed only tests/system/canvas-state/hot-reload.test.ts and this Backlog record. Removed the approved blank separator, declared optional plainPid before the pre-start try, moved the existing try ahead of bin/canvas start, assigned plainPid only after successful status and JSON parsing, and made finally always run canvas stop with plainEnv. When parsing produced a PID, the same finally asserts that PID is dead after stop. No other compaction or semantic change was made; oxfmt produces exactly 499 physical lines.

TDD cleanup evidence: the deliberate pre-parse red changed only the expected start status to 1. The owner ran 3 pass/1 fail/61 expectations, failing on received status 0 before JSON parsing; afterward there were zero archboard-plain-reload roots and zero checkout canvas processes, proving stop could not be bypassed before plainPid assignment. Restoring the expected status to 0 produced the green owner. The focused non-mutating startup cleanup cases passed 2/0/10. Final full hot passed 4/0/67. Independent outer audits matched all three source SHA-256 hashes and bigint mtimes exactly before/after: server ebfb043e287f2cc6991d2ec065eb69b6fcdc4678fe68d46d8012968dbaca3d50 at 2026-08-28 22:23:21.106713096 +0200; board-store c87932b696f40356c3f5021be1ff6a11494b61620e133fa25a7530835a3c3cbe at .103595295; application ebfbaeb584b49e8ab0e6949f424f6309057dce2f4ac820a955f2e572f55c67c7 at .107165248. No new hot/plain root or canvas process remained; two untouched archboard-hot roots predate the run at 2026-08-21 and are unrelated retained residue.

Exact Step 8 non-browser sequence passed: five module owners 23/0/92 under an empty owned sentinel; combined system owners 12/0/269; injection alone 4/0/41; board-version-client alone 1/0/23; hot alone 4/0/67 with exact byte/mtime audit. Real inventory passed 11/0/11 and reaches every native test once. type-check, lint, fmt:check, and git diff --check pass. The initial final type-check exposed callback narrowing of optional plainPid; the already-guarded one-line dead-PID assertion now uses a TypeScript non-null assertion with no runtime change. No browser/full lane ran. Product, dependencies, package, eval, completion contract, and deletion scope are unchanged. Task remains In Progress, @codex, 0/7 AC checked, finalSummary null.
<!-- SECTION:NOTES:END -->
