---
id: TASK-090
title: Differential-check human arrow bindings against pinned Excalidraw
status: Done
assignee:
  - '@codex'
created_date: '2026-08-21 13:36'
updated_date: '2026-08-28 04:18'
labels: []
dependencies: []
references:
  - src/runtime/engine/arrow-binding.ts
  - scripts/check-fixed-point.mjs
  - scripts/check-geometry.mjs
  - package.json
priority: high
type: task
ordinal: 90000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The original claim that nothing detects Excalidraw drift is stale. @excalidraw/excalidraw is pinned to 0.18.1, check-fixed-point renders the converted board in the real browser, text geometry is compared there, and the current fixture returns zero changes.

One important gap remains. The browser fixture covers agent-created centered bindings, while a person can rebind an arrow end with nonzero focus and gap. That stored binding is later interpreted by arrow-binding.ts when an agent moves the attached node. Add a small browser differential for that ordinary collaboration path. Keep the current port unless the differential exposes a visible mismatch. Do not vendor Excalidraw internals or build source-map extraction machinery for a discrepancy the browser cannot show.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The pinned Excalidraw version and the decision to keep the local arrow-binding port are recorded with the browser differential that guards the decision.
- [x] #2 A real browser creates or adopts an arrow end with nonzero focus and gap, an agent moves the bound node, and the check compares the server result with the endpoint Excalidraw settles on.
- [x] #3 The fixture includes the existing human-shaped case of focus 0.9 and gap 15 on a normal architecture node, with a stated visible tolerance and actionable mismatch output.
- [x] #4 The upgrade note names the focused geometry and fixed-point checks to run when @excalidraw/excalidraw changes.
- [x] #5 No vendored Excalidraw geometry, source-map extractor, or second binding implementation is added unless the differential first proves the current implementation visibly wrong.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Replace the current post-PUT read-back in scripts/check-fixed-point.mjs. Keep the adopted focus 0.9, gap 15 fixture in the fixed-point board, and copy its exact pre-move node geometry, two arrow endpoints, and binding into a small server-only comparison board. Assert the browser fixture and server-only fixture start with identical node geometry, arrow start and end points, and binding values.
2. Reuse the trusted interaction already exercised by scripts/check-human-edit-performance.mjs: frame human-node through POST /api/viewport, derive its screen center from the real Excalidraw app state, and drag that center with agent-browser mouse input. Install a focused window.fetch gate for the one POST /api/elements/changes report before the drag. The gate holds the request itself, so neither a server write, a notification delta, nor a correction response can supply a rerouted arrow before the oracle scene is read. Do not call updateScene with elements and do not send the arrow through an Archboard element API during this browser move.
3. Immediately after mouse-up, read the real Excalidraw scene and capture the browser-moved node geometry plus both absolute arrow endpoints. Prove the targeted node moved, focus 0.9 and gap 15 survived, the unbound arrow start stayed at its pre-move point, and the bound end changed. Then release the held human report and restore window.fetch so the existing fixed-point workflow can settle normally.
4. Move only the node on the server-only board through the agent PUT route, using the exact x and y captured from the browser drag. Before comparing endpoints, assert that both paths had the same pre-move node rectangle and arrow endpoints, and now have the same target node x, y, width, height, and angle plus the same unbound arrow start and binding. This makes the bound endpoint the only value under comparison. Compare the independently computed browser endpoint with the server endpoint at the approved 1.0 scene-pixel Euclidean tolerance. Do not import or call src/runtime/engine/arrow-binding.ts from the browser oracle.
5. Use one focused endpoint-comparison function for the real assertion and a pure negative control. Feed that same function a deliberately wrong server point two scene pixels to the right of the captured browser point and assert that it fails the 1.0 pixel gate. The negative control performs no API call, scene update, or note write.
6. Correct docs/agents/test-suite.md to say that trusted browser pointer input computes the oracle while its report is held, and that a separate server-only agent move from identical geometry is the value compared. Retain the Excalidraw 0.18.1 pin, the 1.0 pixel policy, mismatch evidence, and the test:geometry then test:browser upgrade sequence.
7. Keep src/runtime/engine/arrow-binding.ts and scripts/check-geometry.mjs unchanged. Validate with bun run type-check and bun run test:geometry, then request the serialized browser lane for bun run test:browser. Commit only scripts/check-fixed-point.mjs, docs/agents/test-suite.md, and the TASK-090 Backlog update. Leave TASK-090 In Progress with acceptance criteria unchecked for rereview.

8. Correct only the fixture setup in scripts/check-fixed-point.mjs. After POST /api/viewport returns 200, mirror the proven check-human-edit-performance sequence by allowing the animated scrollToContent call to settle for 650 ms before reading Excalidraw app state. Keep the existing scene-to-screen calculation because it already matches pointOf: screenX = (sceneX + scrollX) * zoom + offsetLeft and screenY = (sceneY + scrollY) * zoom + offsetTop. Return the actual .excalidraw DOM rect with the point, and make the wait predicate require the drag start and its 40 by 30 pixel destination to fall inside that rect. Add a pre-drag check with the point, destination, rect, zoom, and framing response as failure evidence. Do not install the fetch gate or issue pointer input unless that check passes. Make no production, geometry-check, UI, or documentation change unless the resulting browser evidence requires another approved plan.

9. Approved amendment to step 8: do not add the legacy 650 ms sleep or any timing constant. The existing waitFor poll directly observes settlement and returns only when the start and destination fit inside the live .excalidraw rectangle. The formula, pre-drag diagnostic, protected scope, and validation remain as recorded.

10. Proposed production correction for approval: extend the Bindable shape input in src/runtime/engine/arrow-binding.ts with the existing roundness record and replace only the rounded-rectangle branch of crossings. Keep determineFocusPoint, rotation, ellipse, diamond, and square-rectangle behavior unchanged. For a rounded rectangle, use the pinned Excalidraw 0.18.1 corner radius rules, offset each corner by gap times the normalized vector from the center to the corner already displaced outward by gap, connect the shifted curve endpoints with the existing segment intersection, and intersect the ray with the four cubic corner curves using B(t) = (1-t)^3 P0 + 3(1-t)^2 t P1 + 3(1-t) t^2 P2 + t^3 P3 for t in [0,1]. Keep the nearest forward intersection, matching updateBoundPoint. Do not port diamond roundness or unrelated binding behavior without a measured failing case.

11. Add a focused arithmetic regression to scripts/check-geometry.mjs for the captured rectangle x=1066.8104451025551, y=1060.7409025475235, width=200, height=100, angle=0, roundness type 3, arrow start (1400,1120), focus 0.9, and gap 15. Assert endpoint (1279.2940245092134,1150.128871410794) within a small numerical tolerance. It is red now at (1281.8104451025551,1149.5007646804281), separation 2.5936250051473406. Add one nearby focus 0.8 check at (1279.8589442886187,1144.108983723157), red by 1.9904058707683054, and retain the existing unrounded focus 0 check as the unchanged control. Preserve the browser differential and its two-pixel negative control without modification.

12. Validate the production change with bun run type-check and bun run test:geometry. For browser validation in this dot-prefixed worktree, keep TASK-090 review range separate, cherry-pick the reviewed TASK-131 prerequisite 93865a27 only in the validation checkout, run bun run build, then run bun run test:browser alone. Require the genuine endpoint separation to be at most 1.0 and the negative control to remain rejected. If the seven later fixed-point failures remain after the endpoint correction, stop and plan fixture cleanup separately rather than mixing it into arrow binding.

13. Precision correction for approval: in src/runtime/engine/arrow-binding.ts, replace only the private rounded-corner scalar cubic bisection and rounded-side ray algebra with the pinned Excalidraw 0.18.1 finite line-segment intersection procedure. Build the same interceptor from adjacent toward focus with length distance(adjacent,current) + distance(adjacent,center) + 2 * max(width,height). For each cubic solve B(t) - (L0 + s(L1-L0)) = (0,0) by the pinned two-variable Newton step, central derivative delta 1e-6, residual limit 1e-3, 10 iterations, determinant equality check, and guesses (0.5,0), (0.2,0), (0.8,0); accept the first t and s both in [0,1]. Use the pinned finite segment containment rule for straight sides. Keep this private to rounded rectangles and do not change focus selection, roundness construction, ellipses, diamonds, fixed points, or comparison tolerances. In scripts/check-geometry.mjs, add or replace only a focused exact expected value if the pinned procedure yields a repeatable bit-identical result; otherwise rely on the existing focused numerical geometry checks and the exact browser fixed-point comparator rather than inventing rounding.
14. Fixture correction for approval: move creation of binding-differential to fixture setup before the browser is opened, when paneFromRequest resolves null and switchPaneTo cannot send board_switched. Seed it later from the adopted fixedpoint geometry as today. Add a focused assertion before the trusted drag that the sole pane still addresses fixedpoint and its scene still contains text1. No reporting or UI code changes are needed. The current first bad transition is the existing POST /api/boards/new after browser open: the sole pane is selected, board_switched applies the empty comparison scene and replaces the reporting baseline. The subsequent drag report therefore targets binding-differential. The later fixedpoint duplicate-index notification is discarded by the board-address filter before origin or merge logic, which is why persisted fixedpoint text1 exists while the browser scene lacks it. This is a fixture board-targeting error, not a stale version conflict or partial-scene merge.
15. Red-green and validation: preserve the current exact fixed-point zero-diff assertion as the precision red case and capture a focused pinned expected value only if bit-stable. The fixture red case asserts that the pane changes from fixedpoint to binding-differential immediately after the current comparison-board creation; green keeps it on fixedpoint through comparison seeding, trusted drag/report, and duplicate-index repair. Run bun run type-check, bun run test:geometry, and bun run test:reporting. Then request the serialized browser lane, use the reviewed TASK-131 prerequisite separately, run bun run build and bun run test:browser alone, and require exact fixed-point zero diff, genuine endpoint separation no more than 1.0 scene pixel, rejection of the 2 pixel negative control, and text1 index repair with the pane still on fixedpoint.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implemented the approved single-browser differential in scripts/check-fixed-point.mjs and documented the Excalidraw 0.18.1 upgrade gate in docs/agents/test-suite.md. The fixture uses the existing focus 0.9, gap 15 human binding, moves its 200 by 100 rectangle through the agent PUT route, and reports server and browser endpoints, dx, dy, Euclidean separation, binding values, and node geometry against the 1.0 scene-pixel tolerance. Non-browser validation passed: bun run type-check and bun run test:geometry with 89 checks. test:browser remains pending because TASK-096 owns the browser lane.

Independent review rejected the first browser assertion at commit 96b8cb9. The agent PUT rerouted both human-node and human-arrow on the server, notificationDelta sent both, and Excalidraw updateScene installed that already-rerouted arrow. The observed zero-pixel separation therefore did not provide an independent browser calculation and does not satisfy AC #2. No production binding code changed. The revised plan gates the browser change-report request before it reaches the server, captures the endpoint produced by trusted Excalidraw pointer input, and compares it with an agent PUT on a separate server-only board seeded from identical pre-move geometry.

Implemented the approved remediation: the fixed-point browser check now gates the human change report before the server, captures the endpoint produced by a trusted Excalidraw pointer drag, and compares it with an agent-only node move on an unopened board seeded from the same canonical geometry. The same 1.0 scene-pixel comparator rejects a purely in-memory endpoint shifted by 2 pixels. Production arrow binding, the geometry check, and UI remain unchanged. Non-browser validation passed at this revision: bun run type-check; bun run test:geometry (89 checks). test:browser is pending release of the serialized browser lane from TASK-096.

Browser diagnosis at 2887c39: check-human-edit-performance frameElement waits 650 ms after the viewport response, then pointOf reads the app state. TASK-090 read immediately and its waitFor predicate accepted any finite nonnegative point. It therefore accepted stale point (1390,1184), outside the registered 650 by 529 canvas at (290,117). The coordinate formula is already identical to the proven helper. The smallest repair is to restore the 650 ms animation wait and require the computed drag path to be inside the actual .excalidraw DOM rect before installing the report gate or moving the pointer. No browser rerun or implementation edit has been made pending approval.

Implemented the approved fixture-only correction without a sleep. The viewport poll now keeps observing Excalidraw app state until the existing 40 by 30 pixel drag fits inside the live .excalidraw DOM rectangle. A separate pre-drag assertion prints the viewport response, start, destination, rectangle, and zoom, and the fetch gate plus pointer input are conditional on that assertion. Non-browser validation passed: bun run type-check; bun run test:geometry (89 checks). Browser validation remains pending release of the serialized lane.

Production diagnosis from the pinned compiled 0.18.1 path, without reading or extracting source maps: determineFocusPoint agrees exactly. For the captured case both implementations aim at (1256.8104451025551,1155.7409025475235) from adjacent point (1400,1120), ray (-143.18955489744485,35.74090254752355). The first divergence is outline construction. The local code uses half extents a=100+15=115 and b=50+15=65, so the ray hits the sharp right slab at (1281.8104451025551,1149.5007646804281). Excalidraw gives roundness type 3 on this normal rectangle a radius of 25. Its bottom-right gap offset is (13.058442580802014,7.38085885001853), and its shifted cubic has points P0 (1279.8688876833571,1143.121761397542), P1 (1279.8688876833571,1159.7884280642088), P2 (1271.5355543500239,1168.121761397542), P3 (1254.8688876833571,1168.121761397542). Ray-cubic intersection reproduces the browser endpoint at (1279.294023437741,1150.1288774052355), within about 0.000006 pixels of the observed browser value. Nearby focus 0.8 and 0.95 separations are 1.990406 and 3.494461 pixels, so the sharp-corner assumption fails systematically as focus approaches the rounded corner. A rounded focus 0 binding at gap 15 also moves 1.943087 pixels under the pinned equation, while an unrounded focus 0 binding remains exact; at the normal gap 4 the rounded centered change is 0.450370 pixels.

The seven later browser failures are not a numerical cascade from the server-only endpoint mismatch. That comparison board is unopened and cannot remove text1 or shell DOM. They are a separate fixture cleanup or settlement problem exposed after the held and released drag mutates the visible fixedpoint board. Earlier browser runs without that genuine drag passed them; the failed run later saw text1 absent, no current-board shell result, and no activity layout. The production math plan does not fix or mask them. Browser validation must report them independently after the binding correction, and any cleanup change requires its own evidence and approval.

Red-green production correction: with only the captured rounded focus 0.9 regression and focus 0.8 neighbor added, bun run test:geometry failed 2 of 91 checks. The sharp local outline produced (1281.8104451025551,1149.5007646804281), 2.5936250051473406 px from the browser at focus 0.9, and (1281.8104451025551,1143.7173715379608), 1.9904058707683054 px from the pinned calculation at focus 0.8. The existing unrounded focus 0 control remained unchanged.

Implemented only rounded rectangle outline construction in src/runtime/engine/arrow-binding.ts. Private helpers apply roundness types 1 and 2 as a proportional radius and type 3 as the pinned adaptive radius, shift each cubic corner along its gap diagonal, re-hang the four sides, and find forward line-cubic intersections. No dependency, general geometry layer, focus selection, fixed-point, ellipse, diamond, or UI behavior changed. The older server integration assertion now reuses the same captured browser geometry because its square max-norm outline was not a valid oracle for the default rounded rectangle.

Green evidence: focus 0.9 now calculates (1279.2940234377415,1150.1288774052355), about 0.000006 px from the captured browser endpoint, and focus 0.8 calculates (1279.8589442886187,1144.108983723157). bun run type-check passed; bun run test:geometry passed all 91 checks; focused oxlint, oxfmt, and git diff --check passed. scripts/check-fixed-point.mjs and its 2 px negative control are unchanged. Browser validation remains pending the serialized lane and must use the reviewed TASK-131 prerequisite separately, enforce at most 1.0 px genuine separation, reject the 2 px control, and report any seven later fixture failures independently.

Browser validation on combined validation HEAD 3d47f71ec5de4a70afd4e8fc3f6095a804d1040b, with separate TASK-131 prerequisite 15cea29 in ancestry: bun run build passed. bun run test:browser ran alone and exited 1 with 9 checks failed.

The TASK-090 differential itself passed. The trusted drag moved human-node to x=1066.762361728694, y=1060.6926392657167. Browser endpoint was (1279.244169760344,1150.090509259871); server endpoint was (1279.2441687040762,1150.0905151585466); dx=0.0000010562678198766662, dy=-0.000005898675681237364, Euclidean separation=0.000005992501689589089, inside the 1.0 px gate. The same comparator rejected the in-memory point shifted 2 px right with separation exactly 2.

Two failures occur earlier in the fixed-point baseline and are separate from the differential: Excalidraw normalizes human-arrow width 205.17374420209694 to 205.17374420209697, height 13.152163089878059 to 13.15216308987806, and the matching endpoint offsets by the same roughly 3e-14 values. This makes one of 18 elements differ and triggers both the count and field-set checks.

The seven previously observed later assertions still fail. Their earliest direct evidence is after the deliberate duplicate-index plant: the persisted server response still contains text1, while sceneWhenStill and whatMoved report text1 as absent from the browser scene, not merely repaired to another index. The next desktop evaluator returns null, so one of its required shell/current-board nodes is absent. All five activity writes then return 200, but the visible pane never reaches five activity lines before the existing poll deadline, so its three layout assertions also return null. The later 420 px shell checks pass, which separates the TASK-131 CSS prerequisite from this visible-board/current-state failure. No fixture, production, docs, or test code changed after this evidence.

Bounded diagnosis after browser failure: the local rounded-corner root is the scalar equation cross(B(t)-origin,direction)=0. It partitions by derivative roots, admits stationary values with 1e-9 and duplicate roots with 1e-10, and bisects sign changes 60 times before projecting the cubic point back onto an infinite ray; straight sides use a separate cross-product ray calculation with 1e-12 and 1e-9 thresholds. Pinned Excalidraw 0.18.1 instead intersects each cubic with a finite segment by solving the two-coordinate equation B(t)=L0+s(L1-L0) using its central-difference Newton solve. The exact constants and ordering are delta 1e-6, residual max norm below 1e-3, 10 iterations, det === 0 refusal, and seed order (0.5,0), (0.2,0), (0.8,0), followed by t,s range checks. updateBoundPoint supplies a finite interceptor whose length is distance(adjacent,current)+distance(adjacent,center)+2*max(width,height), sorts returned intersections by squared distance to adjacent, uses the nearest when there are more than one, focus when there is exactly one, and current when there are none. No epsilon suppression or fixed-point comparator change is proposed.
The later text1 failure is now explained exactly. POST /api/boards/new for binding-differential runs after one browser pane is registered and omits pane. paneFromRequest therefore returns the solo pane; switchPaneTo sends board_switched with the new empty document. The frontend adopts binding-differential, resets reporting generation and baseline, and applyServerScene constructs the first bad scene array as []. Later comparison seeding adds only human-node and human-arrow. The trusted drag report uses boardKeyRef and therefore writes binding-differential, not fixedpoint. When the duplicate-index plant later writes fixedpoint, the socket handler returns on data.board !== boardKeyRef.current before the origin test and before mergeIncoming, so text1 cannot appear in that browser scene. fixedpoint remains persisted with text1 and its own version; no board-version precondition was supplied or refused, and no partial-scene replacement is involved. The desktop and activity nulls are downstream of the pane showing the service-level comparison board. The smallest correction is fixture-only: create the comparison board before browser open and assert pane/scene identity before dragging.

Implemented the approved exact-solver and fixture correction. src/runtime/engine/arrow-binding.ts now removes the old scalar cubic bisection entirely and uses the pinned 0.18.1 finite interceptor, exact rounded side containment, absolute-coordinate cubic construction, three-seed two-variable Newton solve, and pinned intersection count choice only for rounded rectangles. No comparator, tolerance, focus, fixed-point, ellipse, diamond, UI, server, reporting, or documentation code changed.
Red-green arithmetic evidence: before the solver replacement, the new bit-stable focus 0.9 check returned (1279.2940234377415,1150.1288774052355) twice and failed against the pinned procedure result. After replacement, the same current-point fixture returns (1279.2940245092384,1150.1288714106531) bit for bit on repeated calls. The existing browser-captured numerical check, focus 0.8 neighbor, and unrounded controls remain green.
The comparison board is now created before any pane exists and seeded later as before. A pre-drag browser assertion requires the sole pane to remain on fixedpoint with live text1, so the fixture cannot silently switch to binding-differential again. Non-browser validation passed: bun run type-check; bun run test:geometry with 92 checks; bun run test:reporting with 115 checks; focused formatting, lint on changed production and geometry files, and git diff --check. Browser build and test remain gated on explicit serialized-lane release.

Independent rereview remediation: changed the negative-control assertion from a boolean literal comparison to direct negation so the enabled no-unnecessary-boolean-literal-compare rule passes. The separation===2 condition is unchanged, so the assertion is not weakened. The reviewer withdrew the rounded-corner-cutout finding as pre-existing and outside TASK-090; arrow-binding.ts and check-geometry.mjs remain untouched. Validation passed: bun run lint:code; bun run type-check; bun run test:geometry (92 checks); bun run test:reporting (115 checks); focused oxfmt check; git diff --check. The prior green browser evidence remains applicable because this rewrite is behavior-identical, so no serialized browser rerun was requested.

Finalization evidence: the complete TASK-090 change set received an independent REVIEW_CLEAN. The final trusted-pointer browser run returned 0 of 18 fixed-point changes, a genuine independently computed endpoint separation of 6.5623997623614794e-12 scene pixels within the 1.0 gate, rejection of the exact 2 px negative control, a fixedpoint pane with text1, and successful duplicate-index repair. The later integrated sequential bun run check also passed.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added a genuine browser-versus-server differential for focused human arrow bindings, corrected the measured rounded-rectangle mismatch with the narrow pinned intersection math, and fixed the comparison-board fixture. Verified with geometry/reporting checks, exact zero-diff browser rendering, a 6.56e-12 px independent endpoint match, rejection of the 2 px negative control, full bun run check, and independent review.
<!-- SECTION:FINAL_SUMMARY:END -->
