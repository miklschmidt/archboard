---
id: TASK-130.10
title: Convert the four browser checks to one typed serial lane
status: Done
assignee:
  - '@codex'
created_date: '2026-08-28 01:05'
updated_date: '2026-08-29 03:38'
labels: []
dependencies:
  - TASK-130.01
  - TASK-130.02
  - TASK-086
  - TASK-130.06
  - TASK-130.08
  - TASK-130.09
references:
  - scripts/check-fixed-point.mjs
  - scripts/check-human-edit-performance.mjs
  - scripts/check-live-session.mjs
  - scripts/check-typed-text.mjs
  - docs/agents/test-suite.md
  - TASK-086
  - 'https://bun.com/blog/bun-v1.4#bun-test'
parent_task_id: TASK-130
priority: high
type: task
ordinal: 145000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Convert fixed-point, human-edit performance, live-session, and typed-text checks only after the non-browser process lanes have established typed lifecycle patterns. These are the four checks that drive a real renderer and must never share the machine concurrently.

Use native Bun assertions inside the browser tests, with one small typed command adapter for prerequisite detection, frontend build reuse, and the documented could-not-run exit. Do not use Bun file parallelism for this lane.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 check-fixed-point, check-human-edit-performance, check-live-session, and check-typed-text are replaced by typed Bun tests reached only through one explicitly sequential browser lane.
- [x] #2 The lane refuses to claim a pass without agent-browser, returns the documented could-not-run outcome, asserts a headless user agent, and never maps a browser window.
- [x] #3 Frontend freshness is checked once per lane so unchanged sources build at most once, and each browser process and canvas uses verified ownership and bounded cleanup.
- [x] #4 Fixed-point coverage preserves zero document diff, malformed-geometry recovery, off-screen inspection export, exact PNG and manifest bytes, bridge suppression, clipping, and unchanged visible pane state.
- [x] #5 Live-session coverage preserves all 42 cycles, equality after every cycle, server-update ordering, user-edit scheduling, held-board behavior, and hold-generation recovery.
- [x] #6 Typed-text coverage still lets Excalidraw mint IDs, exercises open editors across writes, and proves every character and rename reaches the board and note.
- [x] #7 Human-edit performance preserves the 10,000-element human-only reproduction, compact acknowledgement, no scene replacement, structural response checks, same-run relative frame diagnostics, and the rule against fixed millisecond product gates.
- [x] #8 Every test source file is at most 500 lines; browser tests use condition polling and named timing margins, and no test enters a parallel, random, changed-only, or generic recursive lane by accident.
- [x] #9 Representative fixed-point, typing, report-order, renderer, and human-response regressions fail before the old scripts are deleted, and the complete browser lane passes repeatedly in documented order.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Fixed base, dependencies, and amended scope. Work from fixed BASE and reviewed HEAD 7471c38e6f59279b5ca09c7c4405c8b689482a1f. TASK-130.01, TASK-130.02, TASK-086, TASK-130.06, TASK-130.08, and TASK-130.09 are integrated. Do not start TASK-134, TASK-136, or TASK-137. Product behavior is out of scope and tests/system/support/owned-canvas.ts remains read-only. This task owns tests/system/browser/**, the exact adapter support in tests/system/repository-policy/support/test-inventory.ts and its assertions in test-inventory.test.ts, exactly three named browser-test timing aliases in src/shared/timing/timing.ts, package.json, the four predecessor deletions, the minimal transitional docs/agents/test-suite.md update, and live source-comment references to deleted checks. Implementation stays paused until this complete plan is approved.

2. Fixed-point owner map from scripts/check-fixed-point.mjs. Add six tests/system/browser owners:
- fixed-point-document.test.ts owns every agent-creatable element type, negative input paths, font readiness by measured width, the exact ignored-field allowlist, proof that the frontend strips no extra field, server/browser element and field equality, zero document diff, exact node/arrow geometry, and product bridge metadata, styling, mask, redraw, and z-order.
- malformed-geometry-recovery.test.ts owns malformed persisted scratch startup, the visible actionable board error, unchanged malformed bytes, no malformed renderer input, corrected scratch reload, atlas-open refusal and recovery, finite zoom, and finite registered viewport.
- pane-telemetry-recovery.test.ts owns non-finite browser telemetry suppression, no POST through PANE_DEBOUNCE_MS plus the named negative margin, exact replay of the prior finite rect and viewport after correction, and clearing the invalid publication key.
- arrow-binding-differential.test.ts owns the trusted human rebound at focus 0.9 and gap 15, held human report, browser oracle, unopened server fixture, exact target move, 1.0 scene-pixel tolerance, two-pixel negative control, and complete endpoint, binding, and geometry diagnostics.
- finding-export.test.ts owns off-screen inspection export, one valid and one unmarked crossing, exact persisted report order, separate exact PNG, manifest, and full-screenshot byte proofs, embedded pixels, focus clipping, z-order, viewport independence, and unchanged visible board, scene, selection, viewport, and screenshot.
- shell-layout.test.ts owns desktop shell geometry, touch targets, active-board identity, five activity rows and timestamps, the 420-pixel no-overflow contract, navigator switching, and exact original-board return.

3. Human-performance and typed-text owners remain single-run owners. human-edit-performance.test.ts is the sole owner of the predecessor 10,000-element human-only run. It seeds only through the human report route and preserves the same drag, resize, and typing sequence, one in-flight plus one latest queued report, progress versus idle scheduling, compact document-free acknowledgement, no full-scene replacement or no-op tail, request, response, JSON, and body sizes, hold, report, and release counts, fsync evidence, locally visible edits, and the same-run median-relative frame diagnostic with no fixed millisecond product gate. Do not duplicate the fixture or separate the measurements from the structural response that produced them.
typed-text.test.ts is one browser run containing both real text-tool input and double-click label input. Excalidraw mints both long IDs; each editor remains open across a server write; every character survives; derived renames preserve binding; pane, server, and note agree; the posted-ID recorder positively sees both settled text IDs; no posted text ID remains renameable; and the single final note contains both renamed elements. No programmatic or server-minted substitute is allowed.

4. Live-session owner map and literal claim leaves. live-session-convergence.test.ts owns seeded labels, arrow, and text; the exact 42-cycle rotating agent/human sequence; equality after every cycle; the exact comparison and 0.0012-pixel text-width allowance; proof both sides wrote; and the bounced-equals-zero guard. server-update-ordering.test.ts owns the four deterministic resize, retype, delete, and move interleavings between updateScene and baseline recording, in-flight rescheduling, sparse-drag overdue progress, queued-report preservation, no report caused by a server update, and exact final convergence. hold-generation.test.ts owns delayed A1, A-to-scratch-to-A switching, distinct A2 ownership, late A1 not clearing A2 or scheduling an old retry, and persistence of A2. human-hold-persistence.test.ts owns mid-drag broadcasts, visible unreported edits, pre-write note-change notification without adopting the on-disk document, exact presentation order of reload, overwrite, and save-elsewhere, execution of overwrite only, stopped-saving state, authoritative mutex retry, one single-flight hold, and locally editable recovery.
claim-interaction.test.ts owns the claimed holder who and reason; view=false while a claim remains readable; exact "Take back control" text; zero hold/report deltas after camera movement; the successful agent write after camera movement; the latest narration and per-write step line; the combined banner explanation and framing; the off-pane board-bar line; local drag visible before persistence; exactly one hold; eventual persistence convergence; exact 409/CLAIM_REVOKED for content takeover; the second claim's exact control; Take back control button activation; returned what=null and view=false; exact 409/CLAIM_REVOKED on the next write; frame without takeover; local pointer responsiveness; content revocation; explicit take-back; told-once next write; and disconnected-pane held-by-default behavior. These literal leaves stay beside their assertions and in the bidirectional ledger.

5. Narrow support, typed inputs, and the traced canvas. Add support/agent-browser.ts for typed argv, stdin, stdout, stderr, JSON decoding, retained CLI-child timeouts, condition polling, unique session and namespace, owned socket paths, and exact-session cleanup; support/frontend-build.ts for the single freshness decision; support/page-scene.ts for React-fiber scene access and instrumentation mechanics only; and run-browser-lane.ts as the package adapter. Reuse tests/system/boards/support/http.ts for liveness-aware JSON requests instead of copying a raw fetch wrapper. Add fixtures/fixed-point-scene.ts, human-performance-scene.ts, and live-session-scene.ts for typed authored inputs only, deriving Excalidraw and product views from exported vendor or module-root types. fixtures/traced-canvas-process.ts execve replaces the OwnedCanvas child with strace --daemonize=grandchild -f -e trace=fsync -o <owned trace> bun <real server>. The daemonize mode keeps the server tracee as the direct child, so /health.pid must equal OwnedCanvas.pid. The human owner proves exact fsync output and bounded disappearance of the tracer descendant after canvas disposal. If the PID proof fails, stop for plan review. Do not weaken health ownership, copy OwnedCanvas, attach from a sibling process, or add an unverified wrapper.

6. Executable adapter selection and prerequisite negatives. Executable adapter correction 2 appears here once: export a pure side-effect-free selection validator from run-browser-lane.ts or its browser support and import it into the existing tests/system/repository-policy/test-inventory.test.ts. It accepts only two forms. The package form is exactly bun tests/system/browser/run-browser-lane.ts followed by all 13 canonical literal paths in their package order. The focused form is exactly bun tests/system/browser/run-browser-lane.ts --focus <canonical-path> [<canonical-path>...], with one or more unique paths in canonical relative order. Before prerequisite probing or frontend inspection, reject missing paths, duplicates, reordering, unknown paths, directory or recursive selectors, --changed, --randomize, --shard, and every extra flag or argument. Unit assertions cover every accepted and rejected form. Keep package occurrence and reachability accounting as separate inventory tests.
Add real subprocess assertions that a PATH containing the selected Bun executable but no agent-browser makes a canonical focused command exit exactly 2 without invoking frontend build or starting the owner, and that a PATH containing Bun and agent-browser but no strace does the same for a focus containing human-edit-performance.test.ts. Sentinels prove neither negative creates the bundle, owner root, browser session, canvas, or trace. Preflight spawn errors, signals, and nonzero statuses are could-not-run outcomes. After selection and prerequisites, frontend-build.ts checks frontend/**, src/**, vite.config.js, tsconfig.frontend.json, package.json, and bun.lock once, builds at most once, and requires dist/frontend/index.html to exist and be at least as new as every input. No child test builds.

7. Inventory and intermediate interface cutover. Extend tests/system/repository-policy/support/test-inventory.ts to recognize only the exact typed browser adapter command in addition to ordinary bun test selectors. Preserve literal occurrence counts so a duplicate adapter argument cannot collapse to one owner. Extend test-inventory.test.ts with separate passing, missing, duplicate, reordered, unknown, unreachable, and multiply reached adapter inventory cases. Remove the finished transitional legacy-check matcher and the passing test hardcoded to check-fixed-point.mjs, because these are the last check-*.mjs files. At atomic cutover, update docs/agents/test-suite.md to name the one transitional bun run test:browser lane, its exact order, prerequisites, freshness decision, exit-2 contract, focused adapter command, and strict serial rule. Replace live arbiter or caught-regression references in src/ui/canvas/elements.ts, src/runtime/engine/expand-elements.ts, and src/runtime/engine/board-io.ts with their typed owner paths. Historical ADR and measured-design prose may retain historical names.

8. Adapter lifecycle, nested owner cleanup, and environment isolation. Adapter lifecycle correction 1 appears here once: run-browser-lane.ts installs one single-flight SIGINT and SIGTERM path before acquisition, creates one outer owned root, and immediately registers outer-root cleanup before any nested resource. For each literal file it spawns a retained detached Bun child and process group with the exact command bun test --no-orphans --isolate --max-concurrency=1 <literal-file>. It passes the outer root and one file-specific child directory to the owner. On interruption it signals the retained child handle, waits the named bound, and only then forces the retained spawn-authorized negative PGID. It awaits the actual exit. It never signals a parsed or discovered PID. After every file and on failure, it audits child descendants, the unique agent-browser namespace, socket and daemon, canvas listener, vault, trace, and file-specific directory. The runner removes the outer root last. Never register root cleanup after a nested resource.
Each owner starts with await using and a nested AsyncDisposableStack. It immediately registers later acquisitions in order: canvas, exact browser session, browser or render CLI children, sockets, timers and intervals, trace evidence, and any later temporary directories. LIFO disposal clears later children and timers, then the exact browser session, then the canvas. agent-browser close is only a close request; cleanup must independently observe that the owned namespace, socket, browser, and daemon have disappeared, waiting through the one bounded daemon-idle interval when needed. An absent cleanup observation is a failure, not permission to assume the daemon stopped. Before predecessor deletion, disposable proofs force an assertion failure and interrupt the runner while the canonical focused pane-telemetry-recovery.test.ts owner is live. Both prove the complete child, process-group, namespace, daemon, canvas, listener, vault, trace, and root cleanup path.
Environment isolation correction 3 appears here once: construct the browser CLI environment from a documented allowlist, never from {...process.env}. Keep only the selected Nix-wrapper-aware PATH, LANG=C.UTF-8, LC_ALL=C.UTF-8, NO_COLOR=1, and owned HOME, XDG_CONFIG_HOME, XDG_STATE_HOME, TMPDIR, AGENT_BROWSER_SOCKET_DIR, AGENT_BROWSER_SESSION, AGENT_BROWSER_NAMESPACE, plus one bounded AGENT_BROWSER_IDLE_TIMEOUT_MS. Inherit no AGENT_BROWSER_* value before setting those owned keys. Do not carry provider selection or credentials, CDP or auto-connect, HTTP_PROXY, HTTPS_PROXY, ALL_PROXY, NO_PROXY, profile, restore, state, config, plugin, init-script, extension, executable override, headed, user-agent, or launch-argument settings. Preserve the executable selected by the current Nix PATH rather than replacing it with an unrelated browser binary.
Build the canvas child environment from the same allowlist plus explicit owned values. Clear ARCHBOARD_VAULT, ARCHBOARD_REPOS, ARCHBOARD_INJECT, ARCHBOARD_INJECT_LOUD, ARCHBOARD_INJECT_THREAD, ARCHBOARD_INJECT_DEBOUNCE_MS, ARCHBOARD_INJECT_MIN_INTERVAL_MS, ARCHBOARD_SETTLE_MS, ARCHBOARD_SETTLE_MAX_MS, CYCLES, HOST, PORT, EXPRESS_SERVER_URL, ENABLE_CANVAS_SYNC, EXCALIDRAW_NO_AUTOSTART, CODEX_HOME, LOCALAPPDATA, and every ARCHBOARD_TEST_* or lifecycle-fixture key before setting only the owner values needed by that case. Set owned LOG_FILE_PATH and vault explicitly. Every owner asserts navigator.userAgent contains Headless after launch.

9. Timing and capacity gate. Positive waits use condition polling for pane registration, reports, note marks, held or stopped-saving state, modal appearance, overwrite completion, editor posting, tracer completion, daemon disappearance, and convergence. Full-window waits remain only for negative observations such as no telemetry POST, no stale hold retry, no camera report, and progress-versus-idle ordering. Add exactly TEST_BROWSER_COMMAND_TIMEOUT_MS, TEST_BROWSER_POLL_MS, and TEST_PANE_DEBOUNCE_MARGIN_MS to src/shared/timing/timing.ts. Each alias is exactly two physical lines including its pull-against comment. The final timing.ts ceiling is 497 physical formatted lines. Stop if it exceeds 497 or a fourth addition is needed.
Capacity correction 5 appears here once: every other new or expanded TypeScript test owner, support, fixture, runner, or inventory file has a formatted ceiling of 479 physical lines. Current projections remain below that ceiling except human-edit-performance, which is now projected at 479 rather than 480. If it reaches 480 before its literal observables are complete, stop and request a split by one real observable owner. Do not compact expectations, move an expected value into a helper, or add an expected-value helper. All product imports use module-root entrypoints. Support contains mechanics only.

10. Bidirectional predecessor ledger and mutation classes. Build a disposable source-line ledger for every predecessor check() call and split every compound predicate into literal leaves. Record source line plus ordinal, literal expected value or predicate, exactly one native owner and expect location, then audit in both directions. Require zero unmapped predecessor leaves, zero native behavioral expectations without a predecessor or approved coverage-uplift row, and zero duplicate owners. Keep the four scripts and package mappings while native tests are authored. Run the fixed BASE predecessor baseline unconditionally.
Literal predecessor and mutation correction 4 appears here once: pair old and new only for identical product mutations. Product pairs cover fixed-point field drift and ignored-field growth, malformed geometry reaching Excalidraw, retained invalid pane publication key, a two-pixel endpoint error, separate PNG and manifest byte drift, bridge suppression or clipping, narrow-shell overflow or wrong original-board return, full-scene replacement on compact acknowledgement, loss of the latest queued report, divergence within 42 cycles, one deterministic ordered edit dropped, stale A1 clearing A2, wrong pre-write note notification or held-recovery ordering, camera revoking a claim or emitting a report, text-editor withholding removal, and derived rename removal. Compare exact status, streams, screenshots, manifests, event and report order, IDs, note bytes, cleanup, and ledger predicates.
Replacing the same-run relative performance diagnostic with a fixed threshold is an oracle mutation, not a predecessor behavioral pair. Removing the compound typed recorder or final-note expectation is also an oracle mutation. Each must kill the bidirectional source-line ledger and static native-source audit. Record those failures separately from product parity. Run selected behavioral mutations only through the exact focused adapter form. Run the complete native adapter twice consecutively in canonical order before deleting predecessors.

11. Atomic package, deletion, docs, and inventory cutover. Replace the four old browser-chain entries with one test:browser at their first prior position and set it exactly to:
bun tests/system/browser/run-browser-lane.ts tests/system/browser/human-edit-performance.test.ts tests/system/browser/fixed-point-document.test.ts tests/system/browser/malformed-geometry-recovery.test.ts tests/system/browser/pane-telemetry-recovery.test.ts tests/system/browser/arrow-binding-differential.test.ts tests/system/browser/finding-export.test.ts tests/system/browser/shell-layout.test.ts tests/system/browser/typed-text.test.ts tests/system/browser/live-session-convergence.test.ts tests/system/browser/server-update-ordering.test.ts tests/system/browser/hold-generation.test.ts tests/system/browser/human-hold-persistence.test.ts tests/system/browser/claim-interaction.test.ts
Remove test:human-performance, test:typing, and test:live-session plus their chain entries. Delete exactly scripts/check-fixed-point.mjs, scripts/check-human-edit-performance.mjs, scripts/check-live-session.mjs, and scripts/check-typed-text.mjs. Include inventory, minimal docs, and live comment-reference changes in the same cutover so no integrated state has unowned tests or dangling active commands. Run bun run test:suites immediately and require every native test exactly once.

12. Exact sequential commands and validation. During authoring, run every owner alone only through these exact focus commands:
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/human-edit-performance.test.ts
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/fixed-point-document.test.ts
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/malformed-geometry-recovery.test.ts
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/pane-telemetry-recovery.test.ts
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/arrow-binding-differential.test.ts
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/finding-export.test.ts
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/shell-layout.test.ts
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/typed-text.test.ts
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/live-session-convergence.test.ts
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/server-update-ordering.test.ts
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/hold-generation.test.ts
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/human-hold-persistence.test.ts
bun tests/system/browser/run-browser-lane.ts --focus tests/system/browser/claim-interaction.test.ts
Use the same exact focused form for selected product mutations and the pane-telemetry assertion-failure and interruption cleanup proofs. Before deletion, run the full package-form adapter twice consecutively and audit every owned resource. After atomic cutover run, in order: bun run test:suites; bun run type-check; bun run lint; bun run fmt:check; git diff --check; formatted line audit; bidirectional ledger and static audit; active-reference audit; and full child, process-group, namespace, daemon, listener, socket, vault, trace, and root audit. Then run bun run test:browser once, followed by exactly one bun run check. Because check contains test:browser, this gives two post-cutover full lanes and one complete repository gate. Never overlap a browser lane with another browser, hot-reload, formatter, type checker, or broad test process. Do not run a separate bun run test or a second bun run check.

13. Review, downstream reconciliation, and parent handoff. After approved implementation and validation, create the TASK-130.10 checkpoint commit without pushing. Request independent high-stakes review of the complete fixed range 7471c38e6f59279b5ca09c7c4405c8b689482a1f..HEAD against this plan, all nine acceptance criteria, repository rules, ledger, inventory, lifecycle, environment isolation, seriality, and cleanup evidence. Route valid findings back through the same owner until REVIEW_CLEAN. Record evidence in TASK-130.10 notes, but leave all nine acceptance criteria unchecked, finalSummary null, and terminal status and finalization to the parent.
Downstream reconciliation correction 6 appears here once: TASK-130.10 must not edit TASK-130.11 now. Before TASK-130.11 dispatch, the parent must amend its stale exact 16-file selector and its comment-reference ownership to the landed TASK-130.10 13-file list. TASK-130.11 must not recreate human-edit-acknowledgement.test.ts, typed-text-element.test.ts, or typed-label.test.ts. PARENT_ACTION after this plan is approved and implemented is to perform that TASK-130.11 amendment before dispatch, finalize TASK-130.10 only after clean review, and never push from this worker.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implementation evidence (2026-08-29):
- Migrated the four predecessors into one strict serial adapter and 13 canonical typed Bun owners. The fixed BASE predecessor baseline passed before authoring; each canonical owner passed through the exact focused adapter form.
- Lifecycle proofs: a forced assertion failure and a live SIGINT against pane-telemetry-recovery both reaped the retained Bun child/process group, agent-browser namespace/socket/daemon, canvas/listener, vault, trace, file root, and outer root. Prerequisite subprocess negatives exit exactly 2 before build/owner acquisition when agent-browser is absent, and when strace is absent for a human-performance focus.
- Bidirectional ledger audited 174 predecessor check() calls into 357 literal leaves: zero unmapped predecessor leaves, zero unowned native expectations, and zero duplicate behavioral owners. Owner call/leaf totals: fixed 16/27; malformed 10/21; pane 2/6; arrow 13/44; finding 10/27; shell 11/21; human 18/51; typed 26/44; live convergence 12/17; ordering 7/12; hold generation 5/16; human hold 25/40; claim 19/31.
- Product parity mutations killed both predecessor and native owner for fixed-point field stripping, two-pixel arrow endpoints, typed editor withholding, server baseline loss, and full human documents. Static oracle mutations separately killed the relative-performance and compound typed recorder/note guards.
- Both complete pre-deletion package-form lanes passed consecutively. After the atomic package/deletion/inventory/docs/comment cutover, test:suites, type-check, lint, fmt:check, git diff --check, line/ledger/reference/resource audits, exactly one bun run test:browser, and exactly one bun run check passed.
- Formatted capacity: human-edit-performance 470 lines; every other new or expanded TypeScript file <=441 except timing.ts, exactly 497 with exactly the three approved two-line aliases.
- All owned resources were absent after both post-cutover lanes. No push performed. Parent still owns complete-range review, acceptance checks, final summary, terminal status, and TASK-130.11 reconciliation.

Review remediation (2026-08-29, follow-up to 809e5b0):
- Derived typed-text and human-performance vendor element projections from the authoritative exported ExcalidrawElement type; behavioral assertions are unchanged.
- Extracted only the duplicated ignored-field and canonical snapshot mechanics from live-session-convergence and server-update-ordering into browser-owned page-scene support; scenario expectations and diagnostics remain local.
- Retained frontend builds as detached owned children. SIGINT/SIGTERM now perform bounded handle TERM, retained negative-PGID KILL if needed, awaited exit, outer-root cleanup, and exact 130/143 outcomes. Fake blocked-build subprocess proofs pass for both signals without starting a browser or canvas.
- Cleanup observation is AGENT_BROWSER_IDLE_TIMEOUT_MS plus TEST_BROWSER_POLL_MS, and pollUntil takes a final deadline sample. The timing proof covers both the extra margin and deadline acceptance.
- Corrected test-suite documentation to describe the exact typed adapter/occurrence inventory actually enforced.
- Follow-up validation: test:suites 27 pass (including prerequisite, build-interruption, and cleanup-timing subprocess proofs); test:boundaries 8 pass; type-check, lint, fmt:check, and git diff --check pass. All owned TypeScript files remain <=468 except timing.ts exactly 497. Resource audit found no owned browser/canvas/build/process/root residue.
- Per parent instruction, no second bun run test:browser or bun run check was run.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Replaced the four legacy browser scripts with one strict typed serial adapter and 13 native Bun browser owners. The lane now owns prerequisite checks, frontend freshness, allowlisted headless environments, retained process-group and agent-browser cleanup, exact inventory, and failure/interruption proofs. Preserved fixed-point, renderer/export, typed-text, human-performance, and all live-session/claim contracts with source-line ledger and mutation evidence.

Validation: all focused owners passed; two consecutive pre-cutover native lanes passed; post-cutover inventory and static gates passed; exactly one bun run test:browser and exactly one bun run check passed; final review remediation passed inventory, boundaries, type-check, lint, formatting, diff, and fake build-interruption proofs without another browser/full run. Independent Standards and Spec rereviews are REVIEW_CLEAN. No push performed. TASK-130.11 must be reconciled to this landed 13-file selector before dispatch.
<!-- SECTION:FINAL_SUMMARY:END -->
