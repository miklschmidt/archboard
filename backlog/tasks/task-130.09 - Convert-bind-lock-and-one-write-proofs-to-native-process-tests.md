---
id: TASK-130.09
title: 'Convert bind, lock, and one-write proofs to native process tests'
status: In Progress
assignee:
  - '@codex'
created_date: '2026-08-28 01:05'
updated_date: '2026-08-29 00:53'
labels: []
dependencies:
  - TASK-130.01
  - TASK-086
  - TASK-130.06
references:
  - scripts/check-local-bind.mjs
  - scripts/check-lock.mjs
  - scripts/check-one-write.mjs
  - TASK-086
  - docs/adr/0016-one-writer-at-a-time-per-board.md
parent_task_id: TASK-130
priority: high
type: task
ordinal: 144000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
check-local-bind, check-lock, and check-one-write prove behavior that an in-process test cannot answer. Convert their orchestration and assertions to typed Bun tests while retaining real competing processes, real loopback sockets, and write counting on the wire.

TASK-086 owns generic canvas startup and cleanup. This task owns only the bind, lease-exclusion, claim and hold, and one-write product proofs.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 check-local-bind, check-lock, and check-one-write are replaced by typed native system tests with no in-process substitute for their competing-process or wire observations.
- [ ] #2 Bind tests distinguish the owned child from a foreign or stale responder and preserve the public refusal and recovery behavior on loopback ports.
- [ ] #3 Lock tests run at least two real processes against one vault and preserve lease acquisition, denial, expiry or recovery, claim interaction, content revocation, and camera non-revocation behavior asserted today.
- [ ] #4 One-write tests count real note-changing requests through the proxy and prove each requested align, patch, promote, import, or batch action reaches the note as exactly one write under one lock acquisition.
- [ ] #5 Owned canvas processes use TASK-086 lifecycle behavior; proxy and competitor processes add equivalent typed ownership, bounded shutdown, stderr retention, exit waiting, and cleanup proof.
- [ ] #6 Assertion failure and interrupted-process fixtures leave no owned child, listener, proxy, lease, port, or vault and identify process death separately from a product assertion.
- [ ] #7 Every test source file is at most 500 lines and representative foreign-bind, double-writer, stale-lease, and accidental multi-write mutations fail before the legacy scripts are deleted.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Fixed base and scope. Work from BASE and HEAD 1b177ced1b8e7934798137400c74d9c3c3dd02da. TASK-130.06 is integrated, and tests/system/support/owned-canvas.ts remains read-only and owns every healthy canvas process. Foreign health responders, failed starts, raw lock competitors, resource fixtures, CLI children, and the counting proxy stay real separate processes. No in-process substitute may replace bind competition, cross-process exclusion, interruption cleanup, or wire counts. No production, dependency, eval, CI, browser-owner, or unrelated lane change is planned. TASK-130.10 keeps real browser camera and content interaction. The earlier live-session anomaly adds no scope here.

2. Local bind. Add tests/system/process-contracts/fixtures/canvas-start-environment.ts below 50 lines and use it only as startOwnedCanvas serverPath. It receives the dot-prefixed public src/server.ts entry path through one test-only environment value, constructs the exact child environment by deleting HOST for default bind, setting HOST='::' for broad-bind refusal, or deleting ARCHBOARD_VAULT for no-vault startup, then calls process.execve(process.execPath, [process.execPath, '--preserve-symlinks', '--preserve-symlinks-main', serverPath], environment). execve replaces the retained child without changing its PID, so owned-canvas still authorizes lifecycle and exact health.pid while the real product entrypoint is main. Do not dynamically import or call startServer in the fixture. This resolves the contradiction that owned-canvas otherwise forces HOST=127.0.0.1 and cannot test product default bind. Add tests/system/process-contracts/fixtures/health-responder.ts for foreign or stale schema-valid health. local-bind.test.ts owns the dot-prefixed symlink checkout with both preserve-symlinks flags, exact health.pid, installed CSS bytes and content type, default IPv4 reachability and IPv6 refusal, static 200/404/404, duplicate, broad-bind, and no-vault exits and messages, exact CLI exit 3, and recovery. tests/system/process-contracts/support/static-probes.ts creates collision-safe probes and restores without overwriting pre-existing files.

3. Isolated module owners. Run board-lock-lease.test.ts, board-lock-announcements.test.ts, and board-claim.test.ts as three separate one-file Bun segments joined by && in test:lock and focused validation, followed by one system segment. Each creates and removes its own vault, sets and restores ARCHBOARD_VAULT before first dynamic import, and passes alone. Each finally first clears its test-owned timers and sets watchBoardLocks(null), onBoardSweep(null), and onBoardLockChanged(null); then releases every owned claim and hold; then calls forgetLockAnnouncements last among board-lock operations so release-created linger timers, claims, revocations, announcements, and watch state are cleared. It then restores logger.warn and environment and removes the vault. Separate processes prevent import-cached configuration and kept timers or sinks from crossing owners.

4. Module mapping. board-lock-lease owns free and held writes, refusal type, data, message, wait, same-holder renewal, joined writes, release authority, lease expiry and recovery, timing relationships, patient wait, canonical and nested lock identity, and unreadable stale recovery. board-lock-announcements owns note-watch passenger failure and warning, held news, renewal silence, LOCK_FREE_LINGER_MS free news, eight-write coalescing, and sink lifecycle. board-claim owns claim reason and identity, twenty joined writes with no rival gap, extension, revokeClaim:false not revoking, revokeClaim:true human takeover, claim removal, exact by and reason revocation, told once, idle renewal, deadline expiry, late release, unclaimed controls, and both lapsed-live-claim branches: expired until and deleted lock file. Each lapsed branch proves takeover, claim removal, by and reason, and told once. Add a lapsed-claim mutation. Do not call these browser camera or content predicates.

5. System lock mapping. board-lock-api.test.ts owns initial pane state, hold before edit, report joining hold, waited exact 409 BOARD_HELD response, holder and since, exact current document and version, read and open exemptions, release broadcast, two-pane contention, API claim reason and doing, twenty writes, takeover, CLAIM_REVOKED nothing-was-undone text, exact partial document and version, next ordinary write, all twenty elements, late release false, and missing board, reason, or holder status 400. cross-process-lock.test.ts first owns a raw public-board-lock child taking and releasing a shared-vault lock, remote process identity, exact another canvas wording, and recovery. It then owns two healthy canvases over one vault, cross-canvas denial and recovery, lock-file visibility, remote watcher broadcast before touch, second-canvas takeover, no resurrection, and first-agent revocation. lock-source-policy.test.ts owns only the sole lock-path builder, sole board_lock sender, one exported sink registration, and no lock-directory bypass.

6. AC6 cleanup owner. Add resource-cleanup.test.ts at 220 to 300 lines and tests/system/process-contracts/fixtures/process-resource-owner.ts below 160. The direct and outer fixtures acquire, and immediately register in order, a real health-responder upstream listener, a raw public-board-lock child holding one named lease in the owned vault, and the separate counting proxy. Forced throw or SIGTERM cleanup awaits all retained children and listener close callbacks, then proves both listener ports reusable, the exact lock file absent, and the owned vault/root removed. Reuse TASK-130.06 canvas cleanup coverage without duplication.

7. Peer, proxy, schemas, and environment. tests/system/process-contracts/support/owned-peer-process.ts registers ChildProcess error, exit, stdout, and stderr before readiness, validates readiness pid equals child.pid, and signals only the retained ChildProcess handle, never a numeric PID authorization. It uses bounded SIGTERM then retained-handle SIGKILL and awaits final exit. Startup, listen, parse, and readiness failures reject after cleanup with stderr. Disposal is idempotent. tests/system/process-contracts/support/counting-proxy.ts is a separate real loopback process with typed reset and snapshot control excluded from forwarding and counts. Records preserve ordered method, pathname, query, and raw body. tests/system/process-contracts/support/process-http.ts owns typed requests, responses, liveness, and subprocess JSON. Every health, readiness, control, and subprocess JSON payload has a named Zod schema with full invalid-payload and process diagnostics.

8. Sanitized children. Canvas, peer, proxy, fixture, and CLI children receive owned HOME, XDG_STATE_HOME, LOG_FILE_PATH, and ARCHBOARD_VAULT with stable LOG_LEVEL and NO_COLOR. Clear LOCALAPPDATA, CODEX_HOME, EXPRESS_SERVER_URL, ENABLE_CANVAS_SYNC, ARCHBOARD_REPOS, every ARCHBOARD_INJECT key, settle overrides, inherited HOST and PORT, and EXCALIDRAW_NO_AUTOSTART unless explicitly supplied. CLI adds only the owned proxy URL and explicit inputs. Only the three isolated module owners mutate ambient env, restoring exact strings or absence. The local-bind no-vault server and CLI cases are the only exceptions: after constructing the sanitized base environment they delete ARCHBOARD_VAULT; the CLI case also keeps EXCALIDRAW_NO_AUTOSTART absent and supplies only its explicit unreachable loopback URL.

9. One-write mapping and exclusive origin ownership. element-ops-one-write.test.ts owns one-record proxy proofs for align, distribute, lock, unlock, group, ungroup, and labelled or wired movement, exact geometry and membership, label tolerance 0.5, arrow reroute, same-write settlement, frontend_sync preservation on a human element, absence on an agent element, and agent feed origin. apply-one-write.test.ts owns mixed apply, minted IDs, touched data, fingerprint, optional document, invalid-ID zero writes, isolated atomic refusal, raw /elements/changes browser versus agent answer classification, held no-optimistic answer, compact acknowledgement, canonical corrections, and persisted equality. Apply exclusively owns answer classification and corrections. promotion-delete-bridge-one-write.test.ts owns stencil promote and demote, refusal, multi-delete and refusal, and bridge create and remove with exact receipts and one record.

10. Imports and snapshots. Split import into import-replace-one-write.test.ts for normal image replace with exact receipt, wire record and body, canonical element, ID, label, binding, index, file, version, persistence-before-delta, exact files frame, and selection cleanup; and import-merge-held-one-write.test.ts for merge plus held replace, exact receipt and diagnostic, one batch, unchanged note bytes, mtime, and version, and exact held elements, files, and write count. Each has fresh lifecycle and shared inputs only, projected 280 to 360 lines. Import owns no source tagging unless a literal BASE predicate is cited and reviewed. snapshot-one-write.test.ts owns cross-board zero writes, element-only restore, empty files, repeat restore and unchanged source, held restore, exact receipt, wire record and body, broadcast, version, note bytes and mtime, and selection cleanup. tests/system/process-contracts/fixtures/import-scenes.ts and tests/system/process-contracts/fixtures/snapshot-scenes.ts contain typed inputs only.

11. Write-boundary evidence. write-boundary-policy.test.ts literally asserts deny-by-default non-GET or HEAD /api board middleware precedes all eleven note-changing routes; none matches NOT_A_BOARD_WRITE; exactly one middleware acquisition calls holdBoard with board:key and holder:writer before next; all eleven call answerBoardWrite; the wrapper calls writeBoard once; application has no writeBoardContent or applyElementInput bypass; board-write has one writeBoardContent and one elements_changed sender. Dynamic owners prove exact proxy method, path, query, raw body, and one-record behavior. Add no acquisition instrumentation. The static proof establishes the sole route path, not a second runtime acquisition count.

12. Baseline, parity, and mutations. Keep all three legacy scripts present and mapped during authoring. Run the BASE legacy baseline unconditionally. For the wrong-health-pid ownership mutant only, record predecessor-survives/native-kills as an intentional coverage uplift because BASE check-local-bind treats any HTTP-200 /health response as readiness and has no PID predicate. Retain identical old/new failure pairs for every remaining shared predecessor behavior. Do not alter the legacy script, native assertion, product, or mutation. In disposable checkouts pair old and new failures for IPv6 or broad listening, double writer, stale lease recovery, both lapsed-claim branches, claim gap, missing remote broadcast, per-element align PUTs, partial apply, per-line promote, two-write bridge, replace empty exposure, and snapshot old-file retention. Compare exact status, body, streams, note bytes and mtime, ordered requests and raw bodies, broadcasts, and residue. Interrupt peer, proxy, raw lock owner, and resource fixture and prove cleanup. Delete legacy only after evidence is recorded.

13. Atomic package mapping. After parity, one cutover changes exactly three mappings and deletes exactly scripts/check-local-bind.mjs, scripts/check-lock.mjs, and scripts/check-one-write.mjs. test:bind is bun test tests/system/process-contracts/local-bind.test.ts tests/system/process-contracts/resource-cleanup.test.ts. test:lock is bun test src/runtime/engine/tests/board-lock-lease.test.ts && bun test src/runtime/engine/tests/board-lock-announcements.test.ts && bun test src/runtime/engine/tests/board-claim.test.ts && bun test tests/system/process-contracts/board-lock-api.test.ts tests/system/process-contracts/cross-process-lock.test.ts tests/system/process-contracts/lock-source-policy.test.ts. test:one-write is bun test tests/system/process-contracts/element-ops-one-write.test.ts tests/system/process-contracts/apply-one-write.test.ts tests/system/process-contracts/promotion-delete-bridge-one-write.test.ts tests/system/process-contracts/import-replace-one-write.test.ts tests/system/process-contracts/import-merge-held-one-write.test.ts tests/system/process-contracts/snapshot-one-write.test.ts tests/system/process-contracts/write-boundary-policy.test.ts. Run inventory immediately and require each native test exactly once.

14. Projections and hard stop. Projected formatted lines: lease 330, announcements 210, claim 410, local-bind 450, resource-cleanup 260, API 470, cross-process 410, policy 110, element ops 410, apply 470, promotion-delete-bridge 340, each import owner 330, snapshot 450, boundary policy 150, owned peer 300, proxy 200, HTTP 190, static probes 130, process-resource-owner below 160, canvas-start-environment below 50, health-responder below 100, and each scene fixture below 220. Every authored TS file passes alone and stays below 500 formatted physical lines. If a projected 470 or 450 owner nears the cap, stop and request a literal observable split. Never compress expectations.

15. Validation order. Run each test alone first. Focused commands are the three separate module commands joined by &&; then one bind and lock system command containing local-bind, resource-cleanup, board-lock-api, cross-process-lock, and lock-source-policy; then one one-write command containing element-ops, apply, promotion-delete-bridge, import-replace, import-merge-held, snapshot, and boundary-policy. Audit PIDs, listeners, reusable ports, roots, vaults, leases, timers, sockets, and env. After atomic cutover run inventory immediately, then type-check, lint, fmt:check, exactly one complete bun run check with serial headless browser lanes, git diff --check, line audit, inventory audit, and full residue audit. Do not push.

16. Overlap and finalization. TASK-130.08 paths are disjoint. owned-canvas.ts is protected. package.json is the only authored overlap. AC6 belongs to resource-cleanup plus task-owned peer and proxy fixtures, while TASK-130.06 retains canvas cleanup. Raw lock and deterministic lapsed-claim proofs belong here. TASK-130.10 retains browser interaction. Keep all seven AC unchecked and finalSummary null for parent finalization.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-08-29 plan re-audit: worktree is clean and exactly at required main base 1b177ced1b8e7934798137400c74d9c3c3dd02da. Read TASK-130.06 owned-canvas lifecycle, TASK-130.08 integration, ADR 0016, module boundaries, test-suite ownership, current package/inventory code, and all three legacy scripts. No production or dependency contradiction found. Awaiting parent PLAN_REVIEW_APPROVED before source changes.

2026-08-29 CHANGES_REQUESTED plan amendment at fixed BASE and HEAD 1b177ced1b8e7934798137400c74d9c3c3dd02da. Implementation remains paused. Replaced the prior plan through Backlog CLI with the reviewer-required default-bind fixture, isolated module processes, raw public-lock peer, lapsed-claim branches, exact API predicates, AC6 resource owner, retained-handle peer lifecycle, Zod schemas, sanitized child environment, split import owners, exclusive origin ownership, literal write-boundary limits, updated selectors and projections, unconditional legacy parity, atomic inventory cutover, and final validation order. No source, package, test, dependency, acceptance criteria, status, assignee, or final summary changed. Awaiting plan rereview.

2026-08-29 rereview readback tightened every shortened support and fixture reference to its full exact repository path. No contract, selector, metadata, source, package, or test changed.

2026-08-29 approved health.pid coverage uplift evidence: in identical disposable BASE/native checkouts, changed only public /health from pid: process.pid to pid: process.pid + 1. BASE scripts/check-local-bind.mjs exited 0 and printed its pass summary because it accepts any HTTP-200 health response. Native tests/system/process-contracts/local-bind.test.ts exited 1 and reported the owned-generation diagnostic Port 36405 answered for pid 2042288, not owned pid 2042287. Restored the identical product mutant and removed both disposable checkouts, processes, and listeners. Legacy/native assertions, production, package mappings, and deletions remain unchanged.

2026-08-29 shared predecessor mutation parity: identical disposable BASE/native product changes made both owners exit nonzero for default IPv6/broad listening; double-writer acceptance; stale-lease wedging; expired-until lapsed claim; deleted-lock-file lapsed claim; claim writer gap; missing cross-process lock broadcast; per-element align fan-out (20 requests); partial apply (valid prefix persisted); per-line promote/demote fan-out (7 requests); two-request bridge create; clear-before-replace empty exposure; and snapshot old-file retention. Diagnostics named exact failed predicates, request counts, claim/revocation state, broadcast absence, version delta, held writes, or retained file membership. Every mutant was restored. Both disposable checkouts returned to zero diff and were removed. No mutant touched the implementation worktree.

2026-08-29 implementation validation at 84d9096: atomic cutover changed exactly test:bind, test:lock, and test:one-write and deleted exactly the three approved predecessor scripts. Immediate inventory passed 11/11 and proved every native test reachable exactly once. Each of 15 owner files passed alone (16 tests, 417 expectations total). Approved focused validation passed isolated lock modules 3 tests/98 expectations, bind+lock system owners 6/103, and one-write owners 7/216. Type-check, lint, fmt:check, and git diff --check passed. Exactly one complete bun run check exited 0, including the serial headless human-performance, fixed-point, typed-text, and live-session browser lanes. The 24 authored TypeScript files total 2,906 physical lines; the largest is snapshot-one-write.test.ts at 309 and every file is below 500. Final process audit found no task canvas, peer, proxy, browser, raw-lock, or resource owner. Final task temp-root audit found none; two wrong-PID mutant roots discovered earlier were removed after confirming no live owner. Eleven archboard-live roots predate this run (Aug 21-28) and are the previously recorded unrelated live-session residue, so they were preserved. Worktree scope is TASK-130.09 record, package.json, 24 native TypeScript files, and exactly three legacy deletions. No production, dependency, eval, CI, browser-owner, or owned-canvas change; no push.

2026-08-29 fixed-range review remediation at code commit 2183db8. Restored BASE literal observables for distribution, unlock, group preservation/removal, and <=0.5 label placement; added exact promotion/demotion/delete/refusal/orphaned-bridge state and wire predicates; and changed every dynamic write owner to count the complete non-read proxy snapshot before exact method/path/query/raw-body assertions. Every process owner now creates AsyncDisposableStack before its root and registers root, canvas, proxy, pane, raw peer, or timer immediately after acquisition. The resource fixture owns a nested guaranteed disposal stack, serializes signal/setup/disposal, and proves forced later-acquisition failure plus outer pre-readiness failure, real lease removal, listener-port reuse, and root removal. owned-peer now registers streams/error/exit before PID/readiness, validates readiness PID, uses retained-handle bounded TERM/KILL, and awaits the actual exit through startup/error/exit cleanup. CLI results retain argv, cwd, status, signal/error, stdout, and stderr; named schema parsing reports JSON/Zod causes; ARCHBOARD_SETTLE_MAX_MS is sanitized and directly tested.

Final remediation validation: each affected owner passed alone; isolated lock modules remained 3/98; final bind+lock system selector passed 10 tests/120 expectations; final one-write selector passed 7/259; inventory passed 11/11 with the real checkout exactly once; type-check, lint, fmt:check, and git diff --check passed. All authored TS remains below 500 lines (largest 327; process-resource-owner 148). Residue audit found no task-owned temp roots or Bun processes. The task-authorized single complete bun run check remains the already recorded passing run from the implementation range; no second complete run was started. No production, dependency, owned-canvas, package mapping, deletion, browser, eval, or CI scope changed.

2026-08-29 second fixed-range review remediation at code commit 63477d7. Every dynamic system owner now establishes an await-using AsyncDisposableStack before its first root or process acquisition and immediately registers each acquired resource; a source-policy proof checks every mkdtemp acquisition in all nine listed owners. static-probes preflights all three targets, uses exclusive wx creation, and rolls back earlier creates after a deterministic later TOCTOU collision while preserving foreign bytes. resource-cleanup directly proves pre-readiness/later-acquisition disposal and a TERM-ignoring raw-lock peer's bounded retained-handle SIGKILL, actual SIGKILL exit, listener reuse, stale-lease recovery, exact lease removal, and root removal. The raw lock readiness schema exposes its public process identity, and cross-process-lock requires exact holder.process equality.\n\nElement operations and promotion/delete/bridge now use command-specific required Zod receipt schemas and exact receipts, including groupId and bridgeId. Element operations restore exact distribution, unlock, group preservation/removal, and <=0.5 label placement. Element operations, import replace, and snapshots visibly intersect exported Excalidraw element types with Archboard metadata. Apply proves deletion absence, both updates plus deletion persisted, compact/full-document fingerprint relationships, and valid-invalid-valid middle-position atomic refusal. Import replace proves old element and old persisted ID absence before delta; import held seeds stale/reused elements and files and proves exact replacement membership and bytes.\n\nFinal focused evidence after formatting: all affected owners passed alone; test:bind passed 10 tests/85 expectations; test:lock passed the three isolated module processes at 3/98 followed by the system segment at 3/72; test:one-write passed 7/285. Inventory passed 11/11 with every native test exactly once. Boundary policy passed 8/75, module-scope policy passed 8/11, type-check, lint, fmt:check, and git diff --check passed. The 24 authored TypeScript files total 3,832 lines; largest is promotion-delete-bridge-one-write.test.ts at 422, every file is below 500, resource-cleanup is 282, and process-resource-owner is 159. The already-recorded repository-authorized complete check remains the sole full check; no second bun run check was run. No product, dependency, eval, CI, browser, owned-canvas, package mapping, or deletion scope changed.
<!-- SECTION:NOTES:END -->
